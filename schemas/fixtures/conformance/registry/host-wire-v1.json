{
  "wire_version": "1.0",
  "operations": [
    "registry.author_artifact",
    "registry.commit_plan",
    "registry.configure_source",
    "registry.connect_source",
    "registry.disconnect_source",
    "registry.get_transaction",
    "registry.list_sources",
    "registry.plan_install",
    "registry.recover_transaction",
    "registry.search"
  ],
  "request_required": ["wire_version", "operation", "request_id", "input"],
  "success_required": ["wire_version", "operation", "request_id", "ok", "value"],
  "failure_required": ["wire_version", "operation", "request_id", "ok", "failure"],
  "field_contracts": {
    "source_required": ["source_id", "kind", "endpoint_ref", "trust_policy_id", "cache_policy", "priority", "connected", "revision"],
    "release_required": ["identity", "source_id", "publisher_id", "digest", "manifest_digest", "dependency_metadata_digest", "size", "dependencies", "compatibility", "license", "provenance_digest"],
    "mutation_plan_required": ["registry_transaction_id", "plan_digest", "staged_tree_manifest", "resolved_lock_delta", "source_edits", "trust_policy_digest", "mutation_digest", "authoring_impact", "authoring_impact_digest", "host_operation_impact_digest", "evaluation_digest"],
    "install_plan_required": ["transaction_id", "plan_digest", "action", "project_id", "base_revision", "expected_definition_hash", "expected_resolved_lock_digest", "artifacts", "required_capabilities", "trust_policy_digests", "source_bindings", "dependency_snapshot", "resolved_lock_delta", "rollback_checkpoint", "expires_at", "migration_required", "creates_new_document", "mutation_digest", "authoring_impact_digests", "host_operation_impact_digest", "evaluation_digest", "authoring_impact", "host_operation_impacts", "access_decision", "host_capabilities_digest", "project_mutation_plan", "runtime_session_id"],
    "transaction_required": ["plan", "events"],
    "transaction_optional": ["planning_request", "committed_revision", "operation_result_id", "runtime_input", "superseding_revision", "runtime_result"],
    "commit_result_required": ["committed_revision", "operation_result_id", "document_id", "initial_committed_revision"]
  }
}
