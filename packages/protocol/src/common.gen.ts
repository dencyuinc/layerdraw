// Code generated by layerdraw-protocolgen/1; DO NOT EDIT.
// Schema digest: sha256:640af1b0a23344ff7d27c1827ab4bf55eeb7ca7ea9d499dc2f9e5391ae26718c
// SPDX-License-Identifier: Apache-2.0

export const schemaDigest = "sha256:640af1b0a23344ff7d27c1827ab4bf55eeb7ca7ea9d499dc2f9e5391ae26718c" as const;

export const maxWireJSONBytes = 8388608 as const;
export const maxWireJSONDepth = 128 as const;

function isObject(value: unknown): value is Record<string, unknown> {
  if (typeof value !== "object" || value === null || Array.isArray(value)) return false;
  const prototype = Object.getPrototypeOf(value); if (prototype !== Object.prototype && prototype !== null) return false;
  for (const key of Reflect.ownKeys(value)) { if (typeof key !== "string") return false; const descriptor = Object.getOwnPropertyDescriptor(value, key); if (descriptor === undefined || !descriptor.enumerable || !("value" in descriptor)) return false; }
  return true;
}

function isJSONArray(value: unknown): value is ReadonlyArray<unknown> {
  if (!Array.isArray(value) || Object.getPrototypeOf(value) !== Array.prototype) return false;
  const descriptors = Object.getOwnPropertyDescriptors(value); const keys = Reflect.ownKeys(value); if (keys.some((key) => typeof key !== "string") || keys.length !== value.length + 1) return false;
  for (let index = 0; index < value.length; index++) { const descriptor = descriptors[String(index)]; if (descriptor === undefined || !descriptor.enumerable || !("value" in descriptor)) return false; }
  return Object.keys(value).length === value.length;
}

function hasOwn(value: Record<string, unknown>, key: string): boolean {
  return Object.prototype.propertyIsEnumerable.call(value, key);
}

function hasOnlyKeys(value: Record<string, unknown>, allowed: ReadonlySet<string>): boolean {
  return Object.keys(value).every((key) => allowed.has(key));
}

function isJSONCompatible(value: unknown, active: Set<object> = new Set<object>(), depth = 0): boolean {
  if (value === null || typeof value === "boolean") return true;
  if (typeof value === "string") return hasScalarUnicode(value);
  const array = isJSONArray(value); if (!array && !isObject(value)) return false;
  if (depth >= maxWireJSONDepth || active.has(value)) return false; active.add(value);
  try { return array ? value.every((item) => isJSONCompatible(item, active, depth + 1)) : Object.values(value).every((item) => isJSONCompatible(item, active, depth + 1)); } finally { active.delete(value); }
}

function hasScalarUnicode(value: unknown): value is string {
  if (typeof value !== "string") return false;
  for (let index = 0; index < value.length; index++) { const code = value.charCodeAt(index); if (code >= 0xd800 && code <= 0xdbff) { const low = value.charCodeAt(index + 1); if (low < 0xdc00 || low > 0xdfff) return false; index++; } else if (code >= 0xdc00 && code <= 0xdfff) return false; }
  return true;
}

function matchesCanonicalInt64(value: string): boolean {
  if (!/^(0|-[1-9][0-9]*|[1-9][0-9]*)$/.test(value)) return false;
  try { const parsed = BigInt(value); return parsed >= -(2n ** 63n) && parsed <= (2n ** 63n) - 1n; } catch { return false; }
}

function matchesCanonicalNonNegativeInt64(value: string): boolean {
  if (!/^(0|[1-9][0-9]*)$/.test(value)) return false;
  try { return BigInt(value) <= (2n ** 63n) - 1n; } catch { return false; }
}

function matchesCanonicalUint64(value: string): boolean {
  if (!/^(0|[1-9][0-9]*)$/.test(value)) return false;
  try { const parsed = BigInt(value); return parsed <= (2n ** 64n) - 1n; } catch { return false; }
}

function matchesCanonicalPositiveInt64(value: string): boolean {
  if (!/^[1-9][0-9]*$/.test(value)) return false; try { return BigInt(value) <= (2n ** 63n) - 1n; } catch { return false; }
}

function matchesCanonicalSafeInteger(value: string, minimum = -(2n ** 53n) + 1n): boolean {
  if (!/^(0|-[1-9][0-9]*|[1-9][0-9]*)$/.test(value)) return false; try { const parsed = BigInt(value); return parsed >= minimum && parsed <= (2n ** 53n) - 1n; } catch { return false; }
}

function matchesCanonicalNonNegativeSafeInteger(value: string): boolean {
  return /^(0|[1-9][0-9]*)$/.test(value) && matchesCanonicalSafeInteger(value, 0n);
}

function matchesCanonicalPositiveSafeInteger(value: string): boolean {
  return /^[1-9][0-9]*$/.test(value) && matchesCanonicalSafeInteger(value, 1n);
}

function matchesCanonicalBinary64(value: string, positive: boolean): boolean {
  if (!/^-?(0|[1-9][0-9]*)(?:\.[0-9]+)?(?:e[+-][1-9][0-9]*)?$/.test(value)) return false; const parsed = Number(value); return Number.isFinite(parsed) && !Object.is(parsed, -0) && (!positive || parsed > 0) && String(parsed) === value;
}

function parseProtocolVersion(value: string): readonly [number, number] | undefined {
  const match = /^(0|[1-9][0-9]*)\.(0|[1-9][0-9]*)$/.exec(value); if (match === null) return undefined; const major = Number(match[1]); const minor = Number(match[2]); if (!Number.isSafeInteger(major) || !Number.isSafeInteger(minor) || major > 0xffffffff || minor > 0xffffffff) return undefined; return [major, minor];
}

function parseProtocolVersionRange(value: string): readonly [readonly [number, number], readonly [number, number]] | undefined {
  const parts = value.split(".."); if (parts.length !== 2) return undefined; const lower = parseProtocolVersion(parts[0]!); const upper = parseProtocolVersion(parts[1]!); if (lower === undefined || upper === undefined || lower[0] !== upper[0] || compareProtocolVersions(lower, upper) > 0) return undefined; return [lower, upper];
}

function compareProtocolVersions(left: readonly [number, number], right: readonly [number, number]): number { return left[0] === right[0] ? left[1] - right[1] : left[0] - right[0]; }

function matchesCanonicalSourcePath(value: string): boolean { return value !== "" && !value.startsWith("/") && !value.includes("\\") && !value.includes("\0") && value.split("/").every((segment) => segment !== "" && segment !== "." && segment !== ".."); }

function hasOperatorValueRule(value: Record<string, unknown>, operatorProperty: string, valueProperty: string, valueless: ReadonlySet<string>): boolean { const operator = value[operatorProperty]; if (typeof operator !== "string") return true; return valueless.has(operator) ? !hasOwn(value, valueProperty) : hasOwn(value, valueProperty); }

function hasValidProtocolOffer(value: Record<string, unknown>): boolean { const range = value["supported_range"]; const bindings = value["versions"]; if (typeof range !== "string" || !isJSONArray(bindings)) return false; const parsedRange = parseProtocolVersionRange(range); if (parsedRange === undefined) return false; const seen = new Set<string>(); for (const raw of bindings) { if (!isObject(raw) || typeof raw["version"] !== "string") return false; const text = raw["version"]; const version = parseProtocolVersion(text); if (version === undefined || compareProtocolVersions(version, parsedRange[0]) < 0 || compareProtocolVersions(version, parsedRange[1]) > 0 || seen.has(text)) return false; seen.add(text); } return true; }

function hasValidLimitCapability(value: Record<string, unknown>): boolean { try { const fallback = BigInt(String(value["default_value"])); const effective = BigInt(String(value["effective_maximum"])); const hard = BigInt(String(value["hard_maximum"])); return fallback <= hard && effective <= hard; } catch { return false; } }

function hasUniqueArrayKey(value: Record<string, unknown>, arrayProperty: string, keyProperty: string): boolean { const items = value[arrayProperty]; if (!isJSONArray(items)) return false; const seen = new Set<string>(); for (const raw of items) { if (!isObject(raw) || typeof raw[keyProperty] !== "string" || seen.has(raw[keyProperty])) return false; seen.add(raw[keyProperty]); } return true; }

function hasUniqueItems(value: ReadonlyArray<unknown>): boolean { return new Set(value).size === value.length; }

function hasDisjointArrays(value: Record<string, unknown>, leftProperty: string, rightProperty: string): boolean { const left = value[leftProperty]; const right = value[rightProperty]; if (!isJSONArray(left) || !isJSONArray(right)) return false; const seen = new Set(left); return right.every((item) => !seen.has(item)); }

function isRFC3339(value: string): boolean {
  const match = /^([0-9]{4})-(0[1-9]|1[0-2])-(0[1-9]|[12][0-9]|3[01])T([01][0-9]|2[0-3]):[0-5][0-9]:[0-5][0-9](?:\.[0-9]{1,9})?Z$/.exec(value);
  if (match === null) return false; const year = Number(match[1]); const month = Number(match[2]); const day = Number(match[3]); const leap = year % 4 === 0 && (year % 100 !== 0 || year % 400 === 0); const days = [31, leap ? 29 : 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31]; return day >= 1 && day <= days[month - 1]!;
}

function utf8ByteLength(value: string): number {
  let bytes = 0;
  for (let index = 0; index < value.length; index++) {
    const code = value.charCodeAt(index);
    if (code <= 0x7f) bytes++;
    else if (code <= 0x7ff) bytes += 2;
    else if (code >= 0xd800 && code <= 0xdbff) { const low = value.charCodeAt(index + 1); if (low < 0xdc00 || low > 0xdfff) throw new TypeError("protocol JSON contains an unpaired high surrogate"); bytes += 4; index++; }
    else if (code >= 0xdc00 && code <= 0xdfff) throw new TypeError("protocol JSON contains an unpaired low surrogate");
    else bytes += 3;
  }
  return bytes;
}

function validateWireJSONText(input: string): void {
  if (utf8ByteLength(input) > maxWireJSONBytes) throw new TypeError("protocol JSON exceeds " + maxWireJSONBytes + " UTF-8 bytes");
  let depth = 0;
  for (let index = 0; index < input.length; index++) {
    const character = input[index]!;
    if (character === '"') { index = scanJSONString(input, index); continue; }
    if (character === "{" || character === "[") { depth++; if (depth > maxWireJSONDepth) throw new TypeError("protocol JSON exceeds depth " + maxWireJSONDepth); continue; }
    if (character === "}" || character === "]") { depth--; continue; }
    if (character === "-" || (character >= "0" && character <= "9")) { const end = scanJSONToken(input, index); validateCanonicalJSONNumber(input.slice(index, end)); index = end - 1; }
  }
	const end = scanUniqueJSONValue(input, skipJSONWhitespace(input, 0));
	if (skipJSONWhitespace(input, end) !== input.length) throw new TypeError("protocol JSON must contain exactly one value");
}

function skipJSONWhitespace(input: string, start: number): number {
  let index = start; while (index < input.length && /[ \t\r\n]/.test(input[index]!)) index++; return index;
}

function scanUniqueJSONValue(input: string, start: number): number {
  let index = skipJSONWhitespace(input, start);
  const character = input[index];
  if (character === '"') return scanJSONString(input, index) + 1;
  if (character === "[") {
    index = skipJSONWhitespace(input, index + 1); if (input[index] === "]") return index + 1;
    for (;;) { index = skipJSONWhitespace(input, scanUniqueJSONValue(input, index)); if (input[index] === "]") return index + 1; if (input[index] !== ",") throw new TypeError("protocol JSON array is malformed"); index++; }
  }
  if (character === "{") {
    const keys = new Set<string>(); index = skipJSONWhitespace(input, index + 1); if (input[index] === "}") return index + 1;
    for (;;) {
      if (input[index] !== '"') throw new TypeError("protocol JSON object key must be a string");
      const keyEnd = scanJSONString(input, index); const key: unknown = JSON.parse(input.slice(index, keyEnd + 1));
      if (typeof key !== "string") throw new TypeError("protocol JSON object key must be a string");
      if (keys.has(key)) throw new TypeError("protocol JSON contains duplicate object key " + key); keys.add(key);
      index = skipJSONWhitespace(input, keyEnd + 1); if (input[index] !== ":") throw new TypeError("protocol JSON object is missing a colon");
      index = skipJSONWhitespace(input, scanUniqueJSONValue(input, index + 1)); if (input[index] === "}") return index + 1; if (input[index] !== ",") throw new TypeError("protocol JSON object is malformed"); index = skipJSONWhitespace(input, index + 1);
    }
  }
  const end = scanJSONToken(input, index); if (end === index) throw new TypeError("protocol JSON value is malformed"); return end;
}

function scanJSONToken(input: string, start: number): number {
  let index = start;
  while (index < input.length && !/[{}\[\],:\s]/.test(input[index]!)) index++;
  return index;
}

function validateCanonicalJSONNumber(value: string): void {
  if (!/^(0|-[1-9][0-9]*|[1-9][0-9]*)$/.test(value)) throw new TypeError("protocol JSON number " + value + " is not a canonical integer");
  const parsed = BigInt(value);
  if (parsed < -9007199254740991n || parsed > 9007199254740991n) throw new TypeError("protocol JSON number " + value + " is outside the portable safe range");
}

function scanJSONString(input: string, start: number): number {
  for (let index = start + 1; index < input.length; index++) {
    const code = input.charCodeAt(index);
    if (code === 0x22) return index;
    if (code < 0x20) throw new TypeError("protocol JSON string contains an unescaped control character");
    if (code !== 0x5c) continue;
    index++;
    if (index >= input.length) throw new TypeError("protocol JSON string has a truncated escape");
    if (input[index] !== "u") continue;
    const unit = parseHexCodeUnit(input, index + 1); index += 4;
    if (unit >= 0xdc00 && unit <= 0xdfff) throw new TypeError("protocol JSON string has an unpaired low surrogate");
    if (unit < 0xd800 || unit > 0xdbff) continue;
    if (input[index + 1] !== "\\" || input[index + 2] !== "u") throw new TypeError("protocol JSON string has an unpaired high surrogate");
    const low = parseHexCodeUnit(input, index + 3);
    if (low < 0xdc00 || low > 0xdfff) throw new TypeError("protocol JSON string has an invalid surrogate pair");
    index += 6;
  }
  throw new TypeError("protocol JSON string is unterminated");
}

function parseHexCodeUnit(input: string, start: number): number {
  const text = input.slice(start, start + 4);
  if (!/^[0-9a-fA-F]{4}$/.test(text)) throw new TypeError("protocol JSON string has an invalid Unicode escape");
  return Number.parseInt(text, 16);
}

function canonicalJSONStringify(value: unknown): string {
  if (value === null || typeof value === "boolean") return JSON.stringify(value);
  if (typeof value === "string") { if (!hasScalarUnicode(value)) throw new TypeError("protocol strings must contain Unicode scalar values"); return JSON.stringify(value).replace(/[\u2028\u2029]/g, (character) => character === "\u2028" ? "\\u2028" : "\\u2029"); }
  if (typeof value === "number") { if (!Number.isSafeInteger(value) || Object.is(value, -0)) throw new TypeError("protocol numbers must be canonical safe integers"); return String(value); }
  if (isJSONArray(value)) return `[${value.map(canonicalJSONStringify).join(",")}]`;
  if (isObject(value)) return `{${Object.keys(value).sort().map((key) => `${canonicalJSONStringify(key)}:${canonicalJSONStringify(value[key])}`).join(",")}}`;
  throw new TypeError("unsupported protocol JSON value");
}

/** The maximum lifetime for which a blob reference remains valid. */
export type BlobLifetime = "persistent" | "request" | "session";

export function isBlobLifetime(value: unknown): value is BlobLifetime {
  return typeof value === "string" && hasScalarUnicode(value) && ["persistent", "request", "session"].includes(value);
}

export function decodeBlobLifetime(input: string): BlobLifetime {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isBlobLifetime(value)) throw new TypeError("invalid BlobLifetime");
  return value;
}

export function encodeBlobLifetime(value: BlobLifetime): string {
  if (!isBlobLifetime(value)) throw new TypeError("invalid BlobLifetime");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isBlobLifetime(emitted)) throw new TypeError("encoded value is invalid BlobLifetime");
  return encoded;
}

/** A transport-neutral reference to bytes transferred outside a JSON control envelope. */
export interface BlobRef {
  blob_id: string;
  digest: Digest;
  lifetime: BlobLifetime;
  media_type: string;
  size: CanonicalUint64;
}

export function isBlobRef(value: unknown): value is BlobRef {
  return isObject(value) && hasOnlyKeys(value, new Set(["blob_id", "digest", "lifetime", "media_type", "size"])) && hasOwn(value, "blob_id") && (typeof value["blob_id"] === "string" && hasScalarUnicode(value["blob_id"]) && Array.from(value["blob_id"]).length >= 1) && hasOwn(value, "digest") && (isDigest(value["digest"])) && hasOwn(value, "lifetime") && (isBlobLifetime(value["lifetime"])) && hasOwn(value, "media_type") && (typeof value["media_type"] === "string" && hasScalarUnicode(value["media_type"]) && Array.from(value["media_type"]).length >= 1) && hasOwn(value, "size") && (isCanonicalUint64(value["size"]));
}

export function decodeBlobRef(input: string): BlobRef {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isBlobRef(value)) throw new TypeError("invalid BlobRef");
  return value;
}

export function encodeBlobRef(value: BlobRef): string {
  if (!isBlobRef(value)) throw new TypeError("invalid BlobRef");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isBlobRef(emitted)) throw new TypeError("encoded value is invalid BlobRef");
  return encoded;
}

/** Endpoint default, hard ceiling, and negotiated effective byte ceiling. */
export interface ByteResourceLimitCapability {
  default_value: CanonicalPositiveInt64;
  effective_maximum: CanonicalPositiveInt64;
  hard_maximum: CanonicalPositiveInt64;
  unit: "bytes";
}

export function isByteResourceLimitCapability(value: unknown): value is ByteResourceLimitCapability {
  return isObject(value) && hasOnlyKeys(value, new Set(["default_value", "effective_maximum", "hard_maximum", "unit"])) && hasOwn(value, "default_value") && (isCanonicalPositiveInt64(value["default_value"])) && hasOwn(value, "effective_maximum") && (isCanonicalPositiveInt64(value["effective_maximum"])) && hasOwn(value, "hard_maximum") && (isCanonicalPositiveInt64(value["hard_maximum"])) && hasOwn(value, "unit") && (typeof value["unit"] === "string" && hasScalarUnicode(value["unit"]) && value["unit"] === "bytes") && hasValidLimitCapability(value);
}

export function decodeByteResourceLimitCapability(input: string): ByteResourceLimitCapability {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isByteResourceLimitCapability(value)) throw new TypeError("invalid ByteResourceLimitCapability");
  return value;
}

export function encodeByteResourceLimitCapability(value: ByteResourceLimitCapability): string {
  if (!isByteResourceLimitCapability(value)) throw new TypeError("invalid ByteResourceLimitCapability");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isByteResourceLimitCapability(emitted)) throw new TypeError("encoded value is invalid ByteResourceLimitCapability");
  return encoded;
}

/** A signed 64-bit integer serialized as a canonical decimal string. */
export type CanonicalInt64 = string;

export function isCanonicalInt64(value: unknown): value is CanonicalInt64 {
  return typeof value === "string" && hasScalarUnicode(value) && new RegExp("^(0|-[1-9][0-9]*|[1-9][0-9]*)$").test(value) && matchesCanonicalInt64(value);
}

export function decodeCanonicalInt64(input: string): CanonicalInt64 {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isCanonicalInt64(value)) throw new TypeError("invalid CanonicalInt64");
  return value;
}

export function encodeCanonicalInt64(value: CanonicalInt64): string {
  if (!isCanonicalInt64(value)) throw new TypeError("invalid CanonicalInt64");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isCanonicalInt64(emitted)) throw new TypeError("encoded value is invalid CanonicalInt64");
  return encoded;
}

/** A non-negative signed 64-bit compiler-domain quantity serialized as canonical decimal. */
export type CanonicalNonNegativeInt64 = string;

export function isCanonicalNonNegativeInt64(value: unknown): value is CanonicalNonNegativeInt64 {
  return typeof value === "string" && hasScalarUnicode(value) && new RegExp("^(0|[1-9][0-9]*)$").test(value) && matchesCanonicalNonNegativeInt64(value);
}

export function decodeCanonicalNonNegativeInt64(input: string): CanonicalNonNegativeInt64 {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isCanonicalNonNegativeInt64(value)) throw new TypeError("invalid CanonicalNonNegativeInt64");
  return value;
}

export function encodeCanonicalNonNegativeInt64(value: CanonicalNonNegativeInt64): string {
  if (!isCanonicalNonNegativeInt64(value)) throw new TypeError("invalid CanonicalNonNegativeInt64");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isCanonicalNonNegativeInt64(emitted)) throw new TypeError("encoded value is invalid CanonicalNonNegativeInt64");
  return encoded;
}

/** A non-negative Language 1 integer no greater than 9007199254740991. */
export type CanonicalNonNegativeSafeInteger = string;

export function isCanonicalNonNegativeSafeInteger(value: unknown): value is CanonicalNonNegativeSafeInteger {
  return typeof value === "string" && hasScalarUnicode(value) && new RegExp("^(0|[1-9][0-9]*)$").test(value) && matchesCanonicalNonNegativeSafeInteger(value);
}

export function decodeCanonicalNonNegativeSafeInteger(input: string): CanonicalNonNegativeSafeInteger {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isCanonicalNonNegativeSafeInteger(value)) throw new TypeError("invalid CanonicalNonNegativeSafeInteger");
  return value;
}

export function encodeCanonicalNonNegativeSafeInteger(value: CanonicalNonNegativeSafeInteger): string {
  if (!isCanonicalNonNegativeSafeInteger(value)) throw new TypeError("invalid CanonicalNonNegativeSafeInteger");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isCanonicalNonNegativeSafeInteger(emitted)) throw new TypeError("encoded value is invalid CanonicalNonNegativeSafeInteger");
  return encoded;
}

/** A positive signed 64-bit quantity serialized as canonical decimal. */
export type CanonicalPositiveInt64 = string;

export function isCanonicalPositiveInt64(value: unknown): value is CanonicalPositiveInt64 {
  return typeof value === "string" && hasScalarUnicode(value) && new RegExp("^[1-9][0-9]*$").test(value) && matchesCanonicalPositiveInt64(value);
}

export function decodeCanonicalPositiveInt64(input: string): CanonicalPositiveInt64 {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isCanonicalPositiveInt64(value)) throw new TypeError("invalid CanonicalPositiveInt64");
  return value;
}

export function encodeCanonicalPositiveInt64(value: CanonicalPositiveInt64): string {
  if (!isCanonicalPositiveInt64(value)) throw new TypeError("invalid CanonicalPositiveInt64");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isCanonicalPositiveInt64(emitted)) throw new TypeError("encoded value is invalid CanonicalPositiveInt64");
  return encoded;
}

/** A positive Language 1 integer no greater than 9007199254740991. */
export type CanonicalPositiveSafeInteger = string;

export function isCanonicalPositiveSafeInteger(value: unknown): value is CanonicalPositiveSafeInteger {
  return typeof value === "string" && hasScalarUnicode(value) && new RegExp("^[1-9][0-9]*$").test(value) && matchesCanonicalPositiveSafeInteger(value);
}

export function decodeCanonicalPositiveSafeInteger(input: string): CanonicalPositiveSafeInteger {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isCanonicalPositiveSafeInteger(value)) throw new TypeError("invalid CanonicalPositiveSafeInteger");
  return value;
}

export function encodeCanonicalPositiveSafeInteger(value: CanonicalPositiveSafeInteger): string {
  if (!isCanonicalPositiveSafeInteger(value)) throw new TypeError("invalid CanonicalPositiveSafeInteger");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isCanonicalPositiveSafeInteger(emitted)) throw new TypeError("encoded value is invalid CanonicalPositiveSafeInteger");
  return encoded;
}

/** A Language 1 integer in [-9007199254740991, 9007199254740991], serialized as canonical decimal. */
export type CanonicalSafeInteger = string;

export function isCanonicalSafeInteger(value: unknown): value is CanonicalSafeInteger {
  return typeof value === "string" && hasScalarUnicode(value) && new RegExp("^(0|-[1-9][0-9]*|[1-9][0-9]*)$").test(value) && matchesCanonicalSafeInteger(value);
}

export function decodeCanonicalSafeInteger(input: string): CanonicalSafeInteger {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isCanonicalSafeInteger(value)) throw new TypeError("invalid CanonicalSafeInteger");
  return value;
}

export function encodeCanonicalSafeInteger(value: CanonicalSafeInteger): string {
  if (!isCanonicalSafeInteger(value)) throw new TypeError("invalid CanonicalSafeInteger");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isCanonicalSafeInteger(emitted)) throw new TypeError("encoded value is invalid CanonicalSafeInteger");
  return encoded;
}

/** An unsigned 64-bit integer serialized as a canonical decimal string. */
export type CanonicalUint64 = string;

export function isCanonicalUint64(value: unknown): value is CanonicalUint64 {
  return typeof value === "string" && hasScalarUnicode(value) && new RegExp("^(0|[1-9][0-9]*)$").test(value) && matchesCanonicalUint64(value);
}

export function decodeCanonicalUint64(input: string): CanonicalUint64 {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isCanonicalUint64(value)) throw new TypeError("invalid CanonicalUint64");
  return value;
}

export function encodeCanonicalUint64(value: CanonicalUint64): string {
  if (!isCanonicalUint64(value)) throw new TypeError("invalid CanonicalUint64");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isCanonicalUint64(emitted)) throw new TypeError("encoded value is invalid CanonicalUint64");
  return encoded;
}

/** A typed capability identifier with dot- or colon-separated lowercase namespace segments. Unknown canonical IDs remain representable. */
export type CapabilityID = string;

export function isCapabilityID(value: unknown): value is CapabilityID {
  return typeof value === "string" && hasScalarUnicode(value) && new RegExp("^[a-z][a-z0-9_]*(?:[.:][a-z][a-z0-9_]*)+$").test(value);
}

export function decodeCapabilityID(input: string): CapabilityID {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isCapabilityID(value)) throw new TypeError("invalid CapabilityID");
  return value;
}

export function encodeCapabilityID(value: CapabilityID): string {
  if (!isCapabilityID(value)) throw new TypeError("invalid CapabilityID");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isCapabilityID(emitted)) throw new TypeError("encoded value is invalid CapabilityID");
  return encoded;
}

/** The capabilities this endpoint can actually execute for the negotiated scope. */
export interface CapabilityManifest {
  actor_scope_digest?: Digest;
  authoring_grant_summary?: JsonObject;
  embedding_profiles: ReadonlyArray<ProfileCapability>;
  exporter_profiles: ReadonlyArray<ProfileCapability>;
  extensions?: Extensions;
  limits: CompileResourceLimitCapabilities;
  manifest_etag: ManifestETag;
  manifest_scope: ManifestScope;
  manifest_version: number;
  operations: { readonly [key: string]: OperationCapability };
  query_adapters: ReadonlyArray<ProfileCapability>;
  realtime_profiles: ReadonlyArray<ProfileCapability>;
  registry_sources: ReadonlyArray<ProfileCapability>;
  renderer_profiles: ReadonlyArray<ProfileCapability>;
  required_ladybug_primitives: ReadonlyArray<string>;
  search_profiles: ReadonlyArray<ProfileCapability>;
  storage_capabilities: ReadonlyArray<ProfileCapability>;
  transports: ReadonlyArray<string>;
}

export function isCapabilityManifest(value: unknown): value is CapabilityManifest {
  return isObject(value) && hasOnlyKeys(value, new Set(["actor_scope_digest", "authoring_grant_summary", "embedding_profiles", "exporter_profiles", "extensions", "limits", "manifest_etag", "manifest_scope", "manifest_version", "operations", "query_adapters", "realtime_profiles", "registry_sources", "renderer_profiles", "required_ladybug_primitives", "search_profiles", "storage_capabilities", "transports"])) && (!hasOwn(value, "actor_scope_digest") || (isDigest(value["actor_scope_digest"]))) && (!hasOwn(value, "authoring_grant_summary") || (isJsonObject(value["authoring_grant_summary"]))) && hasOwn(value, "embedding_profiles") && (isJSONArray(value["embedding_profiles"]) && value["embedding_profiles"].every((item) => isProfileCapability(item))) && hasOwn(value, "exporter_profiles") && (isJSONArray(value["exporter_profiles"]) && value["exporter_profiles"].every((item) => isProfileCapability(item))) && (!hasOwn(value, "extensions") || (isExtensions(value["extensions"]))) && hasOwn(value, "limits") && (isCompileResourceLimitCapabilities(value["limits"])) && hasOwn(value, "manifest_etag") && (isManifestETag(value["manifest_etag"])) && hasOwn(value, "manifest_scope") && (isManifestScope(value["manifest_scope"])) && hasOwn(value, "manifest_version") && (typeof value["manifest_version"] === "number" && Number.isSafeInteger(value["manifest_version"]) && !Object.is(value["manifest_version"], -0) && value["manifest_version"] >= 1 && value["manifest_version"] <= 2.147483647e+09) && hasOwn(value, "operations") && (isObject(value["operations"]) && Object.values(value["operations"]).every((item) => isOperationCapability(item))) && hasOwn(value, "query_adapters") && (isJSONArray(value["query_adapters"]) && value["query_adapters"].every((item) => isProfileCapability(item))) && hasOwn(value, "realtime_profiles") && (isJSONArray(value["realtime_profiles"]) && value["realtime_profiles"].every((item) => isProfileCapability(item))) && hasOwn(value, "registry_sources") && (isJSONArray(value["registry_sources"]) && value["registry_sources"].every((item) => isProfileCapability(item))) && hasOwn(value, "renderer_profiles") && (isJSONArray(value["renderer_profiles"]) && value["renderer_profiles"].every((item) => isProfileCapability(item))) && hasOwn(value, "required_ladybug_primitives") && (isJSONArray(value["required_ladybug_primitives"]) && value["required_ladybug_primitives"].every((item) => typeof item === "string" && hasScalarUnicode(item))) && hasOwn(value, "search_profiles") && (isJSONArray(value["search_profiles"]) && value["search_profiles"].every((item) => isProfileCapability(item))) && hasOwn(value, "storage_capabilities") && (isJSONArray(value["storage_capabilities"]) && value["storage_capabilities"].every((item) => isProfileCapability(item))) && hasOwn(value, "transports") && (isJSONArray(value["transports"]) && value["transports"].every((item) => typeof item === "string" && hasScalarUnicode(item)));
}

export function decodeCapabilityManifest(input: string): CapabilityManifest {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isCapabilityManifest(value)) throw new TypeError("invalid CapabilityManifest");
  return value;
}

export function encodeCapabilityManifest(value: CapabilityManifest): string {
  if (!isCapabilityManifest(value)) throw new TypeError("invalid CapabilityManifest");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isCapabilityManifest(emitted)) throw new TypeError("encoded value is invalid CapabilityManifest");
  return encoded;
}

/** The closed Engine 1.0 compile-limit catalog. Each key fixes its unit and separates endpoint default, endpoint hard maximum, and negotiated effective maximum. */
export interface CompileResourceLimitCapabilities {
  max_asset_bytes: ByteResourceLimitCapability;
  max_assets: ItemResourceLimitCapability;
  max_declarations: ItemResourceLimitCapability;
  max_pack_bytes: ByteResourceLimitCapability;
  max_pack_files: ItemResourceLimitCapability;
  max_project_source_bytes: ByteResourceLimitCapability;
  max_project_source_files: ItemResourceLimitCapability;
  max_raster_dimension: RasterDimensionResourceLimitCapability;
  max_raster_pixels: RasterPixelResourceLimitCapability;
}

export function isCompileResourceLimitCapabilities(value: unknown): value is CompileResourceLimitCapabilities {
  return isObject(value) && hasOnlyKeys(value, new Set(["max_asset_bytes", "max_assets", "max_declarations", "max_pack_bytes", "max_pack_files", "max_project_source_bytes", "max_project_source_files", "max_raster_dimension", "max_raster_pixels"])) && hasOwn(value, "max_asset_bytes") && (isByteResourceLimitCapability(value["max_asset_bytes"])) && hasOwn(value, "max_assets") && (isItemResourceLimitCapability(value["max_assets"])) && hasOwn(value, "max_declarations") && (isItemResourceLimitCapability(value["max_declarations"])) && hasOwn(value, "max_pack_bytes") && (isByteResourceLimitCapability(value["max_pack_bytes"])) && hasOwn(value, "max_pack_files") && (isItemResourceLimitCapability(value["max_pack_files"])) && hasOwn(value, "max_project_source_bytes") && (isByteResourceLimitCapability(value["max_project_source_bytes"])) && hasOwn(value, "max_project_source_files") && (isItemResourceLimitCapability(value["max_project_source_files"])) && hasOwn(value, "max_raster_dimension") && (isRasterDimensionResourceLimitCapability(value["max_raster_dimension"])) && hasOwn(value, "max_raster_pixels") && (isRasterPixelResourceLimitCapability(value["max_raster_pixels"]));
}

export function decodeCompileResourceLimitCapabilities(input: string): CompileResourceLimitCapabilities {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isCompileResourceLimitCapabilities(value)) throw new TypeError("invalid CompileResourceLimitCapabilities");
  return value;
}

export function encodeCompileResourceLimitCapabilities(value: CompileResourceLimitCapabilities): string {
  if (!isCompileResourceLimitCapabilities(value)) throw new TypeError("invalid CompileResourceLimitCapabilities");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isCompileResourceLimitCapabilities(emitted)) throw new TypeError("encoded value is invalid CompileResourceLimitCapabilities");
  return encoded;
}

/** Optional positive client ceilings. Omission means the client imposes no ceiling for that key; zero is not a client-ceiling sentinel. */
export interface CompileResourceLimitConstraints {
  max_asset_bytes?: CanonicalPositiveInt64;
  max_assets?: CanonicalPositiveInt64;
  max_declarations?: CanonicalPositiveInt64;
  max_pack_bytes?: CanonicalPositiveInt64;
  max_pack_files?: CanonicalPositiveInt64;
  max_project_source_bytes?: CanonicalPositiveInt64;
  max_project_source_files?: CanonicalPositiveInt64;
  max_raster_dimension?: CanonicalPositiveInt64;
  max_raster_pixels?: CanonicalPositiveInt64;
}

export function isCompileResourceLimitConstraints(value: unknown): value is CompileResourceLimitConstraints {
  return isObject(value) && hasOnlyKeys(value, new Set(["max_asset_bytes", "max_assets", "max_declarations", "max_pack_bytes", "max_pack_files", "max_project_source_bytes", "max_project_source_files", "max_raster_dimension", "max_raster_pixels"])) && (!hasOwn(value, "max_asset_bytes") || (isCanonicalPositiveInt64(value["max_asset_bytes"]))) && (!hasOwn(value, "max_assets") || (isCanonicalPositiveInt64(value["max_assets"]))) && (!hasOwn(value, "max_declarations") || (isCanonicalPositiveInt64(value["max_declarations"]))) && (!hasOwn(value, "max_pack_bytes") || (isCanonicalPositiveInt64(value["max_pack_bytes"]))) && (!hasOwn(value, "max_pack_files") || (isCanonicalPositiveInt64(value["max_pack_files"]))) && (!hasOwn(value, "max_project_source_bytes") || (isCanonicalPositiveInt64(value["max_project_source_bytes"]))) && (!hasOwn(value, "max_project_source_files") || (isCanonicalPositiveInt64(value["max_project_source_files"]))) && (!hasOwn(value, "max_raster_dimension") || (isCanonicalPositiveInt64(value["max_raster_dimension"]))) && (!hasOwn(value, "max_raster_pixels") || (isCanonicalPositiveInt64(value["max_raster_pixels"])));
}

export function decodeCompileResourceLimitConstraints(input: string): CompileResourceLimitConstraints {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isCompileResourceLimitConstraints(value)) throw new TypeError("invalid CompileResourceLimitConstraints");
  return value;
}

export function encodeCompileResourceLimitConstraints(value: CompileResourceLimitConstraints): string {
  if (!isCompileResourceLimitConstraints(value)) throw new TypeError("invalid CompileResourceLimitConstraints");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isCompileResourceLimitConstraints(emitted)) throw new TypeError("encoded value is invalid CompileResourceLimitConstraints");
  return encoded;
}

/** A SHA-256 digest with an exact lowercase 64-hex payload. */
export type Digest = string;

export function isDigest(value: unknown): value is Digest {
  return typeof value === "string" && hasScalarUnicode(value) && new RegExp("^sha256:[0-9a-f]{64}$").test(value);
}

export function decodeDigest(input: string): Digest {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isDigest(value)) throw new TypeError("invalid Digest");
  return value;
}

export function encodeDigest(value: Digest): string {
  if (!isDigest(value)) throw new TypeError("invalid Digest");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isDigest(emitted)) throw new TypeError("encoded value is invalid Digest");
  return encoded;
}

/** An injected opaque non-secret identifier stable for one endpoint instance. Its construction and lifecycle belong to Issue #28. */
export type EndpointInstanceID = string;

export function isEndpointInstanceID(value: unknown): value is EndpointInstanceID {
  return typeof value === "string" && hasScalarUnicode(value) && new RegExp("^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$").test(value);
}

export function decodeEndpointInstanceID(input: string): EndpointInstanceID {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isEndpointInstanceID(value)) throw new TypeError("invalid EndpointInstanceID");
  return value;
}

export function encodeEndpointInstanceID(value: EndpointInstanceID): string {
  if (!isEndpointInstanceID(value)) throw new TypeError("invalid EndpointInstanceID");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isEndpointInstanceID(emitted)) throw new TypeError("encoded value is invalid EndpointInstanceID");
  return encoded;
}

/** Explicit namespaced compatible-minor extension data. Unknown top-level fields are rejected. */
export type Extensions = { readonly [key: string]: JsonValue };

export function isExtensions(value: unknown): value is Extensions {
  return isObject(value) && Object.values(value).every((item) => isJsonValue(item));
}

export function decodeExtensions(input: string): Extensions {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isExtensions(value)) throw new TypeError("invalid Extensions");
  return value;
}

export function encodeExtensions(value: Extensions): string {
  if (!isExtensions(value)) throw new TypeError("invalid Extensions");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isExtensions(emitted)) throw new TypeError("encoded value is invalid Extensions");
  return encoded;
}

/** Client protocol ranges, exact schema digests, capability requirements, and limits. */
export interface HandshakeRequest {
  client_limits?: CompileResourceLimitConstraints;
  client_release: ReleaseVersion;
  extensions?: Extensions;
  optional_capabilities: ReadonlyArray<CapabilityID>;
  protocols: ReadonlyArray<ProtocolOffer>;
  required_capabilities: ReadonlyArray<CapabilityID>;
}

export function isHandshakeRequest(value: unknown): value is HandshakeRequest {
  return isObject(value) && hasOnlyKeys(value, new Set(["client_limits", "client_release", "extensions", "optional_capabilities", "protocols", "required_capabilities"])) && (!hasOwn(value, "client_limits") || (isCompileResourceLimitConstraints(value["client_limits"]))) && hasOwn(value, "client_release") && (isReleaseVersion(value["client_release"])) && (!hasOwn(value, "extensions") || (isExtensions(value["extensions"]))) && hasOwn(value, "optional_capabilities") && (isJSONArray(value["optional_capabilities"]) && value["optional_capabilities"].every((item) => isCapabilityID(item)) && hasUniqueItems(value["optional_capabilities"])) && hasOwn(value, "protocols") && (isJSONArray(value["protocols"]) && value["protocols"].every((item) => isProtocolOffer(item)) && value["protocols"].length >= 1) && hasOwn(value, "required_capabilities") && (isJSONArray(value["required_capabilities"]) && value["required_capabilities"].every((item) => isCapabilityID(item)) && hasUniqueItems(value["required_capabilities"])) && hasUniqueArrayKey(value, "protocols", "name") && hasDisjointArrays(value, "required_capabilities", "optional_capabilities");
}

export function decodeHandshakeRequest(input: string): HandshakeRequest {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isHandshakeRequest(value)) throw new TypeError("invalid HandshakeRequest");
  return value;
}

export function encodeHandshakeRequest(value: HandshakeRequest): string {
  if (!isHandshakeRequest(value)) throw new TypeError("invalid HandshakeRequest");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isHandshakeRequest(emitted)) throw new TypeError("encoded value is invalid HandshakeRequest");
  return encoded;
}

/** The host identity, negotiated versions, effective manifest, and release manifest digest. */
export interface HandshakeResult {
  capability_manifest: CapabilityManifest;
  capability_statuses: ReadonlyArray<RequestedCapabilityStatus>;
  endpoint_instance_id: EndpointInstanceID;
  extensions?: Extensions;
  host_release: ReleaseVersion;
  negotiated_protocols: ReadonlyArray<NegotiatedProtocol>;
  release_manifest_digest: Digest;
}

export function isHandshakeResult(value: unknown): value is HandshakeResult {
  return isObject(value) && hasOnlyKeys(value, new Set(["capability_manifest", "capability_statuses", "endpoint_instance_id", "extensions", "host_release", "negotiated_protocols", "release_manifest_digest"])) && hasOwn(value, "capability_manifest") && (isCapabilityManifest(value["capability_manifest"])) && hasOwn(value, "capability_statuses") && (isJSONArray(value["capability_statuses"]) && value["capability_statuses"].every((item) => isRequestedCapabilityStatus(item))) && hasOwn(value, "endpoint_instance_id") && (isEndpointInstanceID(value["endpoint_instance_id"])) && (!hasOwn(value, "extensions") || (isExtensions(value["extensions"]))) && hasOwn(value, "host_release") && (isReleaseVersion(value["host_release"])) && hasOwn(value, "negotiated_protocols") && (isJSONArray(value["negotiated_protocols"]) && value["negotiated_protocols"].every((item) => isNegotiatedProtocol(item))) && hasOwn(value, "release_manifest_digest") && (isDigest(value["release_manifest_digest"])) && hasUniqueArrayKey(value, "capability_statuses", "capability_id") && hasUniqueArrayKey(value, "negotiated_protocols", "name");
}

export function decodeHandshakeResult(input: string): HandshakeResult {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isHandshakeResult(value)) throw new TypeError("invalid HandshakeResult");
  return value;
}

export function encodeHandshakeResult(value: HandshakeResult): string {
  if (!isHandshakeResult(value)) throw new TypeError("invalid HandshakeResult");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isHandshakeResult(emitted)) throw new TypeError("encoded value is invalid HandshakeResult");
  return encoded;
}

/** Endpoint default, hard ceiling, and negotiated effective item ceiling. */
export interface ItemResourceLimitCapability {
  default_value: CanonicalPositiveInt64;
  effective_maximum: CanonicalPositiveInt64;
  hard_maximum: CanonicalPositiveInt64;
  unit: "items";
}

export function isItemResourceLimitCapability(value: unknown): value is ItemResourceLimitCapability {
  return isObject(value) && hasOnlyKeys(value, new Set(["default_value", "effective_maximum", "hard_maximum", "unit"])) && hasOwn(value, "default_value") && (isCanonicalPositiveInt64(value["default_value"])) && hasOwn(value, "effective_maximum") && (isCanonicalPositiveInt64(value["effective_maximum"])) && hasOwn(value, "hard_maximum") && (isCanonicalPositiveInt64(value["hard_maximum"])) && hasOwn(value, "unit") && (typeof value["unit"] === "string" && hasScalarUnicode(value["unit"]) && value["unit"] === "items") && hasValidLimitCapability(value);
}

export function decodeItemResourceLimitCapability(input: string): ItemResourceLimitCapability {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isItemResourceLimitCapability(value)) throw new TypeError("invalid ItemResourceLimitCapability");
  return value;
}

export function encodeItemResourceLimitCapability(value: ItemResourceLimitCapability): string {
  if (!isItemResourceLimitCapability(value)) throw new TypeError("invalid ItemResourceLimitCapability");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isItemResourceLimitCapability(emitted)) throw new TypeError("encoded value is invalid ItemResourceLimitCapability");
  return encoded;
}

/** An explicitly open JSON object used only for safe diagnostic or extension data. */
export type JsonObject = { readonly [key: string]: JsonValue };

export function isJsonObject(value: unknown): value is JsonObject {
  return isObject(value) && Object.values(value).every((item) => isJsonValue(item));
}

export function decodeJsonObject(input: string): JsonObject {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isJsonObject(value)) throw new TypeError("invalid JsonObject");
  return value;
}

export function encodeJsonObject(value: JsonObject): string {
  if (!isJsonObject(value)) throw new TypeError("invalid JsonObject");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isJsonObject(emitted)) throw new TypeError("encoded value is invalid JsonObject");
  return encoded;
}

/** A recursive canonical JSON value excluding numbers so JavaScript precision cannot change diagnostic or extension data. */
export type JsonValue = null | boolean | string | ReadonlyArray<JsonValue> | { readonly [key: string]: JsonValue };

export function isJsonValue(value: unknown): value is JsonValue {
  return isJSONCompatible(value);
}

export function decodeJsonValue(input: string): JsonValue {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isJsonValue(value)) throw new TypeError("invalid JsonValue");
  return value;
}

export function encodeJsonValue(value: JsonValue): string {
  if (!isJsonValue(value)) throw new TypeError("invalid JsonValue");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isJsonValue(emitted)) throw new TypeError("encoded value is invalid JsonValue");
  return encoded;
}

/** The SHA-256 identity of the canonical effective manifest projection with this field excluded. Construction belongs to Issue #28. */
export type ManifestETag = Digest;

export function isManifestETag(value: unknown): value is ManifestETag {
  return isDigest(value);
}

export function decodeManifestETag(input: string): ManifestETag {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isManifestETag(value)) throw new TypeError("invalid ManifestETag");
  return value;
}

export function encodeManifestETag(value: ManifestETag): string {
  if (!isManifestETag(value)) throw new TypeError("invalid ManifestETag");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isManifestETag(emitted)) throw new TypeError("encoded value is invalid ManifestETag");
  return encoded;
}

export type ManifestScope = "client" | "effective" | "endpoint";

export function isManifestScope(value: unknown): value is ManifestScope {
  return typeof value === "string" && hasScalarUnicode(value) && ["client", "effective", "endpoint"].includes(value);
}

export function decodeManifestScope(input: string): ManifestScope {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isManifestScope(value)) throw new TypeError("invalid ManifestScope");
  return value;
}

export function encodeManifestScope(value: ManifestScope): string {
  if (!isManifestScope(value)) throw new TypeError("invalid ManifestScope");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isManifestScope(emitted)) throw new TypeError("encoded value is invalid ManifestScope");
  return encoded;
}

export interface NegotiatedProtocol {
  name: string;
  schema_digest: Digest;
  version: ProtocolVersion;
}

export function isNegotiatedProtocol(value: unknown): value is NegotiatedProtocol {
  return isObject(value) && hasOnlyKeys(value, new Set(["name", "schema_digest", "version"])) && hasOwn(value, "name") && (typeof value["name"] === "string" && hasScalarUnicode(value["name"]) && new RegExp("^[a-z][a-z0-9._-]*$").test(value["name"])) && hasOwn(value, "schema_digest") && (isDigest(value["schema_digest"])) && hasOwn(value, "version") && (isProtocolVersion(value["version"]));
}

export function decodeNegotiatedProtocol(input: string): NegotiatedProtocol {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isNegotiatedProtocol(value)) throw new TypeError("invalid NegotiatedProtocol");
  return value;
}

export function encodeNegotiatedProtocol(value: NegotiatedProtocol): string {
  if (!isNegotiatedProtocol(value)) throw new TypeError("invalid NegotiatedProtocol");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isNegotiatedProtocol(emitted)) throw new TypeError("encoded value is invalid NegotiatedProtocol");
  return encoded;
}

export interface OperationCapability {
  enabled: boolean;
  extensions?: Extensions;
  limits?: CompileResourceLimitConstraints;
  protocol_version: ProtocolVersion;
  required_authoring_capabilities?: ReadonlyArray<string>;
  unavailable_reason?: UnavailableReason;
}

export function isOperationCapability(value: unknown): value is OperationCapability {
  return isObject(value) && hasOnlyKeys(value, new Set(["enabled", "extensions", "limits", "protocol_version", "required_authoring_capabilities", "unavailable_reason"])) && hasOwn(value, "enabled") && (typeof value["enabled"] === "boolean") && (!hasOwn(value, "extensions") || (isExtensions(value["extensions"]))) && (!hasOwn(value, "limits") || (isCompileResourceLimitConstraints(value["limits"]))) && hasOwn(value, "protocol_version") && (isProtocolVersion(value["protocol_version"])) && (!hasOwn(value, "required_authoring_capabilities") || (isJSONArray(value["required_authoring_capabilities"]) && value["required_authoring_capabilities"].every((item) => typeof item === "string" && hasScalarUnicode(item)))) && (!hasOwn(value, "unavailable_reason") || (isUnavailableReason(value["unavailable_reason"]))) && ((value["enabled"] === false && hasOwn(value, "unavailable_reason")) || (value["enabled"] === true && !hasOwn(value, "unavailable_reason")));
}

export function decodeOperationCapability(input: string): OperationCapability {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isOperationCapability(value)) throw new TypeError("invalid OperationCapability");
  return value;
}

export function encodeOperationCapability(value: OperationCapability): string {
  if (!isOperationCapability(value)) throw new TypeError("invalid OperationCapability");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isOperationCapability(emitted)) throw new TypeError("encoded value is invalid OperationCapability");
  return encoded;
}

export type Outcome = "cancelled" | "failed" | "rejected" | "success";

export function isOutcome(value: unknown): value is Outcome {
  return typeof value === "string" && hasScalarUnicode(value) && ["cancelled", "failed", "rejected", "success"].includes(value);
}

export function decodeOutcome(input: string): Outcome {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isOutcome(value)) throw new TypeError("invalid Outcome");
  return value;
}

export function encodeOutcome(value: Outcome): string {
  if (!isOutcome(value)) throw new TypeError("invalid Outcome");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isOutcome(emitted)) throw new TypeError("encoded value is invalid Outcome");
  return encoded;
}

export interface PageInfo {
  next_cursor?: string;
  result_truncated: boolean;
  returned_bytes: CanonicalUint64;
  returned_items: CanonicalUint64;
  total_items?: TotalItems;
}

export function isPageInfo(value: unknown): value is PageInfo {
  return isObject(value) && hasOnlyKeys(value, new Set(["next_cursor", "result_truncated", "returned_bytes", "returned_items", "total_items"])) && (!hasOwn(value, "next_cursor") || (typeof value["next_cursor"] === "string" && hasScalarUnicode(value["next_cursor"]))) && hasOwn(value, "result_truncated") && (typeof value["result_truncated"] === "boolean") && hasOwn(value, "returned_bytes") && (isCanonicalUint64(value["returned_bytes"])) && hasOwn(value, "returned_items") && (isCanonicalUint64(value["returned_items"])) && (!hasOwn(value, "total_items") || (isTotalItems(value["total_items"])));
}

export function decodePageInfo(input: string): PageInfo {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isPageInfo(value)) throw new TypeError("invalid PageInfo");
  return value;
}

export function encodePageInfo(value: PageInfo): string {
  if (!isPageInfo(value)) throw new TypeError("invalid PageInfo");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isPageInfo(emitted)) throw new TypeError("encoded value is invalid PageInfo");
  return encoded;
}

export interface ProfileCapability {
  enabled: boolean;
  extensions?: Extensions;
  id: string;
  unavailable_reason?: UnavailableReason;
  version: string;
}

export function isProfileCapability(value: unknown): value is ProfileCapability {
  return isObject(value) && hasOnlyKeys(value, new Set(["enabled", "extensions", "id", "unavailable_reason", "version"])) && hasOwn(value, "enabled") && (typeof value["enabled"] === "boolean") && (!hasOwn(value, "extensions") || (isExtensions(value["extensions"]))) && hasOwn(value, "id") && (typeof value["id"] === "string" && hasScalarUnicode(value["id"]) && Array.from(value["id"]).length >= 1) && (!hasOwn(value, "unavailable_reason") || (isUnavailableReason(value["unavailable_reason"]))) && hasOwn(value, "version") && (typeof value["version"] === "string" && hasScalarUnicode(value["version"]) && Array.from(value["version"]).length >= 1) && ((value["enabled"] === false && hasOwn(value, "unavailable_reason")) || (value["enabled"] === true && !hasOwn(value, "unavailable_reason")));
}

export function decodeProfileCapability(input: string): ProfileCapability {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isProfileCapability(value)) throw new TypeError("invalid ProfileCapability");
  return value;
}

export function encodeProfileCapability(value: ProfileCapability): string {
  if (!isProfileCapability(value)) throw new TypeError("invalid ProfileCapability");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isProfileCapability(emitted)) throw new TypeError("encoded value is invalid ProfileCapability");
  return encoded;
}

/** A non-LDL protocol, compatibility, policy, or operation diagnostic. LDL compiler diagnostics use semantic.Diagnostic instead. */
export interface ProtocolDiagnostic {
  code: string;
  data?: JsonObject;
  message: string;
  related: ReadonlyArray<ProtocolDiagnosticRelated>;
  remediation?: string;
  severity: ProtocolDiagnosticSeverity;
  source?: ProtocolDiagnosticSource;
}

export function isProtocolDiagnostic(value: unknown): value is ProtocolDiagnostic {
  return isObject(value) && hasOnlyKeys(value, new Set(["code", "data", "message", "related", "remediation", "severity", "source"])) && hasOwn(value, "code") && (typeof value["code"] === "string" && hasScalarUnicode(value["code"]) && new RegExp("^[a-z][a-z0-9_]*(?:\\.[a-z][a-z0-9_]*)+$").test(value["code"])) && (!hasOwn(value, "data") || (isJsonObject(value["data"]))) && hasOwn(value, "message") && (typeof value["message"] === "string" && hasScalarUnicode(value["message"])) && hasOwn(value, "related") && (isJSONArray(value["related"]) && value["related"].every((item) => isProtocolDiagnosticRelated(item))) && (!hasOwn(value, "remediation") || (typeof value["remediation"] === "string" && hasScalarUnicode(value["remediation"]))) && hasOwn(value, "severity") && (isProtocolDiagnosticSeverity(value["severity"])) && (!hasOwn(value, "source") || (isProtocolDiagnosticSource(value["source"])));
}

export function decodeProtocolDiagnostic(input: string): ProtocolDiagnostic {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isProtocolDiagnostic(value)) throw new TypeError("invalid ProtocolDiagnostic");
  return value;
}

export function encodeProtocolDiagnostic(value: ProtocolDiagnostic): string {
  if (!isProtocolDiagnostic(value)) throw new TypeError("invalid ProtocolDiagnostic");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isProtocolDiagnostic(emitted)) throw new TypeError("encoded value is invalid ProtocolDiagnostic");
  return encoded;
}

export interface ProtocolDiagnosticRelated {
  message: string;
  relation: string;
  source?: ProtocolDiagnosticSource;
}

export function isProtocolDiagnosticRelated(value: unknown): value is ProtocolDiagnosticRelated {
  return isObject(value) && hasOnlyKeys(value, new Set(["message", "relation", "source"])) && hasOwn(value, "message") && (typeof value["message"] === "string" && hasScalarUnicode(value["message"])) && hasOwn(value, "relation") && (typeof value["relation"] === "string" && hasScalarUnicode(value["relation"]) && Array.from(value["relation"]).length >= 1) && (!hasOwn(value, "source") || (isProtocolDiagnosticSource(value["source"])));
}

export function decodeProtocolDiagnosticRelated(input: string): ProtocolDiagnosticRelated {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isProtocolDiagnosticRelated(value)) throw new TypeError("invalid ProtocolDiagnosticRelated");
  return value;
}

export function encodeProtocolDiagnosticRelated(value: ProtocolDiagnosticRelated): string {
  if (!isProtocolDiagnosticRelated(value)) throw new TypeError("invalid ProtocolDiagnosticRelated");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isProtocolDiagnosticRelated(emitted)) throw new TypeError("encoded value is invalid ProtocolDiagnosticRelated");
  return encoded;
}

export type ProtocolDiagnosticSeverity = "error" | "hint" | "information" | "warning";

export function isProtocolDiagnosticSeverity(value: unknown): value is ProtocolDiagnosticSeverity {
  return typeof value === "string" && hasScalarUnicode(value) && ["error", "hint", "information", "warning"].includes(value);
}

export function decodeProtocolDiagnosticSeverity(input: string): ProtocolDiagnosticSeverity {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isProtocolDiagnosticSeverity(value)) throw new TypeError("invalid ProtocolDiagnosticSeverity");
  return value;
}

export function encodeProtocolDiagnosticSeverity(value: ProtocolDiagnosticSeverity): string {
  if (!isProtocolDiagnosticSeverity(value)) throw new TypeError("invalid ProtocolDiagnosticSeverity");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isProtocolDiagnosticSeverity(emitted)) throw new TypeError("encoded value is invalid ProtocolDiagnosticSeverity");
  return encoded;
}

export interface ProtocolDiagnosticSource {
  module_path: string;
  span: ProtocolDiagnosticSpan;
  stable_address?: string;
}

export function isProtocolDiagnosticSource(value: unknown): value is ProtocolDiagnosticSource {
  return isObject(value) && hasOnlyKeys(value, new Set(["module_path", "span", "stable_address"])) && hasOwn(value, "module_path") && (typeof value["module_path"] === "string" && hasScalarUnicode(value["module_path"])) && hasOwn(value, "span") && (isProtocolDiagnosticSpan(value["span"])) && (!hasOwn(value, "stable_address") || (typeof value["stable_address"] === "string" && hasScalarUnicode(value["stable_address"])));
}

export function decodeProtocolDiagnosticSource(input: string): ProtocolDiagnosticSource {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isProtocolDiagnosticSource(value)) throw new TypeError("invalid ProtocolDiagnosticSource");
  return value;
}

export function encodeProtocolDiagnosticSource(value: ProtocolDiagnosticSource): string {
  if (!isProtocolDiagnosticSource(value)) throw new TypeError("invalid ProtocolDiagnosticSource");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isProtocolDiagnosticSource(emitted)) throw new TypeError("encoded value is invalid ProtocolDiagnosticSource");
  return encoded;
}

export interface ProtocolDiagnosticSpan {
  end_byte: CanonicalUint64;
  start_byte: CanonicalUint64;
}

export function isProtocolDiagnosticSpan(value: unknown): value is ProtocolDiagnosticSpan {
  return isObject(value) && hasOnlyKeys(value, new Set(["end_byte", "start_byte"])) && hasOwn(value, "end_byte") && (isCanonicalUint64(value["end_byte"])) && hasOwn(value, "start_byte") && (isCanonicalUint64(value["start_byte"])) && BigInt(value["start_byte"]) <= BigInt(value["end_byte"]);
}

export function decodeProtocolDiagnosticSpan(input: string): ProtocolDiagnosticSpan {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isProtocolDiagnosticSpan(value)) throw new TypeError("invalid ProtocolDiagnosticSpan");
  return value;
}

export function encodeProtocolDiagnosticSpan(value: ProtocolDiagnosticSpan): string {
  if (!isProtocolDiagnosticSpan(value)) throw new TypeError("invalid ProtocolDiagnosticSpan");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isProtocolDiagnosticSpan(emitted)) throw new TypeError("encoded value is invalid ProtocolDiagnosticSpan");
  return encoded;
}

/** A safe transport or execution-platform failure, never a source diagnostic. */
export interface ProtocolFailure {
  category: ProtocolFailureCategory;
  code: string;
  correlation_id?: string;
  extensions?: Extensions;
  message: string;
  retry_after_ms?: CanonicalUint64;
  retryable: boolean;
  safe_details?: JsonObject;
}

export function isProtocolFailure(value: unknown): value is ProtocolFailure {
  return isObject(value) && hasOnlyKeys(value, new Set(["category", "code", "correlation_id", "extensions", "message", "retry_after_ms", "retryable", "safe_details"])) && hasOwn(value, "category") && (isProtocolFailureCategory(value["category"])) && hasOwn(value, "code") && (typeof value["code"] === "string" && hasScalarUnicode(value["code"]) && new RegExp("^[a-z][a-z0-9_]*(?:\\.[a-z][a-z0-9_]*)+$").test(value["code"])) && (!hasOwn(value, "correlation_id") || (typeof value["correlation_id"] === "string" && hasScalarUnicode(value["correlation_id"]))) && (!hasOwn(value, "extensions") || (isExtensions(value["extensions"]))) && hasOwn(value, "message") && (typeof value["message"] === "string" && hasScalarUnicode(value["message"])) && (!hasOwn(value, "retry_after_ms") || (isCanonicalUint64(value["retry_after_ms"]))) && hasOwn(value, "retryable") && (typeof value["retryable"] === "boolean") && (!hasOwn(value, "safe_details") || (isJsonObject(value["safe_details"])));
}

export function decodeProtocolFailure(input: string): ProtocolFailure {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isProtocolFailure(value)) throw new TypeError("invalid ProtocolFailure");
  return value;
}

export function encodeProtocolFailure(value: ProtocolFailure): string {
  if (!isProtocolFailure(value)) throw new TypeError("invalid ProtocolFailure");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isProtocolFailure(emitted)) throw new TypeError("encoded value is invalid ProtocolFailure");
  return encoded;
}

export type ProtocolFailureCategory = "adapter" | "cancelled" | "invariant" | "io" | "resource" | "transport";

export function isProtocolFailureCategory(value: unknown): value is ProtocolFailureCategory {
  return typeof value === "string" && hasScalarUnicode(value) && ["adapter", "cancelled", "invariant", "io", "resource", "transport"].includes(value);
}

export function decodeProtocolFailureCategory(input: string): ProtocolFailureCategory {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isProtocolFailureCategory(value)) throw new TypeError("invalid ProtocolFailureCategory");
  return value;
}

export function encodeProtocolFailureCategory(value: ProtocolFailureCategory): string {
  if (!isProtocolFailureCategory(value)) throw new TypeError("invalid ProtocolFailureCategory");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isProtocolFailureCategory(emitted)) throw new TypeError("encoded value is invalid ProtocolFailureCategory");
  return encoded;
}

export interface ProtocolOffer {
  name: string;
  supported_range: ProtocolVersionRange;
  versions: ReadonlyArray<ProtocolVersionBinding>;
}

export function isProtocolOffer(value: unknown): value is ProtocolOffer {
  return isObject(value) && hasOnlyKeys(value, new Set(["name", "supported_range", "versions"])) && hasOwn(value, "name") && (typeof value["name"] === "string" && hasScalarUnicode(value["name"]) && new RegExp("^[a-z][a-z0-9._-]*$").test(value["name"])) && hasOwn(value, "supported_range") && (isProtocolVersionRange(value["supported_range"])) && hasOwn(value, "versions") && (isJSONArray(value["versions"]) && value["versions"].every((item) => isProtocolVersionBinding(item)) && value["versions"].length >= 1) && hasValidProtocolOffer(value);
}

export function decodeProtocolOffer(input: string): ProtocolOffer {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isProtocolOffer(value)) throw new TypeError("invalid ProtocolOffer");
  return value;
}

export function encodeProtocolOffer(value: ProtocolOffer): string {
  if (!isProtocolOffer(value)) throw new TypeError("invalid ProtocolOffer");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isProtocolOffer(emitted)) throw new TypeError("encoded value is invalid ProtocolOffer");
  return encoded;
}

export interface ProtocolRef {
  name: string;
  version: ProtocolVersion;
}

export function isProtocolRef(value: unknown): value is ProtocolRef {
  return isObject(value) && hasOnlyKeys(value, new Set(["name", "version"])) && hasOwn(value, "name") && (typeof value["name"] === "string" && hasScalarUnicode(value["name"]) && new RegExp("^[a-z][a-z0-9._-]*$").test(value["name"])) && hasOwn(value, "version") && (isProtocolVersion(value["version"]));
}

export function decodeProtocolRef(input: string): ProtocolRef {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isProtocolRef(value)) throw new TypeError("invalid ProtocolRef");
  return value;
}

export function encodeProtocolRef(value: ProtocolRef): string {
  if (!isProtocolRef(value)) throw new TypeError("invalid ProtocolRef");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isProtocolRef(emitted)) throw new TypeError("encoded value is invalid ProtocolRef");
  return encoded;
}

/** A canonical major.minor protocol version whose components are unsigned 32-bit integers. */
export type ProtocolVersion = string;

export function isProtocolVersion(value: unknown): value is ProtocolVersion {
  return typeof value === "string" && hasScalarUnicode(value) && new RegExp("^(0|[1-9][0-9]*)\\.(0|[1-9][0-9]*)$").test(value) && parseProtocolVersion(value) !== undefined;
}

export function decodeProtocolVersion(input: string): ProtocolVersion {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isProtocolVersion(value)) throw new TypeError("invalid ProtocolVersion");
  return value;
}

export function encodeProtocolVersion(value: ProtocolVersion): string {
  if (!isProtocolVersion(value)) throw new TypeError("invalid ProtocolVersion");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isProtocolVersion(emitted)) throw new TypeError("encoded value is invalid ProtocolVersion");
  return encoded;
}

/** The exact schema digest associated with one offered protocol version. */
export interface ProtocolVersionBinding {
  schema_digest: Digest;
  version: ProtocolVersion;
}

export function isProtocolVersionBinding(value: unknown): value is ProtocolVersionBinding {
  return isObject(value) && hasOnlyKeys(value, new Set(["schema_digest", "version"])) && hasOwn(value, "schema_digest") && (isDigest(value["schema_digest"])) && hasOwn(value, "version") && (isProtocolVersion(value["version"]));
}

export function decodeProtocolVersionBinding(input: string): ProtocolVersionBinding {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isProtocolVersionBinding(value)) throw new TypeError("invalid ProtocolVersionBinding");
  return value;
}

export function encodeProtocolVersionBinding(value: ProtocolVersionBinding): string {
  if (!isProtocolVersionBinding(value)) throw new TypeError("invalid ProtocolVersionBinding");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isProtocolVersionBinding(emitted)) throw new TypeError("encoded value is invalid ProtocolVersionBinding");
  return encoded;
}

/** Either one canonical major.minor version or one canonical inclusive lower..upper range. */
export type ProtocolVersionOrRange = string;

export function isProtocolVersionOrRange(value: unknown): value is ProtocolVersionOrRange {
  return typeof value === "string" && hasScalarUnicode(value) && (parseProtocolVersion(value) !== undefined || parseProtocolVersionRange(value) !== undefined);
}

export function decodeProtocolVersionOrRange(input: string): ProtocolVersionOrRange {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isProtocolVersionOrRange(value)) throw new TypeError("invalid ProtocolVersionOrRange");
  return value;
}

export function encodeProtocolVersionOrRange(value: ProtocolVersionOrRange): string {
  if (!isProtocolVersionOrRange(value)) throw new TypeError("invalid ProtocolVersionOrRange");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isProtocolVersionOrRange(emitted)) throw new TypeError("encoded value is invalid ProtocolVersionOrRange");
  return encoded;
}

/** A closed inclusive lower..upper range within one major. Both bounds are canonical major.minor values and the lower bound must not exceed the upper bound. */
export type ProtocolVersionRange = string;

export function isProtocolVersionRange(value: unknown): value is ProtocolVersionRange {
  return typeof value === "string" && hasScalarUnicode(value) && new RegExp("^(0|[1-9][0-9]*)\\.(0|[1-9][0-9]*)\\.\\.(0|[1-9][0-9]*)\\.(0|[1-9][0-9]*)$").test(value) && parseProtocolVersionRange(value) !== undefined;
}

export function decodeProtocolVersionRange(input: string): ProtocolVersionRange {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isProtocolVersionRange(value)) throw new TypeError("invalid ProtocolVersionRange");
  return value;
}

export function encodeProtocolVersionRange(value: ProtocolVersionRange): string {
  if (!isProtocolVersionRange(value)) throw new TypeError("invalid ProtocolVersionRange");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isProtocolVersionRange(emitted)) throw new TypeError("encoded value is invalid ProtocolVersionRange");
  return encoded;
}

/** Endpoint default, hard ceiling, and negotiated effective pixels-per-axis ceiling. */
export interface RasterDimensionResourceLimitCapability {
  default_value: CanonicalPositiveInt64;
  effective_maximum: CanonicalPositiveInt64;
  hard_maximum: CanonicalPositiveInt64;
  unit: "pixels_per_axis";
}

export function isRasterDimensionResourceLimitCapability(value: unknown): value is RasterDimensionResourceLimitCapability {
  return isObject(value) && hasOnlyKeys(value, new Set(["default_value", "effective_maximum", "hard_maximum", "unit"])) && hasOwn(value, "default_value") && (isCanonicalPositiveInt64(value["default_value"])) && hasOwn(value, "effective_maximum") && (isCanonicalPositiveInt64(value["effective_maximum"])) && hasOwn(value, "hard_maximum") && (isCanonicalPositiveInt64(value["hard_maximum"])) && hasOwn(value, "unit") && (typeof value["unit"] === "string" && hasScalarUnicode(value["unit"]) && value["unit"] === "pixels_per_axis") && hasValidLimitCapability(value);
}

export function decodeRasterDimensionResourceLimitCapability(input: string): RasterDimensionResourceLimitCapability {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isRasterDimensionResourceLimitCapability(value)) throw new TypeError("invalid RasterDimensionResourceLimitCapability");
  return value;
}

export function encodeRasterDimensionResourceLimitCapability(value: RasterDimensionResourceLimitCapability): string {
  if (!isRasterDimensionResourceLimitCapability(value)) throw new TypeError("invalid RasterDimensionResourceLimitCapability");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isRasterDimensionResourceLimitCapability(emitted)) throw new TypeError("encoded value is invalid RasterDimensionResourceLimitCapability");
  return encoded;
}

/** Endpoint default, hard ceiling, and negotiated effective raster-pixel ceiling. */
export interface RasterPixelResourceLimitCapability {
  default_value: CanonicalPositiveInt64;
  effective_maximum: CanonicalPositiveInt64;
  hard_maximum: CanonicalPositiveInt64;
  unit: "pixels";
}

export function isRasterPixelResourceLimitCapability(value: unknown): value is RasterPixelResourceLimitCapability {
  return isObject(value) && hasOnlyKeys(value, new Set(["default_value", "effective_maximum", "hard_maximum", "unit"])) && hasOwn(value, "default_value") && (isCanonicalPositiveInt64(value["default_value"])) && hasOwn(value, "effective_maximum") && (isCanonicalPositiveInt64(value["effective_maximum"])) && hasOwn(value, "hard_maximum") && (isCanonicalPositiveInt64(value["hard_maximum"])) && hasOwn(value, "unit") && (typeof value["unit"] === "string" && hasScalarUnicode(value["unit"]) && value["unit"] === "pixels") && hasValidLimitCapability(value);
}

export function decodeRasterPixelResourceLimitCapability(input: string): RasterPixelResourceLimitCapability {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isRasterPixelResourceLimitCapability(value)) throw new TypeError("invalid RasterPixelResourceLimitCapability");
  return value;
}

export function encodeRasterPixelResourceLimitCapability(value: RasterPixelResourceLimitCapability): string {
  if (!isRasterPixelResourceLimitCapability(value)) throw new TypeError("invalid RasterPixelResourceLimitCapability");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isRasterPixelResourceLimitCapability(emitted)) throw new TypeError("encoded value is invalid RasterPixelResourceLimitCapability");
  return encoded;
}

/** A canonical SemVer 2.0.0 release identifier. */
export type ReleaseVersion = string;

export function isReleaseVersion(value: unknown): value is ReleaseVersion {
  return typeof value === "string" && hasScalarUnicode(value) && new RegExp("^(0|[1-9][0-9]*)\\.(0|[1-9][0-9]*)\\.(0|[1-9][0-9]*)(?:-(?:0|[1-9][0-9]*|[0-9]*[A-Za-z-][0-9A-Za-z-]*)(?:\\.(?:0|[1-9][0-9]*|[0-9]*[A-Za-z-][0-9A-Za-z-]*))*)?(?:\\+[0-9A-Za-z-]+(?:\\.[0-9A-Za-z-]+)*)?$").test(value);
}

export function decodeReleaseVersion(input: string): ReleaseVersion {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isReleaseVersion(value)) throw new TypeError("invalid ReleaseVersion");
  return value;
}

export function encodeReleaseVersion(value: ReleaseVersion): string {
  if (!isReleaseVersion(value)) throw new TypeError("invalid ReleaseVersion");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isReleaseVersion(emitted)) throw new TypeError("encoded value is invalid ReleaseVersion");
  return encoded;
}

/** The common top-level metadata copied into every operation request envelope. */
export interface RequestMetadata {
  deadline_at?: Rfc3339Time;
  extensions?: Extensions;
  operation: string;
  protocol: ProtocolRef;
  request_id: string;
  trace_context?: JsonObject;
}

export function isRequestMetadata(value: unknown): value is RequestMetadata {
  return isObject(value) && hasOnlyKeys(value, new Set(["deadline_at", "extensions", "operation", "protocol", "request_id", "trace_context"])) && (!hasOwn(value, "deadline_at") || (isRfc3339Time(value["deadline_at"]))) && (!hasOwn(value, "extensions") || (isExtensions(value["extensions"]))) && hasOwn(value, "operation") && (typeof value["operation"] === "string" && hasScalarUnicode(value["operation"]) && new RegExp("^[a-z][a-z0-9_]*\\.[a-z][a-z0-9_]*$").test(value["operation"])) && hasOwn(value, "protocol") && (isProtocolRef(value["protocol"])) && hasOwn(value, "request_id") && (typeof value["request_id"] === "string" && hasScalarUnicode(value["request_id"]) && Array.from(value["request_id"]).length >= 1) && (!hasOwn(value, "trace_context") || (isJsonObject(value["trace_context"])));
}

export function decodeRequestMetadata(input: string): RequestMetadata {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isRequestMetadata(value)) throw new TypeError("invalid RequestMetadata");
  return value;
}

export function encodeRequestMetadata(value: RequestMetadata): string {
  if (!isRequestMetadata(value)) throw new TypeError("invalid RequestMetadata");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isRequestMetadata(emitted)) throw new TypeError("encoded value is invalid RequestMetadata");
  return encoded;
}

/** An explicit result for every requested capability ID, including unknown optional IDs. */
export interface RequestedCapabilityStatus {
  capability_id: CapabilityID;
  enabled: boolean;
  protocol_version: ProtocolVersion;
  unavailable_reason?: UnavailableReason;
}

export function isRequestedCapabilityStatus(value: unknown): value is RequestedCapabilityStatus {
  return isObject(value) && hasOnlyKeys(value, new Set(["capability_id", "enabled", "protocol_version", "unavailable_reason"])) && hasOwn(value, "capability_id") && (isCapabilityID(value["capability_id"])) && hasOwn(value, "enabled") && (typeof value["enabled"] === "boolean") && hasOwn(value, "protocol_version") && (isProtocolVersion(value["protocol_version"])) && (!hasOwn(value, "unavailable_reason") || (isUnavailableReason(value["unavailable_reason"]))) && ((value["enabled"] === false && hasOwn(value, "unavailable_reason")) || (value["enabled"] === true && !hasOwn(value, "unavailable_reason")));
}

export function decodeRequestedCapabilityStatus(input: string): RequestedCapabilityStatus {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isRequestedCapabilityStatus(value)) throw new TypeError("invalid RequestedCapabilityStatus");
  return value;
}

export function encodeRequestedCapabilityStatus(value: RequestedCapabilityStatus): string {
  if (!isRequestedCapabilityStatus(value)) throw new TypeError("invalid RequestedCapabilityStatus");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isRequestedCapabilityStatus(emitted)) throw new TypeError("encoded value is invalid RequestedCapabilityStatus");
  return encoded;
}

/** The common top-level metadata copied into every operation response envelope. */
export interface ResponseMetadata {
  engine_release?: ReleaseVersion;
  extensions?: Extensions;
  host_release?: ReleaseVersion;
  outcome: Outcome;
  protocol: ProtocolRef;
  request_id: string;
}

export function isResponseMetadata(value: unknown): value is ResponseMetadata {
  return isObject(value) && hasOnlyKeys(value, new Set(["engine_release", "extensions", "host_release", "outcome", "protocol", "request_id"])) && (!hasOwn(value, "engine_release") || (isReleaseVersion(value["engine_release"]))) && (!hasOwn(value, "extensions") || (isExtensions(value["extensions"]))) && (!hasOwn(value, "host_release") || (isReleaseVersion(value["host_release"]))) && hasOwn(value, "outcome") && (isOutcome(value["outcome"])) && hasOwn(value, "protocol") && (isProtocolRef(value["protocol"])) && hasOwn(value, "request_id") && (typeof value["request_id"] === "string" && hasScalarUnicode(value["request_id"]) && Array.from(value["request_id"]).length >= 1);
}

export function decodeResponseMetadata(input: string): ResponseMetadata {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isResponseMetadata(value)) throw new TypeError("invalid ResponseMetadata");
  return value;
}

export function encodeResponseMetadata(value: ResponseMetadata): string {
  if (!isResponseMetadata(value)) throw new TypeError("invalid ResponseMetadata");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isResponseMetadata(emitted)) throw new TypeError("encoded value is invalid ResponseMetadata");
  return encoded;
}

/** A real-calendar UTC RFC 3339 timestamp with a required Z suffix and at most nine fractional digits. */
export type Rfc3339Time = string;

export function isRfc3339Time(value: unknown): value is Rfc3339Time {
  return typeof value === "string" && hasScalarUnicode(value) && new RegExp("^[0-9]{4}-(0[1-9]|1[0-2])-(0[1-9]|[12][0-9]|3[01])T([01][0-9]|2[0-3]):[0-5][0-9]:[0-5][0-9](\\.[0-9]{1,9})?Z$").test(value) && isRFC3339(value);
}

export function decodeRfc3339Time(input: string): Rfc3339Time {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isRfc3339Time(value)) throw new TypeError("invalid Rfc3339Time");
  return value;
}

export function encodeRfc3339Time(value: Rfc3339Time): string {
  if (!isRfc3339Time(value)) throw new TypeError("invalid Rfc3339Time");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isRfc3339Time(emitted)) throw new TypeError("encoded value is invalid Rfc3339Time");
  return encoded;
}

/** An explicit unknown or exact item count; omission is never used to imply truncation. */
export interface TotalItems {
  exact?: CanonicalUint64;
  known: boolean;
}

export function isTotalItems(value: unknown): value is TotalItems {
  return isObject(value) && hasOnlyKeys(value, new Set(["exact", "known"])) && (!hasOwn(value, "exact") || (isCanonicalUint64(value["exact"]))) && hasOwn(value, "known") && (typeof value["known"] === "boolean") && ((value["known"] === false && !hasOwn(value, "exact")) || (value["known"] === true && hasOwn(value, "exact")));
}

export function decodeTotalItems(input: string): TotalItems {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isTotalItems(value)) throw new TypeError("invalid TotalItems");
  return value;
}

export function encodeTotalItems(value: TotalItems): string {
  if (!isTotalItems(value)) throw new TypeError("invalid TotalItems");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isTotalItems(emitted)) throw new TypeError("encoded value is invalid TotalItems");
  return encoded;
}

export type UnavailableReason = "degraded" | "incompatible" | "not_authorized" | "not_configured" | "not_entitled" | "not_linked" | "offline" | "unsupported";

export function isUnavailableReason(value: unknown): value is UnavailableReason {
  return typeof value === "string" && hasScalarUnicode(value) && ["degraded", "incompatible", "not_authorized", "not_configured", "not_entitled", "not_linked", "offline", "unsupported"].includes(value);
}

export function decodeUnavailableReason(input: string): UnavailableReason {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isUnavailableReason(value)) throw new TypeError("invalid UnavailableReason");
  return value;
}

export function encodeUnavailableReason(value: UnavailableReason): string {
  if (!isUnavailableReason(value)) throw new TypeError("invalid UnavailableReason");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isUnavailableReason(emitted)) throw new TypeError("encoded value is invalid UnavailableReason");
  return encoded;
}

/** The normative Section 10.9 upgrade and compatibility diagnostic data. */
export interface UpgradeDiagnosticData {
  affected_artifacts: ReadonlyArray<string>;
  current_version: ProtocolVersion;
  migration_available: boolean;
  migration_plan_ref?: string;
  readonly_possible: boolean;
  replacement_profile?: string;
  required_version_or_range: ProtocolVersionOrRange;
}

export function isUpgradeDiagnosticData(value: unknown): value is UpgradeDiagnosticData {
  return isObject(value) && hasOnlyKeys(value, new Set(["affected_artifacts", "current_version", "migration_available", "migration_plan_ref", "readonly_possible", "replacement_profile", "required_version_or_range"])) && hasOwn(value, "affected_artifacts") && (isJSONArray(value["affected_artifacts"]) && value["affected_artifacts"].every((item) => typeof item === "string" && hasScalarUnicode(item) && Array.from(item).length >= 1)) && hasOwn(value, "current_version") && (isProtocolVersion(value["current_version"])) && hasOwn(value, "migration_available") && (typeof value["migration_available"] === "boolean") && (!hasOwn(value, "migration_plan_ref") || (typeof value["migration_plan_ref"] === "string" && hasScalarUnicode(value["migration_plan_ref"]) && Array.from(value["migration_plan_ref"]).length >= 1)) && hasOwn(value, "readonly_possible") && (typeof value["readonly_possible"] === "boolean") && (!hasOwn(value, "replacement_profile") || (typeof value["replacement_profile"] === "string" && hasScalarUnicode(value["replacement_profile"]) && Array.from(value["replacement_profile"]).length >= 1)) && hasOwn(value, "required_version_or_range") && (isProtocolVersionOrRange(value["required_version_or_range"]));
}

export function decodeUpgradeDiagnosticData(input: string): UpgradeDiagnosticData {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isUpgradeDiagnosticData(value)) throw new TypeError("invalid UpgradeDiagnosticData");
  return value;
}

export function encodeUpgradeDiagnosticData(value: UpgradeDiagnosticData): string {
  if (!isUpgradeDiagnosticData(value)) throw new TypeError("invalid UpgradeDiagnosticData");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isUpgradeDiagnosticData(emitted)) throw new TypeError("encoded value is invalid UpgradeDiagnosticData");
  return encoded;
}
