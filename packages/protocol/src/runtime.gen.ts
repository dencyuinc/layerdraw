// Code generated by layerdraw-protocolgen/1; DO NOT EDIT.
// Schema digest: sha256:2e7d23c51aeda025c3c9e443c5ec61282c507209f1a45d3cb370d036899e9062
// SPDX-License-Identifier: Apache-2.0

import type { AuthoringDecision, AuthoringGrantSummary, PolicyRef } from "./access.gen.js";
import { isAuthoringDecision, isAuthoringGrantSummary, isPolicyRef } from "./access.gen.js";
import type { BlobRef, CanonicalNonNegativeInt64, CanonicalPositiveInt64, CanonicalPositiveSafeInteger, CanonicalUint64, CapabilityID, Digest, EndpointInstanceID, Extensions, ManifestETag, NegotiatedProtocol, OperationCapability, Outcome, PageInfo, ProtocolDiagnostic, ProtocolFailure, ProtocolOffer, ProtocolVersion, ReleaseVersion, RequestedCapabilityStatus, Rfc3339Time } from "./common.gen.js";
import { isBlobRef, isCanonicalNonNegativeInt64, isCanonicalPositiveInt64, isCanonicalPositiveSafeInteger, isCanonicalUint64, isCapabilityID, isDigest, isEndpointInstanceID, isExtensions, isManifestETag, isNegotiatedProtocol, isOperationCapability, isOutcome, isPageInfo, isProtocolDiagnostic, isProtocolFailure, isProtocolOffer, isProtocolVersion, isReleaseVersion, isRequestedCapabilityStatus, isRfc3339Time } from "./common.gen.js";
import type { SemanticOperationBatch } from "./engine.gen.js";
import { isSemanticOperationBatch } from "./engine.gen.js";
import type { AuthoringImpact, StableAddress, StateQuerySnapshot } from "./semantic.gen.js";
import { isAuthoringImpact, isStableAddress, isStateQuerySnapshot } from "./semantic.gen.js";

export const schemaDigest = "sha256:2e7d23c51aeda025c3c9e443c5ec61282c507209f1a45d3cb370d036899e9062" as const;

export const maxWireJSONBytes = 8388608 as const;
export const maxWireJSONDepth = 128 as const;

function isObject(value: unknown): value is Record<string, unknown> {
  if (typeof value !== "object" || value === null || Array.isArray(value)) return false;
  const prototype = Object.getPrototypeOf(value); if (prototype !== Object.prototype && prototype !== null) return false;
  for (const key of Reflect.ownKeys(value)) { if (typeof key !== "string" || !hasScalarUnicode(key)) return false; const descriptor = Object.getOwnPropertyDescriptor(value, key); if (descriptor === undefined || !descriptor.enumerable || !("value" in descriptor)) return false; }
  return true;
}

function isJSONArray(value: unknown): value is ReadonlyArray<unknown> {
  if (!Array.isArray(value) || Object.getPrototypeOf(value) !== Array.prototype) return false;
  const descriptors = Object.getOwnPropertyDescriptors(value); const keys = Reflect.ownKeys(value); if (keys.some((key) => typeof key !== "string") || keys.length !== value.length + 1) return false;
  for (let index = 0; index < value.length; index++) { const descriptor = descriptors[String(index)]; if (descriptor === undefined || !descriptor.enumerable || !("value" in descriptor)) return false; }
  return Object.keys(value).length === value.length;
}

function hasOwn(value: Record<string, unknown>, key: string): boolean {
  return Object.prototype.propertyIsEnumerable.call(value, key);
}

function hasOnlyKeys(value: Record<string, unknown>, allowed: ReadonlySet<string>): boolean {
  return Object.keys(value).every((key) => allowed.has(key));
}

function isJSONCompatible(value: unknown, active: Set<object> = new Set<object>(), depth = 0): boolean {
  if (value === null || typeof value === "boolean") return true;
  if (typeof value === "string") return hasScalarUnicode(value);
  const array = isJSONArray(value); if (!array && !isObject(value)) return false;
  if (depth >= maxWireJSONDepth || active.has(value)) return false; active.add(value);
  try { return array ? value.every((item) => isJSONCompatible(item, active, depth + 1)) : Object.values(value).every((item) => isJSONCompatible(item, active, depth + 1)); } finally { active.delete(value); }
}

function hasScalarUnicode(value: unknown): value is string {
  if (typeof value !== "string") return false;
  for (let index = 0; index < value.length; index++) { const code = value.charCodeAt(index); if (code >= 0xd800 && code <= 0xdbff) { const low = value.charCodeAt(index + 1); if (!(low >= 0xdc00 && low <= 0xdfff)) return false; index++; } else if (code >= 0xdc00 && code <= 0xdfff) return false; }
  return true;
}

function matchesCanonicalInt64(value: string): boolean {
  if (!/^(0|-[1-9][0-9]*|[1-9][0-9]*)$/.test(value)) return false;
  try { const parsed = BigInt(value); return parsed >= -(2n ** 63n) && parsed <= (2n ** 63n) - 1n; } catch { return false; }
}

function matchesCanonicalNonNegativeInt64(value: string): boolean {
  if (!/^(0|[1-9][0-9]*)$/.test(value)) return false;
  try { return BigInt(value) <= (2n ** 63n) - 1n; } catch { return false; }
}

function matchesCanonicalUint64(value: string): boolean {
  if (!/^(0|[1-9][0-9]*)$/.test(value)) return false;
  try { const parsed = BigInt(value); return parsed <= (2n ** 64n) - 1n; } catch { return false; }
}

function matchesCanonicalPositiveInt64(value: string): boolean {
  if (!/^[1-9][0-9]*$/.test(value)) return false; try { return BigInt(value) <= (2n ** 63n) - 1n; } catch { return false; }
}

function matchesCanonicalSafeInteger(value: string, minimum = -(2n ** 53n) + 1n): boolean {
  if (!/^(0|-[1-9][0-9]*|[1-9][0-9]*)$/.test(value)) return false; try { const parsed = BigInt(value); return parsed >= minimum && parsed <= (2n ** 53n) - 1n; } catch { return false; }
}

function matchesCanonicalNonNegativeSafeInteger(value: string): boolean {
  return /^(0|[1-9][0-9]*)$/.test(value) && matchesCanonicalSafeInteger(value, 0n);
}

function matchesCanonicalPositiveSafeInteger(value: string): boolean {
  return /^[1-9][0-9]*$/.test(value) && matchesCanonicalSafeInteger(value, 1n);
}

function matchesCanonicalBinary64(value: string, positive: boolean): boolean {
  if (!/^-?(0|[1-9][0-9]*)(?:\.[0-9]+)?(?:e[+-][1-9][0-9]*)?$/.test(value)) return false; const parsed = Number(value); return Number.isFinite(parsed) && !Object.is(parsed, -0) && (!positive || parsed > 0) && String(parsed) === value;
}

function parseProtocolVersion(value: string): readonly [number, number] | undefined {
  const match = /^(0|[1-9][0-9]*)\.(0|[1-9][0-9]*)$/.exec(value); if (match === null) return undefined; const major = Number(match[1]); const minor = Number(match[2]); if (!Number.isSafeInteger(major) || !Number.isSafeInteger(minor) || major > 0xffffffff || minor > 0xffffffff) return undefined; return [major, minor];
}

function parseProtocolVersionRange(value: string): readonly [readonly [number, number], readonly [number, number]] | undefined {
  const parts = value.split(".."); if (parts.length !== 2) return undefined; const lower = parseProtocolVersion(parts[0]!); const upper = parseProtocolVersion(parts[1]!); if (lower === undefined || upper === undefined || lower[0] !== upper[0] || compareProtocolVersions(lower, upper) > 0) return undefined; return [lower, upper];
}

function compareProtocolVersions(left: readonly [number, number], right: readonly [number, number]): number { return left[0] === right[0] ? left[1] - right[1] : left[0] - right[0]; }

function matchesCanonicalSourcePath(value: string): boolean { return value !== "" && !value.startsWith("/") && !value.includes("\\") && !value.includes("\0") && value.split("/").every((segment) => segment !== "" && segment !== "." && segment !== ".."); }

function hasOperatorValueRule(value: Record<string, unknown>, operatorProperty: string, valueProperty: string, valueless: ReadonlySet<string>): boolean { const operator = value[operatorProperty]; if (typeof operator !== "string") return true; return valueless.has(operator) ? !hasOwn(value, valueProperty) : hasOwn(value, valueProperty); }

function hasValidOutcomeEnvelope(value: Record<string, unknown>): boolean { const outcome=value["outcome"]; if (outcome==="success") return hasOwn(value,"payload")&&!hasOwn(value,"failure"); if (outcome==="rejected") return !hasOwn(value,"payload")&&!hasOwn(value,"failure")&&isJSONArray(value["diagnostics"])&&value["diagnostics"].length>0; if (outcome==="failed"||outcome==="cancelled") { const failure=value["failure"]; if (hasOwn(value,"payload")||!isObject(failure)) return false; const category=failure["workbench_category"]; return typeof category!=="string"||(outcome==="cancelled"?category==="cancelled":category!=="cancelled"); } return false; }

function hasValidProtocolOffer(value: Record<string, unknown>): boolean { const range = value["supported_range"]; const bindings = value["versions"]; if (typeof range !== "string" || !isJSONArray(bindings)) return false; const parsedRange = parseProtocolVersionRange(range); if (parsedRange === undefined) return false; const seen = new Set<string>(); for (const raw of bindings) { if (!isObject(raw) || typeof raw["version"] !== "string") return false; const text = raw["version"]; const version = parseProtocolVersion(text); if (version === undefined || compareProtocolVersions(version, parsedRange[0]) < 0 || compareProtocolVersions(version, parsedRange[1]) > 0 || seen.has(text)) return false; seen.add(text); } return true; }

function hasValidLimitCapability(value: Record<string, unknown>): boolean { try { const fallback = BigInt(String(value["default_value"])); const effective = BigInt(String(value["effective_maximum"])); const hard = BigInt(String(value["hard_maximum"])); return fallback <= hard && effective <= hard; } catch { return false; } }

function hasUniqueArrayKey(value: Record<string, unknown>, arrayProperty: string, keyProperty: string): boolean { if (!hasOwn(value,arrayProperty)) return true; const items = value[arrayProperty]; if (!isJSONArray(items)) return false; const seen = new Set<string>(); for (const raw of items) { if (!isObject(raw) || typeof raw[keyProperty] !== "string" || seen.has(raw[keyProperty])) return false; seen.add(raw[keyProperty]); } return true; }

function hasUniqueItems(value: ReadonlyArray<unknown>): boolean { try { return new Set(value.map(canonicalJSONStringify)).size === value.length; } catch { return false; } }

function stableAddressOrderValue(value: unknown, selector: string): string | undefined { if (selector === "$item") return typeof value === "string" ? value : undefined; return isObject(value) && typeof value[selector] === "string" ? value[selector] : undefined; }

function hasStableAddressOrder(value: ReadonlyArray<unknown>, selector: string): boolean { for (let index = 1; index < value.length; index++) { const left = stableAddressOrderValue(value[index - 1], selector); const right = stableAddressOrderValue(value[index], selector); if (left === undefined || right === undefined || compareStableAddresses(left, right) >= 0) return false; } return true; }

function hasCanonicalEnumOrder(value: ReadonlyArray<unknown>, values: ReadonlyArray<string>): boolean { const ranks = new Map(values.map((item, index) => [item, index])); for (let index = 1; index < value.length; index++) { const left = value[index - 1]; const right = value[index]; if (typeof left !== "string" || typeof right !== "string" || ranks.get(left) === undefined || ranks.get(right) === undefined || ranks.get(left)! >= ranks.get(right)!) return false; } return true; }

function isCanonicalLocalIdentifier(value: unknown): value is string { return typeof value === "string" && /^[a-z][a-z0-9_]*$/.test(value); }

function hasCanonicalIdentifierOrder(value: ReadonlyArray<unknown>): boolean { return value.every(isCanonicalLocalIdentifier) && value.every((item, index) => index === 0 || (value[index - 1] as string) < (item as string)); }

function compareUnicodeScalars(left: string, right: string): number { const leftScalars = Array.from(left, (item) => item.codePointAt(0)!); const rightScalars = Array.from(right, (item) => item.codePointAt(0)!); for (let index = 0; index < Math.min(leftScalars.length, rightScalars.length); index++) { if (leftScalars[index] !== rightScalars[index]) return leftScalars[index]! - rightScalars[index]!; } return leftScalars.length - rightScalars.length; }

function hasUnicodeScalarOrder(value: ReadonlyArray<unknown>): boolean { return value.every((item) => typeof item === "string") && value.every((item, index) => index === 0 || compareUnicodeScalars(value[index - 1] as string, item as string) < 0); }

function hasDirectStableAddressOwner(owner: string, child: string): boolean { const parts = child.split(":"); return parts.length >= 2 && parts.slice(0, -2).join(":") === owner; }

function hasAddressOwner(value: Record<string, unknown>, ownerProperty: string, childrenProperty: string, selector: string): boolean { if (!hasOwn(value, ownerProperty)) return true; const owner = value[ownerProperty]; if (typeof owner !== "string") return false; const rawChildren = value[childrenProperty]; let children: ReadonlyArray<unknown>; if (selector === "$value") children = [rawChildren]; else if (selector === "$propertyNames") { if (!isObject(rawChildren)) return false; children = Object.keys(rawChildren); } else { if (!isJSONArray(rawChildren)) return false; children = rawChildren.map((item) => isObject(item) ? item[selector] : undefined); } return children.every((child) => typeof child === "string" && hasDirectStableAddressOwner(owner, child)); }

function compareStableAddresses(left: string, right: string): number { const leftTuple = stableAddressTuple(left); const rightTuple = stableAddressTuple(right); if (leftTuple === undefined || rightTuple === undefined) return 0; if (leftTuple.origin !== rightTuple.origin) return leftTuple.origin - rightTuple.origin; for (let index = 0; index < Math.min(leftTuple.components.length, rightTuple.components.length); index++) { const compared = compareASCII(leftTuple.components[index]!, rightTuple.components[index]!); if (compared !== 0) return compared; } if (leftTuple.components.length !== rightTuple.components.length) return leftTuple.components.length - rightTuple.components.length; if (leftTuple.path.length !== rightTuple.path.length) return leftTuple.path.length - rightTuple.path.length; for (let index = 0; index < leftTuple.path.length; index++) { const leftSegment = leftTuple.path[index]!; const rightSegment = rightTuple.path[index]!; const kind = stableAddressKindRank(leftSegment[0]) - stableAddressKindRank(rightSegment[0]); if (kind !== 0) return kind; const id = compareASCII(leftSegment[1], rightSegment[1]); if (id !== 0) return id; } return 0; }

function stableAddressTuple(value: string): { origin: number; components: ReadonlyArray<string>; path: ReadonlyArray<readonly [string, string]> } | undefined { const parts = value.split(":"); if (parts.length < 3 || parts[0] !== "ldl") return undefined; let origin: number; let components: ReadonlyArray<string>; let pathStart: number; if (parts[1] === "project") { origin = 0; components = [parts[2]!]; pathStart = 3; } else if (parts[1] === "pack" && parts.length >= 4) { origin = 1; components = [parts[2]!, parts[3]!]; pathStart = 4; } else return undefined; if ((parts.length - pathStart) % 2 !== 0) return undefined; const path: Array<readonly [string, string]> = []; for (let index = pathStart; index < parts.length; index += 2) path.push([parts[index]!, parts[index + 1]!]); return {origin, components, path}; }

function stableAddressKindRank(kind: string): number { return new Map<string, number>([["entity-type",0],["relation-type",1],["layer",2],["entity",3],["relation",4],["query",5],["view",6],["reference",7],["column",8],["constraint",9],["row",10],["parameter",11],["table-column",12],["export",13]]).get(kind) ?? Number.MAX_SAFE_INTEGER; }

function compareASCII(left: string, right: string): number { return left < right ? -1 : left > right ? 1 : 0; }

function hasDisjointArrays(value: Record<string, unknown>, leftProperty: string, rightProperty: string): boolean { const left = hasOwn(value, leftProperty) ? value[leftProperty] : []; const right = hasOwn(value, rightProperty) ? value[rightProperty] : []; if (!isJSONArray(left) || !isJSONArray(right)) return false; const seen = new Set(left); return right.every((item) => !seen.has(item)); }

function compareCanonicalUnsignedDecimals(left: string, right: string): number | undefined { if (!/^(0|[1-9][0-9]*)$/.test(left) || !/^(0|[1-9][0-9]*)$/.test(right)) return undefined; return left.length === right.length ? (left < right ? -1 : left > right ? 1 : 0) : left.length - right.length; }

function semanticSubjectKindRank(kind: string): number {
  return ["project","pack","entity_type","relation_type","layer","entity","relation","query","view","reference","entity_type_column","entity_type_constraint","relation_type_column","relation_type_constraint","entity_row","relation_row","query_parameter","view_table_column","view_export"].indexOf(kind);
}
function compareModuleOrder(left: Record<string,unknown>, right: Record<string,unknown>): number | undefined {
  const origin = (raw: unknown): readonly [number,string] | undefined => {
    if (!isObject(raw) || typeof raw["kind"] !== "string") return undefined;
    if (raw["kind"] === "project") return [0,""];
    return raw["kind"] === "pack" && typeof raw["pack_address"] === "string" ? [1,raw["pack_address"]] : undefined;
  };
  const a = origin(left["origin"]), b = origin(right["origin"]), aPath = left["module_path"], bPath = right["module_path"];
  if (a === undefined || b === undefined || typeof aPath !== "string" || typeof bPath !== "string") return undefined;
  if (a[0] !== b[0]) return a[0]-b[0];
  const pack = compareUnicodeScalars(a[1],b[1]); return pack !== 0 ? pack : compareUnicodeScalars(aPath,bPath);
}
function compareRangePosition(left: Record<string,unknown>, right: Record<string,unknown>): number | undefined {
  const aStart = left["start_byte"], bStart = right["start_byte"], aEnd = left["end_byte"], bEnd = right["end_byte"];
  if (typeof aStart !== "string" || typeof bStart !== "string" || typeof aEnd !== "string" || typeof bEnd !== "string") return undefined;
  const start = compareCanonicalUnsignedDecimals(aStart,bStart); if (start === undefined || start !== 0) return start;
  return compareCanonicalUnsignedDecimals(aEnd,bEnd);
}
function compareCanonicalCollection(profile: string, left: unknown, right: unknown): number | undefined {
  if (!isObject(left) || !isObject(right)) return undefined;
  const stable = (property: string): number | undefined => typeof left[property] === "string" && typeof right[property] === "string" ? compareStableAddresses(left[property],right[property]) : undefined;
  const text = (property: string): number | undefined => typeof left[property] === "string" && typeof right[property] === "string" ? compareUnicodeScalars(left[property],right[property]) : undefined;
  const kind = (property: string): number | undefined => {
    if (typeof left[property] !== "string" || typeof right[property] !== "string") return undefined;
    const a = semanticSubjectKindRank(left[property]), b = semanticSubjectKindRank(right[property]); return a < 0 || b < 0 ? undefined : a-b;
  };
  const range = (): number | undefined => isObject(left["range"]) && isObject(right["range"]) ? compareRangePosition(left["range"],right["range"]) : undefined;
  const identity = (): number | undefined => { const a = left["before_address"] ?? left["after_address"], b = right["before_address"] ?? right["after_address"]; return typeof a === "string" && typeof b === "string" ? compareStableAddresses(a,b) : undefined; };
  const conflictAddress = (property: string): number | undefined => { const a=left[property] ?? "", b=right[property] ?? ""; return typeof a === "string" && typeof b === "string" ? (a === "" || b === "" ? compareUnicodeScalars(a,b) : compareStableAddresses(a,b)) : undefined; };
  const conflictKind = (): number | undefined => { const order = ["stale_revision","subject_changed","subtree_changed","child_set_changed","same_field_changed","delete_vs_update","duplicate_identity","reference_broken","schema_row_incompatible","placement_changed","project_identity_changed"]; if (typeof left["kind"] !== "string" || typeof right["kind"] !== "string") return undefined; const a=order.indexOf(left["kind"]), b=order.indexOf(right["kind"]); return a < 0 || b < 0 ? undefined : a-b; };
  const conflictChildKind = (): number | undefined => { const a=left["child_kind"] ?? "", b=right["child_kind"] ?? ""; if (typeof a !== "string" || typeof b !== "string") return undefined; if (a === "" || b === "") return compareUnicodeScalars(a,b); const ar=semanticSubjectKindRank(a), br=semanticSubjectKindRank(b); return ar < 0 || br < 0 ? undefined : ar-br; };
  const path = (): number | undefined => { const a = left["path"] ?? [], b = right["path"] ?? []; if (!Array.isArray(a) || !Array.isArray(b)) return undefined; for (let index=0; index<Math.min(a.length,b.length); index++) { if (typeof a[index] !== "string" || typeof b[index] !== "string") return undefined; const value=compareUnicodeScalars(a[index],b[index]); if (value !== 0) return value; } return a.length-b.length; };
  const optionalSourceRange = (): number | undefined => { const a=left["source_range"], b=right["source_range"]; if (!isObject(a) || !isObject(b)) return isObject(a) ? 1 : isObject(b) ? -1 : 0; return compareRangePosition(a,b); };
  const stringArray = (property: string): number | undefined => { const a=left[property] ?? [], b=right[property] ?? []; if (!Array.isArray(a) || !Array.isArray(b)) return undefined; for (let index=0; index<Math.min(a.length,b.length); index++) { if (typeof a[index] !== "string" || typeof b[index] !== "string") return undefined; const value=compareUnicodeScalars(a[index],b[index]); if (value !== 0) return value; } return a.length-b.length; };
  const chain = (...comparisons: ReadonlyArray<() => number | undefined>): number | undefined => { for (const compare of comparisons) { const value = compare(); if (value === undefined || value !== 0) return value; } return 0; };
  if (profile === "authored_field_path") return stringArray("tokens");
  if (profile === "authoring_impact") {
    const address = (): number | undefined => { const a=left["subject_address"] ?? left["owner_address"] ?? "", b=right["subject_address"] ?? right["owner_address"] ?? ""; return typeof a === "string" && typeof b === "string" ? (a === "" || b === "" ? compareUnicodeScalars(a,b) : compareStableAddresses(a,b)) : undefined; };
    return chain(address,() => text("capability"),() => text("action"));
  }
  if (profile === "bounded_text_chunk") {
    const address = (): number | undefined => { const a=left["address"] ?? left["owner_address"], b=right["address"] ?? right["owner_address"]; return typeof a === "string" && typeof b === "string" ? compareStableAddresses(a,b) : undefined; };
    const offset = (): number | undefined => { const a=left["source_chunk"] ?? left["text_chunk"], b=right["source_chunk"] ?? right["text_chunk"]; return isObject(a) && isObject(b) && typeof a["offset"] === "string" && typeof b["offset"] === "string" ? compareCanonicalUnsignedDecimals(a["offset"],b["offset"]) : undefined; };
    return chain(address,offset);
  }
  if (profile === "child_set") return chain(() => stable("owner_address"),() => kind("child_kind"));
  if (profile === "conflict") return chain(conflictKind,() => conflictAddress("target_address"),() => conflictAddress("owner_address"),conflictChildKind,path);
  if (profile === "reference_id") return text("id");
  if (profile === "subject_kind") return kind("kind");
  if (profile === "module_scope") return isObject(left["module"]) && isObject(right["module"]) ? compareModuleOrder(left["module"],right["module"]) : undefined;
  if (profile === "neighbor") return typeof left["traversal_index"] === "string" && typeof right["traversal_index"] === "string" ? compareCanonicalUnsignedDecimals(left["traversal_index"],right["traversal_index"]) : undefined;
  if (profile === "source_file") return compareModuleOrder(left,right);
  if (profile === "source_asset") return chain(() => stable("subject_address"),() => text("locator"));
  if (profile === "source_patch") {
    const leftRange = left["source_range"], rightRange = right["source_range"];
    if (!isObject(leftRange) || !isObject(rightRange)) return undefined;
    const module = compareModuleOrder(leftRange,rightRange);
    return module === 0 ? compareRangePosition(leftRange,rightRange) : module;
  }
  if (profile === "semantic_diff") return chain(identity,() => text("kind"));
  if (profile === "semantic_map_entry") return text("key");
  if (profile === "source_diff") {
    const module = (value: Record<string,unknown>): Record<string,unknown> | undefined => isObject(value["source_range"]) ? value["source_range"] : isObject(value["before_module"]) ? value["before_module"] : isObject(value["after_module"]) ? value["after_module"] : undefined;
    const primary = (): number | undefined => { const a=module(left), b=module(right); return a === undefined || b === undefined ? undefined : compareModuleOrder(a,b); };
    const after = (): number | undefined => { const a=left["after_module"], b=right["after_module"]; if (!isObject(a) || !isObject(b)) return isObject(a) ? 1 : isObject(b) ? -1 : 0; return compareModuleOrder(a,b); };
    return chain(primary,() => text("kind"),optionalSourceRange,after);
  }
  if (profile === "source_range") { const module=compareModuleOrder(left,right); return module === 0 ? compareRangePosition(left,right) : module; }
  if (profile === "subgraph") return typeof left["traversal_index"] === "string" && typeof right["traversal_index"] === "string" ? compareCanonicalUnsignedDecimals(left["traversal_index"],right["traversal_index"]) : undefined;
  if (profile === "semantic_reference") return chain(() => stable("source_address"),range,() => stable("target_address"),() => kind("target_kind"),() => text("via"));
  if (profile === "source_binding") {
    const owner = (): number | undefined => { const a = left["target_owner_address"] ?? "", b = right["target_owner_address"] ?? ""; if (typeof a !== "string" || typeof b !== "string") return undefined; return a === "" || b === "" ? compareUnicodeScalars(a,b) : compareStableAddresses(a,b); };
    return chain(() => stable("source_address"),range,() => stable("target_address"),() => kind("target_kind"),owner,() => text("via"));
  }
  if (profile === "export_binding") {
    const module = (): number | undefined => isObject(left["module"]) && isObject(right["module"]) ? compareModuleOrder(left["module"],right["module"]) : undefined;
    const reexport = (): number | undefined => typeof left["re_export"] === "boolean" && typeof right["re_export"] === "boolean" ? Number(left["re_export"])-Number(right["re_export"]) : undefined;
    return chain(module,range,() => text("public_name"),() => stable("target_address"),reexport);
  }
  return undefined;
}
function hasCanonicalCollectionOrder(values: ReadonlyArray<unknown>, profile: string): boolean {
  return values.every((item,index) => {
    if (index === 0) return true;
    const previous = values[index-1];
    if ((compareCanonicalCollection(profile,previous,item) ?? 0) >= 0) return false;
    if (profile !== "source_patch" || !isObject(previous) || !isObject(item) || !isObject(previous["source_range"]) || !isObject(item["source_range"])) return profile !== "source_patch";
    if (compareModuleOrder(previous["source_range"],item["source_range"]) !== 0) return true;
    const leftEnd = previous["source_range"]["end_byte"], rightStart = item["source_range"]["start_byte"];
    if (typeof leftEnd !== "string" || typeof rightStart !== "string") return false;
    const overlap = compareCanonicalUnsignedDecimals(leftEnd,rightStart);
    return overlap !== undefined && overlap <= 0;
  });
}
function hasValidChildSet(value: Record<string,unknown>): boolean {
  const owner = value["owner_address"], child = value["child_kind"];
  if (typeof owner !== "string" || typeof child !== "string") return false;
  const ownerKind = authoritySubject(owner)?.kind;
  const allowed: Readonly<Record<string,ReadonlyArray<string>>> = {
    project:["entity_type","relation_type","layer","entity","relation","query","view","reference"],
    pack:["entity_type","relation_type","query","view","reference"],
    entity_type:["entity_type_column","entity_type_constraint"], relation_type:["relation_type_column","relation_type_constraint"],
    entity:["entity_row"], relation:["relation_row"], query:["query_parameter"], view:["view_table_column","view_export"],
  };
  return ownerKind !== undefined && (allowed[ownerKind]?.includes(child) ?? false);
}

function hasOrderedPair(value: Record<string, unknown>, lowerProperty: string, upperProperty: string, comparison: string): boolean { if (!hasOwn(value, lowerProperty) || !hasOwn(value, upperProperty)) return true; const lower = value[lowerProperty]; const upper = value[upperProperty]; if (typeof lower !== "string" || typeof upper !== "string") return false; if (comparison === "unsigned_decimal") { const ordered = compareCanonicalUnsignedDecimals(lower, upper); return ordered !== undefined && ordered <= 0; } if (comparison === "finite_binary64") { const lowerValue = Number(lower); const upperValue = Number(upper); return Number.isFinite(lowerValue) && Number.isFinite(upperValue) && lowerValue <= upperValue; } return false; }

function hasAddressTerminalID(value: Record<string, unknown>, addressProperty: string, idProperty: string): boolean { const address = value[addressProperty]; const id = value[idProperty]; return typeof address === "string" && typeof id === "string" && address.split(":").at(-1) === id; }

function authorityStateType(field: string): string {
  if (["system.created_at","system.updated_at","provenance.observed_at","provenance.verified_at","provenance.stale_after"].includes(field)) return "datetime";
  if (["system.created_by.kind","system.updated_by.kind","provenance.source.kind","provenance.verified_by.kind"].includes(field)) return "enum";
  return field === "provenance.confidence" ? "number" : "string";
}

const authorityStateFields = ["system.created_at","system.updated_at","system.created_by.kind","system.created_by.id","system.created_by.display_name","system.updated_by.kind","system.updated_by.id","system.updated_by.display_name","system.created_revision","system.updated_revision","provenance.source.kind","provenance.source.label","provenance.source.uri","provenance.source.external_id","provenance.observed_at","provenance.verified_at","provenance.stale_after","provenance.verified_by.kind","provenance.verified_by.id","provenance.verified_by.display_name","provenance.confidence"] as const;
const authorityStateSubjects = ["entity","relation","entity_row","relation_row"] as const;

function hasValidStateRead(value: Record<string, unknown>): boolean { return typeof value["field_path"] === "string" && value["value_type"] === authorityStateType(value["field_path"]); }
function hasStateReadOrder(values: ReadonlyArray<unknown>): boolean { let previous = -1; for (const raw of values) { if (!isObject(raw) || !hasValidStateRead(raw)) return false; const subject = authorityStateSubjects.indexOf(raw["subject_kind"] as never); const field = authorityStateFields.indexOf(raw["field_path"] as never); const rank = subject * authorityStateFields.length + field; if (subject < 0 || field < 0 || rank <= previous) return false; previous = rank; } return true; }

function authorityRealDate(value: string): boolean { const match = /^(\d{4})-(\d{2})-(\d{2})$/.exec(value); if (match === null || match[1] === "0000") return false; const year = Number(match[1]); const month = Number(match[2]); const day = Number(match[3]); const leap = year % 4 === 0 && (year % 100 !== 0 || year % 400 === 0); const days = [31, leap ? 29 : 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31]; return month >= 1 && month <= 12 && day >= 1 && day <= days[month - 1]!; }
function hasValidRecipeScalar(value: Record<string, unknown>): boolean { const kind = value["kind"]; const text = value["string_value"]; if (kind === "date") return typeof text === "string" && authorityRealDate(text); if (kind === "datetime") { if (typeof text !== "string") return false; const match = /^(\d{4}-\d{2}-\d{2})T(?:[01]\d|2[0-3]):[0-5]\d:[0-5]\d(?:\.(\d{1,3}))?Z$/.exec(text); return match !== null && authorityRealDate(match[1]!) && (match[2] === undefined || !match[2].endsWith("0")); } return kind !== "enum" || typeof text === "string" && text.length > 0; }

function authorityHostname(value: string): boolean { return value.length > 0 && value.length <= 253 && value === value.toLowerCase() && !value.endsWith(".") && value.split(".").every((label) => label.length > 0 && label.length <= 63 && /^[a-z0-9](?:[a-z0-9-]*[a-z0-9])?$/.test(label)); }
function authorityParseIPv4(value: string): ReadonlyArray<number> | undefined { const parts = value.split("."); if (parts.length !== 4 || !parts.every((part) => /^(0|[1-9]\d{0,2})$/.test(part) && Number(part) <= 255)) return undefined; return parts.map(Number); }
function authorityParseIPv6(value: string): ReadonlyArray<number> | undefined {
  if (value.includes("%")) return undefined;
  let expanded = value;
  if (value.includes(".")) { const colon = value.lastIndexOf(":"); const ipv4 = colon < 0 ? undefined : authorityParseIPv4(value.slice(colon + 1)); if (ipv4 === undefined) return undefined; expanded = value.slice(0,colon + 1) + ((ipv4[0]! << 8) | ipv4[1]!).toString(16) + ":" + ((ipv4[2]! << 8) | ipv4[3]!).toString(16); }
  if (!/^[0-9A-Fa-f:]+$/.test(expanded) || expanded.split("::").length > 2) return undefined;
  const hasElision = expanded.includes("::"), halves = expanded.split("::");
  const left = halves[0] === "" ? [] : halves[0]!.split(":"), right = !hasElision || halves[1] === "" ? [] : halves[1]!.split(":");
  if (![...left,...right].every((part) => /^[0-9A-Fa-f]{1,4}$/.test(part))) return undefined;
  const omitted = 8 - left.length - right.length;
  if (hasElision ? omitted < 1 : omitted !== 0) return undefined;
  const words = [...left.map((part) => Number.parseInt(part,16)), ...Array(omitted).fill(0), ...right.map((part) => Number.parseInt(part,16))];
  return words.flatMap((word) => [word >>> 8, word & 255]);
}
function authorityFormatIPv6(bytes: ReadonlyArray<number>): string {
  const mapped = bytes.slice(0,10).every((value) => value === 0) && bytes[10] === 255 && bytes[11] === 255;
  if (mapped) return "::ffff:" + bytes.slice(12).join(".");
  const words = Array.from({length:8},(_,index) => (bytes[index*2]! << 8) | bytes[index*2+1]!);
  let bestStart = -1, bestLength = 0;
  for (let index = 0; index < words.length;) { if (words[index] !== 0) { index++; continue; } let end = index; while (end < words.length && words[end] === 0) end++; if (end-index > bestLength && end-index >= 2) { bestStart=index; bestLength=end-index; } index=end; }
  let result = "";
  for (let index = 0; index < words.length;) { if (index === bestStart) { result += "::"; index += bestLength; continue; } if (result !== "" && !result.endsWith(":")) result += ":"; result += words[index]!.toString(16); index++; }
  return result;
}
function authorityCanonicalIP(value: string): {bytes:ReadonlyArray<number>;bits:number} | undefined { const ipv4=authorityParseIPv4(value); if (ipv4 !== undefined) return {bytes:ipv4,bits:32}; const ipv6=authorityParseIPv6(value); return ipv6 !== undefined && authorityFormatIPv6(ipv6) === value ? {bytes:ipv6,bits:128} : undefined; }
function authorityCanonicalCIDR(value: string): boolean { const parts=value.split("/"); if (parts.length !== 2 || !/^(0|[1-9]\d*)$/.test(parts[1]!)) return false; const address=authorityCanonicalIP(parts[0]!); if (address === undefined) return false; const prefix=Number(parts[1]); if (!Number.isSafeInteger(prefix) || prefix > address.bits) return false; for (let index=0; index<address.bytes.length; index++) { const remaining=prefix-index*8; const mask=remaining >= 8 ? 255 : remaining <= 0 ? 0 : (255 << (8-remaining)) & 255; if ((address.bytes[index]! & mask) !== address.bytes[index]) return false; } return true; }
function authorityURIAlpha(value: string): boolean { return /^[A-Za-z]$/.test(value); }
function authorityURIDigit(value: string): boolean { return /^[0-9]$/.test(value); }
function authorityURIHex(value: string): boolean { return /^[0-9A-Fa-f]$/.test(value); }
function authorityURIUnreserved(value: string): boolean { return authorityURIAlpha(value) || authorityURIDigit(value) || "-._~".includes(value); }
function authorityURIComponent(value: string, allowEmpty: boolean, extra: string): boolean { if (value === "") return allowEmpty; for (let index=0; index<value.length; index++) { const ch=value[index]!; if (ch === "%") { if (index+2 >= value.length || !authorityURIHex(value[index+1]!) || !authorityURIHex(value[index+2]!)) return false; index += 2; continue; } if (!authorityURIUnreserved(ch) && !("!$&'()*+,;="+extra).includes(ch)) return false; } return true; }
function authorityIPLiteral(value: string): boolean { if (authorityParseIPv6(value) !== undefined) return true; if (value.length < 4 || value[0] !== "v" && value[0] !== "V") return false; const dot=value.indexOf("."); if (dot < 2 || !Array.from(value.slice(1,dot)).every(authorityURIHex)) return false; return value.slice(dot+1) !== "" && authorityURIComponent(value.slice(dot+1),false,":"); }
function authorityURIAuthority(value: string): boolean { if (value.split("@").length > 2) return false; let hostPort=value; const at=value.indexOf("@"); if (at >= 0) { if (!authorityURIComponent(value.slice(0,at),true,":")) return false; hostPort=value.slice(at+1); } if (hostPort.startsWith("[")) { const close=hostPort.indexOf("]"); if (close <= 1) return false; const rest=hostPort.slice(close+1); return authorityIPLiteral(hostPort.slice(1,close)) && (rest === "" || rest.startsWith(":") && Array.from(rest.slice(1)).every(authorityURIDigit)); } if (hostPort.includes("[") || hostPort.includes("]")) return false; let host=hostPort; const colon=hostPort.lastIndexOf(":"); if (colon >= 0) { host=hostPort.slice(0,colon); if (host.includes(":") || !Array.from(hostPort.slice(colon+1)).every(authorityURIDigit)) return false; } return authorityURIComponent(host,true,""); }
function authorityAbsoluteURI(value: string): boolean { const colon=value.indexOf(":"); if (colon <= 0 || !/^[A-Za-z][A-Za-z0-9+.-]*$/.test(value.slice(0,colon)) || Array.from(value).some((ch) => ch.codePointAt(0)! >= 128 || ch.codePointAt(0)! < 32 || ch.codePointAt(0) === 127) || value.includes("\\")) return false; for (let index=0; index<value.length; index++) { const ch=value[index]!; if (ch === "%") { if (index+2 >= value.length || !authorityURIHex(value[index+1]!) || !authorityURIHex(value[index+2]!)) return false; index += 2; continue; } if (!authorityURIUnreserved(ch) && !":/?#[]@!$&'()*+,;=%".includes(ch)) return false; } const remainder=value.slice(colon+1); if (remainder.split("#").length > 2) return false; const hash=remainder.indexOf("#"), beforeFragment=hash < 0 ? remainder : remainder.slice(0,hash), fragment=hash < 0 ? undefined : remainder.slice(hash+1); if (fragment !== undefined && !authorityURIComponent(fragment,true,"/?:@")) return false; const question=beforeFragment.indexOf("?"), hierarchical=question < 0 ? beforeFragment : beforeFragment.slice(0,question), query=question < 0 ? undefined : beforeFragment.slice(question+1); if (query !== undefined && !authorityURIComponent(query,true,"/?:@")) return false; if (hierarchical.startsWith("//")) { const authorityAndPath=hierarchical.slice(2), slash=authorityAndPath.indexOf("/"), authority=slash < 0 ? authorityAndPath : authorityAndPath.slice(0,slash), path=slash < 0 ? "" : authorityAndPath.slice(slash); return authorityURIAuthority(authority) && authorityURIComponent(path,true,"/:@"); } return authorityURIComponent(hierarchical,true,"/:@"); }
function authorityStringFormat(format: string, value: string): boolean {
  if (format === "hostname") return authorityHostname(value);
  if (format === "email") { const match = new RegExp("^[A-Za-z0-9!#$%&'*+/=?^_\\x60{|}~-]+(?:\\.[A-Za-z0-9!#$%&'*+/=?^_\\x60{|}~-]+)*@([A-Za-z0-9.-]+)$").exec(value); return match !== null && authorityHostname(match[1]!.toLowerCase()); }
  if (format === "ipv4") return authorityParseIPv4(value) !== undefined;
  if (format === "ipv6") { const parsed=authorityParseIPv6(value); return parsed !== undefined && authorityFormatIPv6(parsed) === value; }
  if (format === "cidr") return authorityCanonicalCIDR(value);
  if (format === "uri") return authorityAbsoluteURI(value);
  return false;
}

function hasValidQueryParameter(value: Record<string, unknown>): boolean {
  const type = value["value_type"]; const reserved = value["reserved_enum_values"];
  if (typeof type !== "string" || !isJSONArray(reserved)) return false;
  const hasEnum = hasOwn(value, "enum_values"), hasFormat = hasOwn(value, "format"), hasMin = hasOwn(value, "min"), hasMax = hasOwn(value, "max"), hasMinLength = hasOwn(value, "min_length"), hasMaxLength = hasOwn(value, "max_length");
  if (type === "enum") { const active = value["enum_values"]; if (!isJSONArray(active) || active.length === 0 || !active.every((item) => typeof item === "string" && item.length > 0) || !reserved.every((item) => typeof item === "string" && item.length > 0)) return false; }
  else if (hasEnum || reserved.length !== 0) return false;
  if (hasFormat && type !== "string" || (hasMin || hasMax) && type !== "integer" && type !== "number" || (hasMinLength || hasMaxLength) && type !== "string") return false;
  if (type === "integer" && [value["min"], value["max"]].some((item) => item !== undefined && (typeof item !== "string" || !matchesCanonicalSafeInteger(item)))) return false;
  const rawDefault = value["default"]; if (rawDefault === undefined) return true; if (!isObject(rawDefault) || rawDefault["kind"] !== type) return false;
  if (type === "enum") return (value["enum_values"] as ReadonlyArray<unknown>).includes(rawDefault["string_value"]);
  if (type === "string") { const text = rawDefault["string_value"]; if (typeof text !== "string") return false; const length = Array.from(text).length; if (typeof value["min_length"] === "string" && BigInt(length) < BigInt(value["min_length"]) || typeof value["max_length"] === "string" && BigInt(length) > BigInt(value["max_length"])) return false; return typeof value["format"] !== "string" || authorityStringFormat(value["format"], text); }
  if (type === "integer" || type === "number") { const property = type === "integer" ? "integer_value" : "number_value"; const text = rawDefault[property]; if (typeof text !== "string") return false; const number = Number(text); return Number.isFinite(number) && (typeof value["min"] !== "string" || number >= Number(value["min"])) && (typeof value["max"] !== "string" || number <= Number(value["max"])); }
  return true;
}

type AuthoritySubject = {kind: string; owner: string};
function authoritySubject(address: string): AuthoritySubject | undefined { const parts = address.split(":"); if (parts.length < 3 || parts[0] !== "ldl") return undefined; let start = 3, root = "project"; if (parts[1] === "pack") { if (parts.length < 4) return undefined; start = 4; root = "pack"; } else if (parts[1] !== "project") return undefined; if (parts.length === start) return {kind: root, owner: ""}; if ((parts.length - start) % 2 !== 0) return undefined; const last = parts.at(-2)!; let kind = new Map<string,string>([["entity-type","entity_type"],["relation-type","relation_type"],["layer","layer"],["entity","entity"],["relation","relation"],["query","query"],["view","view"],["reference","reference"],["parameter","query_parameter"],["table-column","view_table_column"],["export","view_export"]]).get(last); if (last === "row") kind = parts.at(-4) === "entity" ? "entity_row" : parts.at(-4) === "relation" ? "relation_row" : undefined; if (last === "column" || last === "constraint") { const prefix = parts.at(-4) === "entity-type" ? "entity_type" : parts.at(-4) === "relation-type" ? "relation_type" : undefined; kind = prefix === undefined ? undefined : prefix + "_" + last; } return kind === undefined ? undefined : {kind, owner: parts.slice(0, -2).join(":")}; }
type AuthorityRole = {kind: string; address?: string; addresses?: string; owner?: string; owner_policy?: string};
function hasStableAddressRoles(value: Record<string, unknown>, rules: ReadonlyArray<AuthorityRole>): boolean { for (const rule of rules) { const expected = value[rule.kind]; if (typeof expected !== "string") return false; let addresses: ReadonlyArray<unknown>; if (rule.address !== undefined) addresses = [value[rule.address]]; else { const raw = value[rule.addresses!]; if (!isJSONArray(raw)) return false; addresses = raw; } const owner = rule.owner === undefined ? undefined : value[rule.owner]; for (const raw of addresses) { if (typeof raw !== "string") return false; const subject = authoritySubject(raw); if (subject === undefined || subject.kind !== expected) return false; const present = typeof owner === "string"; if (rule.owner_policy === "children" && (!present || subject.owner !== owner) || rule.owner_policy === "exact" && ((subject.owner !== "") !== present || present && subject.owner !== owner) || rule.owner_policy === "if_present" && present && subject.owner !== owner || rule.owner_policy === "row_only" && ((subject.kind === "entity_row" || subject.kind === "relation_row") !== present || present && subject.owner !== owner)) return false; } } return true; }

type AuthorityOperand = {kind: string; scalar: string; address: string};
function authorityOperand(raw: unknown): AuthorityOperand | undefined { if (!isObject(raw) || typeof raw["kind"] !== "string") return undefined; const value = {kind: raw["kind"], scalar: typeof raw["scalar_type"] === "string" ? raw["scalar_type"] : "", address: typeof raw["address_kind"] === "string" ? raw["address_kind"] : ""}; return value.kind === "scalar" && value.scalar !== "" || value.kind === "address" && value.address !== "" || value.kind === "string_set" ? value : undefined; }
function authorityOperandEqual(a: AuthorityOperand, b: AuthorityOperand): boolean { return a.kind === b.kind && a.scalar === b.scalar && a.address === b.address; }
function authorityFieldOperand(field: string): AuthorityOperand | undefined { if (["id","display_name","description"].includes(field)) return {kind:"scalar",scalar:"string",address:""}; if (field === "tags") return {kind:"string_set",scalar:"",address:""}; if (field === "layer") return {kind:"address",scalar:"",address:"layer"}; if (field === "from" || field === "to") return {kind:"address",scalar:"",address:"entity"}; return undefined; }
function authorityScalarCompare(a: Record<string, unknown>, b: Record<string, unknown>): number { if (a["kind"] === "boolean") return Number(a["boolean_value"]) - Number(b["boolean_value"]); if (a["kind"] === "integer") return BigInt(String(a["integer_value"])) < BigInt(String(b["integer_value"])) ? -1 : BigInt(String(a["integer_value"])) > BigInt(String(b["integer_value"])) ? 1 : 0; if (a["kind"] === "number") return Number(a["number_value"]) - Number(b["number_value"]); return compareUnicodeScalars(String(a["string_value"]), String(b["string_value"])); }
function authorityScalarSet(raw: unknown, type: string): boolean { return isJSONArray(raw) && raw.every((item) => isObject(item) && item["kind"] === type); }
function authorityPredicateValue(value: Record<string, unknown>, operator: string, operand: AuthorityOperand): boolean { const kind = value["kind"]; if (kind === "parameter") return operator !== "in" && operator !== "not_in" && (operand.kind === "scalar" || operand.kind === "string_set" && operator === "contains"); if (operator === "in" || operator === "not_in") { if (operand.kind === "scalar") return kind === "scalar_set" && authorityScalarSet(value["scalar_values"], operand.scalar); if (operand.kind === "address" && kind === "address_set" && isJSONArray(value["address_values"])) return value["address_values"].every((item) => typeof item === "string" && authoritySubject(item)?.kind === operand.address); return false; } if (operand.kind === "string_set") return operator === "eq" || operator === "ne" ? kind === "scalar_set" && authorityScalarSet(value["scalar_values"], "string") : operator === "contains" && kind === "scalar" && isObject(value["scalar_value"]) && value["scalar_value"]["kind"] === "string"; if (operand.kind === "scalar") return kind === "scalar" && isObject(value["scalar_value"]) && value["scalar_value"]["kind"] === operand.scalar; return operand.kind === "address" && kind === "address" && typeof value["address_value"] === "string" && authoritySubject(value["address_value"])?.kind === operand.address; }
function hasValidRecipePredicate(value: Record<string, unknown>, predicateKind: string): boolean { const kind = value["kind"]; if (kind === "all" || kind === "any") return isJSONArray(value["children"]) && value["children"].every((item) => isObject(item) && hasValidRecipePredicate(item, predicateKind)); if (kind === "not") return isObject(value["child"]) && hasValidRecipePredicate(value["child"], predicateKind); if (kind === "rows") return isObject(value["predicate"]) && hasValidRecipePredicate(value["predicate"], "row"); if (kind !== "field" && kind !== "cell" && kind !== "state") return true; const operand = authorityOperand(value["operand_type"]); if (operand === undefined) return false; if (kind === "field") { const expected = authorityFieldOperand(String(value["field"])); if (expected !== undefined && !authorityOperandEqual(operand, expected)) return false; } if (kind === "state" && !authorityOperandEqual(operand, {kind:"scalar",scalar:authorityStateType(String(value["field_path"])),address:""})) return false; const operator = value["operator"]; if (typeof operator !== "string") return false; let compatible = ["eq","ne","exists","missing"].includes(operator); if (["lt","lte","gt","gte"].includes(operator)) compatible = operand.kind === "scalar" && ["integer","number","date","datetime"].includes(operand.scalar); if (["in","not_in"].includes(operator)) compatible = operand.kind === "scalar" || operand.kind === "address"; if (operator === "contains") compatible = operand.kind === "string_set" || operand.kind === "scalar" && operand.scalar === "string"; if (["starts_with","ends_with"].includes(operator)) compatible = operand.kind === "scalar" && operand.scalar === "string"; return compatible && (["exists","missing"].includes(operator) || isObject(value["value"]) && authorityPredicateValue(value["value"], operator, operand)); }
function hasValidViewProjection(value: Record<string, unknown>, kind: string): boolean { const distinct = (a: string, b: string): boolean => typeof value[a] === "string" && typeof value[b] === "string" && value[a] !== value[b]; if (kind !== "composed") { const pairs = new Map<string,readonly [string,string]>([["diagram",["source_endpoint","target_endpoint"]],["flow",["source_endpoint","target_endpoint"]],["matrix",["row_endpoint","column_endpoint"]],["tree",["parent_endpoint","child_endpoint"]]]); const pair = pairs.get(kind); return pair !== undefined && distinct(pair[0], pair[1]); } const mode = value["mode"]; const present = (name: string): boolean => hasOwn(value, name); if (mode === "nest") return distinct("parent_endpoint","child_endpoint") && !present("overlay_endpoint") && !present("target_endpoint") && !present("badge_endpoint"); if (mode === "overlay") return distinct("overlay_endpoint","target_endpoint") && !present("parent_endpoint") && !present("child_endpoint") && !present("badge_endpoint"); if (mode === "badge") return distinct("badge_endpoint","target_endpoint") && !present("parent_endpoint") && !present("child_endpoint") && !present("overlay_endpoint"); return (mode === "edge" || mode === "hide") && ["parent_endpoint","child_endpoint","overlay_endpoint","target_endpoint","badge_endpoint"].every((name) => !present(name)); }

function authorityContextOperand(field: string, context: string): AuthorityOperand | undefined { if (["id","display_name","description"].includes(field)) return {kind:"scalar",scalar:"string",address:""}; if (field === "tags") return {kind:"string_set",scalar:"",address:""}; if (field === "layer") return context === "entity" ? {kind:"address",scalar:"",address:"layer"} : undefined; if (field === "from" || field === "to") return context === "relation" ? {kind:"address",scalar:"",address:"entity"} : undefined; if (field === "address") return context === "entity" ? {kind:"address",scalar:"",address:"entity"} : context === "relation" ? {kind:"address",scalar:"",address:"relation"} : undefined; if (field === "type") return context === "entity" ? {kind:"address",scalar:"",address:"entity_type"} : context === "relation" ? {kind:"address",scalar:"",address:"relation_type"} : undefined; return undefined; }
type AuthorityDependencies = {layer:Set<string>; entity_type:Set<string>; relation_type:Set<string>; entity:Set<string>; relation:Set<string>; column:Set<string>; parameter:Set<string>; state:Map<string,Record<string,unknown>>};
function authorityDependencies(): AuthorityDependencies { return {layer:new Set(),entity_type:new Set(),relation_type:new Set(),entity:new Set(),relation:new Set(),column:new Set(),parameter:new Set(),state:new Map()}; }
function authorityAddDependency(sets: AuthorityDependencies, kind: string, address: string): boolean { const target = kind === "entity_type_column" || kind === "relation_type_column" ? sets.column : sets[kind as keyof AuthorityDependencies]; if (!(target instanceof Set)) return false; target.add(address); return true; }
function authorityQueryPredicate(raw: unknown, context: string, query: string, parameters: ReadonlyMap<string,string>, sets: AuthorityDependencies): boolean { if (!isObject(raw)) return false; const kind = raw["kind"]; if (kind === "all" || kind === "any") return isJSONArray(raw["children"]) && raw["children"].every((item) => authorityQueryPredicate(item, context, query, parameters, sets)); if (kind === "not") return authorityQueryPredicate(raw["child"], context, query, parameters, sets); if (kind === "rows") { if (!isJSONArray(raw["type_addresses"])) return false; const expected = context === "entity" ? "entity_type" : "relation_type", row = context === "entity" ? "entity_row" : "relation_row"; for (const item of raw["type_addresses"]) { if (typeof item !== "string" || authoritySubject(item)?.kind !== expected) return false; authorityAddDependency(sets, expected, item); } return authorityQueryPredicate(raw["predicate"], row, query, parameters, sets); } if (kind === "field") { const expected = authorityContextOperand(String(raw["field"]), context), operand = authorityOperand(raw["operand_type"]); if (expected === undefined || operand === undefined || !authorityOperandEqual(expected, operand)) return false; } else if (kind === "cell") { if (context !== "entity_row" && context !== "relation_row" || !isJSONArray(raw["column_addresses"]) || raw["column_addresses"].length === 0) return false; const expected = context === "entity_row" ? "entity_type_column" : "relation_type_column"; for (const item of raw["column_addresses"]) { if (typeof item !== "string" || authoritySubject(item)?.kind !== expected) return false; sets.column.add(item); } } else if (kind === "state") { const field = String(raw["field_path"]), operand = authorityOperand(raw["operand_type"]); if (operand === undefined || !authorityOperandEqual(operand,{kind:"scalar",scalar:authorityStateType(field),address:""})) return false; sets.state.set(context+"\0"+field,{subject_kind:context,field_path:field,value_type:authorityStateType(field)}); } else return false; const operand = authorityOperand(raw["operand_type"])!; if (isObject(raw["value"])) { const value = raw["value"]; if (value["kind"] === "parameter") { const address = value["parameter_address"], expected = operand.kind === "string_set" ? "string" : operand.scalar; if (typeof address !== "string" || authoritySubject(address)?.kind !== "query_parameter" || authoritySubject(address)?.owner !== query || parameters.get(address) !== expected) return false; sets.parameter.add(address); } for (const address of [...(typeof value["address_value"] === "string" ? [value["address_value"]] : []), ...(isJSONArray(value["address_values"]) ? value["address_values"] : [])]) { if (typeof address !== "string") return false; const subject = authoritySubject(address); if (subject === undefined || !authorityAddDependency(sets, subject.kind, address)) return false; } } return true; }
function authoritySetEquals(raw: unknown, expected: ReadonlySet<string>): boolean { return isJSONArray(raw) && raw.length === expected.size && raw.every((item) => typeof item === "string" && expected.has(item)); }
function hasValidQueryRecipe(value: Record<string, unknown>): boolean { const query = value["address"]; if (typeof query !== "string" || !isJSONArray(value["parameters"])) return false; const parameters = new Map<string,string>(); for (const raw of value["parameters"]) { if (!isObject(raw) || typeof raw["address"] !== "string" || typeof raw["value_type"] !== "string") return false; parameters.set(raw["address"],raw["value_type"]); } const sets = authorityDependencies(); const select = value["select"]; if (!isObject(select)) return false; for (const [property,kind] of [["layer_addresses","layer"],["entity_type_addresses","entity_type"],["relation_type_addresses","relation_type"],["root_addresses","entity"]] as const) if (isJSONArray(select[property])) for (const address of select[property]) { if (typeof address !== "string") return false; authorityAddDependency(sets,kind,address); } if (isObject(value["traverse"]) && isJSONArray(value["traverse"]["relation_type_addresses"])) for (const address of value["traverse"]["relation_type_addresses"]) { if (typeof address !== "string") return false; sets.relation_type.add(address); } if (!authorityQueryPredicate(value["where"],"entity",query,parameters,sets) || !authorityQueryPredicate(value["relation_where"],"relation",query,parameters,sets)) return false; const hasState = sets.state.size !== 0; if (hasState !== (value["state_input"] === "optional" || value["state_input"] === "required")) return false; const dependencies = value["dependencies"]; if (!isObject(dependencies)) return false; for (const property of ["layer","entity_type","relation_type","entity","relation","column","parameter"] as const) if (!authoritySetEquals(dependencies[property+"_addresses"],sets[property])) return false; if (!isJSONArray(dependencies["state_reads"]) || dependencies["state_reads"].length !== sets.state.size) return false; return dependencies["state_reads"].every((raw) => isObject(raw) && sets.state.has(String(raw["subject_kind"])+"\0"+String(raw["field_path"]))); }

function authorityFidelityRank(value: unknown): number { return new Map<unknown,number>([["lossy",0],["visual_only",1],["traceable_summary",2],["lossless",3]]).get(value) ?? -1; }
function hasValidExportRecipe(value: Record<string, unknown>): boolean { const format = value["format"], options = value["options"], profile = value["exporter_profile"], extension = value["extension"], filename = value["filename"]; if (typeof format !== "string" || !isObject(options) || !isObject(profile) || options["kind"] !== format || profile["format"] !== format || typeof extension !== "string" || typeof filename !== "string") return false; const expected = new Map<string,string>([["json",".json"],["yaml",".yaml"],["svg",".svg"],["png",".png"],["pdf",".pdf"],["html",".html"],["csv",".csv"],["tsv",".tsv"],["xlsx",".xlsx"],["markdown",".md"],["pptx",".pptx"],["docx",".docx"],["mermaid",".mmd"],["bpmn",".bpmn"],["drawio",".drawio"]]).get(format); if (expected === undefined || extension !== expected || filename === "" || filename === "." || filename === ".." || /[\\/\u0000]/.test(filename) || !filename.endsWith(extension) || filename.slice(0,-extension.length).length === 0) return false; const fixed = new Map<string,string>([["json","lossless"],["yaml","lossless"],["svg","visual_only"],["png","visual_only"],["pdf","visual_only"],["html","traceable_summary"],["xlsx","traceable_summary"],["pptx","visual_only"],["docx","visual_only"],["drawio","visual_only"],["bpmn","lossy"]]).get(format); if (fixed !== undefined && value["native_maximum_fidelity"] !== fixed) return false; if ((format === "csv" || format === "tsv") && value["native_maximum_fidelity"] !== (options["bundle"] === true && options["header"] === true && options["source_manifest"] === true ? "traceable_summary" : "lossy")) return false; const embedded = format === "xlsx" && options["view_data_json"] === true && options["hidden_ids"] === true; if (embedded ? value["fidelity_basis"] !== "embedded_viewdata" || value["effective_maximum_fidelity"] !== "lossless" : value["fidelity_basis"] !== "native" || value["effective_maximum_fidelity"] !== value["native_maximum_fidelity"]) return false; if (authorityFidelityRank(value["fidelity"]) > authorityFidelityRank(value["effective_maximum_fidelity"])) return false; if ((["lossless","traceable_summary"].includes(String(value["fidelity"])) || format === "json" || format === "yaml") && value["source_refs"] !== true) return false; const embeddedManifest = format === "json" || format === "yaml" || format === "xlsx" && options["view_data_json"] === true; const explicitManifest = ["csv","tsv","markdown","mermaid","bpmn","drawio"].includes(format) && options["source_manifest"] === true; return !(explicitManifest || value["source_refs"] === true && !embeddedManifest) || value["requires_source_manifest"] === true; }

function authorityTableValue(value: Record<string, unknown>, kind: string, scalar = "", enumValues?: ReadonlyArray<string>): boolean { if (!isObject(value["value_type"])) return false; const type = value["value_type"]; if (type["kind"] !== kind || kind === "scalar" && type["scalar_type"] !== scalar) return false; if (enumValues !== undefined && (!isJSONArray(type["enum_values"]) || type["enum_values"].length !== enumValues.length || !type["enum_values"].every((item,index) => item === enumValues[index]))) return false; return true; }
function authorityStateEnum(field: string): ReadonlyArray<string> | undefined { if (["system.created_by.kind","system.updated_by.kind","provenance.verified_by.kind"].includes(field)) return ["user","agent","service_account","anonymous"]; return field === "provenance.source.kind" ? ["manual","import","api","agent","external_system"] : undefined; }
function authorityManifest(value: Record<string, unknown>, state: string, embedded: boolean): boolean { const options = value["options"]; if (!isObject(options)) return false; const explicit = (["csv","tsv"].includes(String(options["kind"])) || ["markdown","mermaid","bpmn","drawio"].includes(String(options["kind"]))) && options["source_manifest"] === true; return value["requires_source_manifest"] === (explicit || state !== "none" || value["source_refs"] === true && !embedded); }
function authorityExportInView(value: Record<string, unknown>, category: string, shape: string, state: string, diff: boolean): boolean { const format = String(value["format"]), options = value["options"]; if (!isObject(options)) return false; if (format === "json" || format === "yaml") return value["native_maximum_fidelity"] === "lossless" && value["effective_maximum_fidelity"] === "lossless" && value["fidelity_basis"] === "native" && authorityManifest(value,state,true) && !(diff && options["state_summary"] === true); const matrix: Record<string,Record<string,string>> = {diagram:{xlsx:"traceable_summary",html:"traceable_summary",csv:"traceable_summary",tsv:"traceable_summary",svg:"visual_only",png:"visual_only",pdf:"visual_only",pptx:"visual_only",docx:"visual_only",drawio:"visual_only",mermaid:"lossy"},table:{xlsx:"traceable_summary",csv:"traceable_summary",tsv:"traceable_summary",html:"traceable_summary",pdf:"visual_only",pptx:"visual_only",docx:"visual_only",markdown:"lossy"},matrix:{xlsx:"traceable_summary",csv:"traceable_summary",tsv:"traceable_summary",html:"traceable_summary",svg:"visual_only",png:"visual_only",pdf:"visual_only",pptx:"visual_only",docx:"visual_only"},tree:{xlsx:"traceable_summary",csv:"traceable_summary",tsv:"traceable_summary",html:"traceable_summary",mermaid:"traceable_summary",svg:"visual_only",png:"visual_only",pdf:"visual_only",pptx:"visual_only",docx:"visual_only",drawio:"visual_only"},flow:{xlsx:"traceable_summary",csv:"traceable_summary",tsv:"traceable_summary",html:"traceable_summary",mermaid:"traceable_summary",bpmn:"lossy",svg:"visual_only",png:"visual_only",pdf:"visual_only",pptx:"visual_only",docx:"visual_only",drawio:"visual_only",markdown:"lossy"},context:{csv:"traceable_summary",tsv:"traceable_summary",xlsx:"traceable_summary",html:"traceable_summary",markdown:"traceable_summary",pdf:"visual_only",pptx:"visual_only",docx:"visual_only"},diff:{csv:"traceable_summary",tsv:"traceable_summary",xlsx:"traceable_summary",html:"traceable_summary",markdown:"traceable_summary",pdf:"visual_only",pptx:"visual_only",docx:"visual_only"}}; let native = matrix[shape]?.[format]; if (native === undefined) return false; if ((format === "csv" || format === "tsv") && !(options["bundle"] === true && options["header"] === true && options["source_manifest"] === true) || (shape === "tree" || shape === "flow") && format === "mermaid" && options["source_manifest"] !== true) native = "lossy"; if (value["native_maximum_fidelity"] !== native) return false; const fidelityEmbedded = format === "xlsx" && options["view_data_json"] === true && options["hidden_ids"] === true; if (fidelityEmbedded ? value["effective_maximum_fidelity"] !== "lossless" || value["fidelity_basis"] !== "embedded_viewdata" : value["effective_maximum_fidelity"] !== native || value["fidelity_basis"] !== "native") return false; if (format === "xlsx") { const profile = options["profile"]; const compatible = profile === "type_workbook" && shape === "table" || ["diagram_workbook","composed_diagram_workbook","diagram_inventory_workbook"].includes(String(profile)) && shape === "diagram" || profile === "matrix_workbook" && shape === "matrix" || profile === "tree_workbook" && shape === "tree" || profile === "flow_workbook" && shape === "flow" || profile === "diff_workbook" && shape === "diff" || profile === "context_workbook" && shape === "context" || profile === "impact_workbook" && category === "impact" && ["diagram","table","matrix"].includes(shape); if (!compatible) return false; } return authorityManifest(value,state,format === "xlsx" && options["view_data_json"] === true); }
type AuthorityViewDependencies = {query:Set<string>;parameter:Set<string>;layer:Set<string>;entity_type:Set<string>;relation_type:Set<string>;entity:Set<string>;relation:Set<string>;column:Set<string>};
function authorityViewDependencies(): AuthorityViewDependencies { return {query:new Set(),parameter:new Set(),layer:new Set(),entity_type:new Set(),relation_type:new Set(),entity:new Set(),relation:new Set(),column:new Set()}; }
function authorityAddViewDependencyValues(raw: unknown, sets: AuthorityViewDependencies): void { const values = typeof raw === "string" ? [raw] : isJSONArray(raw) ? raw : []; for (const item of values) { if (typeof item !== "string") continue; const subject=authoritySubject(item); if (subject === undefined) continue; const property=subject.kind === "query_parameter" ? "parameter" : subject.kind === "entity_type_column" || subject.kind === "relation_type_column" ? "column" : subject.kind; const target=sets[property as keyof AuthorityViewDependencies]; if (target instanceof Set) target.add(item); } }
function authorityCollectViewDependencies(raw: unknown, sets: AuthorityViewDependencies): void { if (isJSONArray(raw)) { for (const item of raw) authorityCollectViewDependencies(item,sets); return; } if (!isObject(raw)) return; for (const [property,item] of Object.entries(raw)) { if (property === "arguments") { if (isObject(item)) for (const address of Object.keys(item)) authorityAddViewDependencyValues(address,sets); continue; } if (["query_address","entity_address","relation_address","layer_address","parameter_address","branch_value_column_address","layer_addresses","entity_type_addresses","relation_type_addresses","entity_addresses","relation_addresses","column_addresses","lane_column_addresses","attribute_column_addresses"].includes(property)) { authorityAddViewDependencyValues(item,sets); continue; } authorityCollectViewDependencies(item,sets); } }
function authorityContainsViewDependencies(raw: unknown, expected: ReadonlySet<string>): boolean { return isJSONArray(raw) && [...expected].every((value) => raw.includes(value)); }
function authorityValidViewDependencies(value: Record<string,unknown>): boolean { const dependencies=value["dependencies"], source=value["source"], shape=value["shape"], overrides=value["relation_projection_overrides"], exports=value["exports"]; if (!isObject(dependencies) || !isObject(source) || !isObject(shape) || !isObject(overrides) || !isJSONArray(exports)) return false; const sets=authorityViewDependencies(); authorityCollectViewDependencies(source,sets); authorityCollectViewDependencies(shape,sets); for (const [address,override] of Object.entries(overrides)) { authorityAddViewDependencyValues(address,sets); authorityCollectViewDependencies(override,sets); } if (!authoritySetEquals(dependencies["query_addresses"],sets.query)) return false; const hasSourceQuery=typeof source["query_address"] === "string"; for (const property of ["parameter","layer","entity_type","relation_type","entity","relation","column"] as const) if (hasSourceQuery ? !authorityContainsViewDependencies(dependencies[property+"_addresses"],sets[property]) : !authoritySetEquals(dependencies[property+"_addresses"],sets[property])) return false; const addresses=dependencies["export_addresses"]; return isJSONArray(addresses) && addresses.length === exports.length && exports.every((raw,index) => isObject(raw) && raw["address"] === addresses[index]); }
function hasValidViewRecipe(value: Record<string, unknown>): boolean { const address = value["address"], shapeValue = value["shape"], source = value["source"], reservedRaw = value["reserved_table_column_ids"]; if (typeof address !== "string" || !isObject(shapeValue) || !isObject(source) || !isJSONArray(reservedRaw)) return false; const category = String(value["category"]), sourceKind = String(source["kind"]), shape = String(shapeValue["kind"]); const diffCount = Number(category === "diff") + Number(sourceKind === "diff") + Number(shape === "diff"); if (diffCount !== 0 && diffCount !== 3) return false; const stateInput = String(value["state_input"]), stateRequirement = String(value["state_requirement"]), rank = (item: string): number => ["none","optional","required"].indexOf(item); if (rank(stateRequirement) < rank(stateInput) || diffCount === 3 && stateRequirement !== "none") return false; const direct: Array<Record<string,unknown>> = []; if (shape === "table") { const table = shapeValue["table"]; if (!isObject(table) || !isJSONArray(table["columns"]) || !isJSONArray(table["sorts"])) return false; const row = String(table["row_source"]), entity = row === "entity" || row === "entity_rows"; if (!entity && (table["include_entity_id"] === true || table["include_type"] === true || table["include_layer"] === true || hasOwn(table,"entity_type_addresses"))) return false; const available = new Set<string>(), reserved = new Set(reservedRaw); if (table["include_entity_id"] === true) available.add("entity_id"); if (table["include_type"] === true) available.add("entity_type"); if (table["include_layer"] === true) available.add("entity_layer"); const automatic = table["automatic_relation_columns"]; if (!isJSONArray(automatic) || row !== "automatic_relations" && automatic.length !== 0 || !automatic.every((item) => typeof item === "string")) return false; for (const item of automatic) available.add(item); for (const raw of table["columns"]) { if (!isObject(raw) || typeof raw["id"] !== "string" || typeof raw["address"] !== "string" || !hasDirectStableAddressOwner(address,raw["address"]) || reserved.has(raw["id"]) || available.has(raw["id"]) || !isObject(raw["source"])) return false; available.add(raw["id"]); const column = raw["source"], kind = column["kind"], aggregate = raw["aggregate"]; if (kind === "attribute") { if (row !== "entity_rows" && row !== "relation_rows" || !isJSONArray(column["column_addresses"]) || column["column_addresses"].length === 0) return false; const expected = row === "entity_rows" ? "entity_type_column" : "relation_type_column"; if (!column["column_addresses"].every((item) => typeof item === "string" && authoritySubject(item)?.kind === expected)) return false; } else if (kind === "relation_endpoint") { if (row !== "relation" && row !== "relation_rows") return false; const field = String(column["field"]); if (!authorityTableValue(raw,field === "id" || field === "display_name" ? "scalar" : "stable_address",field === "id" || field === "display_name" ? "string" : "")) return false; } else if (kind === "derived_count") { if (!entity || !authorityTableValue(raw,"scalar","integer")) return false; } else if (kind === "field") { const field = String(column["field"]); if (["id","display_name","description"].includes(field) ? !authorityTableValue(raw,"scalar","string") : field === "tags" ? !authorityTableValue(raw,"string_set") : !authorityTableValue(raw,"stable_address")) return false; } else if (kind === "state") { const field = String(column["field_path"]), type = authorityStateType(field), enums = authorityStateEnum(field); if (!authorityTableValue(raw,"scalar",type,enums)) return false; const subjects = row === "automatic_relations" ? ["relation","relation_row"] : [row === "entity" ? "entity" : row === "entity_rows" ? "entity_row" : row === "relation" ? "relation" : "relation_row"]; for (const subject_kind of subjects) direct.push({subject_kind,field_path:field,value_type:type}); } else return false; if ((aggregate === "count" || aggregate === "count_distinct") && !authorityTableValue(raw,"scalar","integer") || aggregate === "join_unique" && !authorityTableValue(raw,"scalar","string") || (aggregate === "min" || aggregate === "max") && (!isObject(raw["value_type"]) || raw["value_type"]["kind"] !== "scalar" || !["integer","number","date","datetime","enum"].includes(String(raw["value_type"]["scalar_type"])))) return false; } if (!table["sorts"].every((item) => isObject(item) && typeof item["column_id"] === "string" && available.has(item["column_id"]))) return false; } else if (stateInput !== "none") return false; if ((direct.length !== 0) !== (stateInput === "optional" || stateInput === "required")) return false; const dependencies = value["dependencies"]; if (!isObject(dependencies)) return false; const stateReads = dependencies["state_reads"]; if (!isJSONArray(stateReads) || direct.some((read) => !stateReads.some((item: unknown) => isObject(item) && item["subject_kind"] === read["subject_kind"] && item["field_path"] === read["field_path"] && item["value_type"] === read["value_type"]))) return false; if (typeof source["query_address"] !== "string" && stateReads.length !== direct.length) return false; if (!isJSONArray(value["exports"]) || !authorityValidViewDependencies(value)) return false; return value["exports"].every((raw) => isObject(raw) && raw["view_address"] === address && authorityExportInView(raw,category,shape,stateRequirement,diffCount === 3)); }

function hasValidDiffSource(value: Record<string, unknown>): boolean { if (value["kind"] !== "diff") return true; const before = value["before"]; const after = value["after"]; if (typeof before !== "string" || typeof after !== "string" || before.length === 0 || after.length === 0 || before === after) return false; return hasOwn(value, "query_address") || (isObject(value["arguments"]) && Object.keys(value["arguments"]).length === 0); }

function isRFC3339(value: string): boolean {
  const match = /^([0-9]{4})-(0[1-9]|1[0-2])-(0[1-9]|[12][0-9]|3[01])T([01][0-9]|2[0-3]):[0-5][0-9]:[0-5][0-9](?:\.[0-9]{1,9})?Z$/.exec(value);
  if (match === null) return false; const year = Number(match[1]); const month = Number(match[2]); const day = Number(match[3]); const leap = year % 4 === 0 && (year % 100 !== 0 || year % 400 === 0); const days = [31, leap ? 29 : 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31]; return day >= 1 && day <= days[month - 1]!;
}

function utf8ByteLength(value: string): number {
  let bytes = 0;
  for (let index = 0; index < value.length; index++) {
    const code = value.charCodeAt(index);
    if (code <= 0x7f) bytes++;
    else if (code <= 0x7ff) bytes += 2;
    else if (code >= 0xd800 && code <= 0xdbff) { const low = value.charCodeAt(index + 1); if (!(low >= 0xdc00 && low <= 0xdfff)) throw new TypeError("protocol JSON contains an unpaired high surrogate"); bytes += 4; index++; }
    else if (code >= 0xdc00 && code <= 0xdfff) throw new TypeError("protocol JSON contains an unpaired low surrogate");
    else bytes += 3;
  }
  return bytes;
}

function validateProgrammaticWireValue(value: unknown, active: Set<object> = new Set<object>(), depth = 0): void {
  if (value === null || typeof value === "boolean") return;
  if (typeof value === "string") { if (!hasScalarUnicode(value)) throw new TypeError("protocol value contains malformed Unicode"); return; }
  if (typeof value === "number") { if (!Number.isSafeInteger(value) || Object.is(value, -0)) throw new TypeError("protocol numbers must be canonical safe integers"); return; }
  const array = isJSONArray(value); if (!array && !isObject(value)) throw new TypeError("unsupported protocol JSON value");
  if (active.has(value)) throw new TypeError("protocol value contains a cycle");
  if (depth >= maxWireJSONDepth) throw new TypeError("protocol value exceeds depth " + maxWireJSONDepth);
  active.add(value);
  try {
    if (array) { for (const item of value) validateProgrammaticWireValue(item, active, depth + 1); }
    else { for (const item of Object.values(value)) validateProgrammaticWireValue(item, active, depth + 1); }
  } finally { active.delete(value); }
}

function isProgrammaticWireValue(value: unknown, predicate: () => boolean): boolean {
  try { validateProgrammaticWireValue(value); return predicate(); } catch { return false; }
}

function validateWireJSONText(input: string): void {
  if (utf8ByteLength(input) > maxWireJSONBytes) throw new TypeError("protocol JSON exceeds " + maxWireJSONBytes + " UTF-8 bytes");
  let depth = 0;
  for (let index = 0; index < input.length; index++) {
    const character = input[index]!;
    if (character === '"') { index = scanJSONString(input, index); continue; }
    if (character === "{" || character === "[") { depth++; if (depth > maxWireJSONDepth) throw new TypeError("protocol JSON exceeds depth " + maxWireJSONDepth); continue; }
    if (character === "}" || character === "]") { depth--; continue; }
    if (character === "-" || (character >= "0" && character <= "9")) { const end = scanJSONToken(input, index); validateCanonicalJSONNumber(input.slice(index, end)); index = end - 1; }
  }
	const end = scanUniqueJSONValue(input, skipJSONWhitespace(input, 0));
	if (skipJSONWhitespace(input, end) !== input.length) throw new TypeError("protocol JSON must contain exactly one value");
}

function skipJSONWhitespace(input: string, start: number): number {
  let index = start; while (index < input.length && /[ \t\r\n]/.test(input[index]!)) index++; return index;
}

function scanUniqueJSONValue(input: string, start: number): number {
  let index = skipJSONWhitespace(input, start);
  const character = input[index];
  if (character === '"') return scanJSONString(input, index) + 1;
  if (character === "[") {
    index = skipJSONWhitespace(input, index + 1); if (input[index] === "]") return index + 1;
    for (;;) { index = skipJSONWhitespace(input, scanUniqueJSONValue(input, index)); if (input[index] === "]") return index + 1; if (input[index] !== ",") throw new TypeError("protocol JSON array is malformed"); index++; }
  }
  if (character === "{") {
    const keys = new Set<string>(); index = skipJSONWhitespace(input, index + 1); if (input[index] === "}") return index + 1;
    for (;;) {
      if (input[index] !== '"') throw new TypeError("protocol JSON object key must be a string");
      const keyEnd = scanJSONString(input, index); const key: unknown = JSON.parse(input.slice(index, keyEnd + 1));
      if (typeof key !== "string") throw new TypeError("protocol JSON object key must be a string");
      if (keys.has(key)) throw new TypeError("protocol JSON contains duplicate object key " + key); keys.add(key);
      index = skipJSONWhitespace(input, keyEnd + 1); if (input[index] !== ":") throw new TypeError("protocol JSON object is missing a colon");
      index = skipJSONWhitespace(input, scanUniqueJSONValue(input, index + 1)); if (input[index] === "}") return index + 1; if (input[index] !== ",") throw new TypeError("protocol JSON object is malformed"); index = skipJSONWhitespace(input, index + 1);
    }
  }
  const end = scanJSONToken(input, index); if (end === index) throw new TypeError("protocol JSON value is malformed"); return end;
}

function scanJSONToken(input: string, start: number): number {
  let index = start;
  while (index < input.length && !/[{}\[\],:\s]/.test(input[index]!)) index++;
  return index;
}

function validateCanonicalJSONNumber(value: string): void {
  if (!/^(0|-[1-9][0-9]*|[1-9][0-9]*)$/.test(value)) throw new TypeError("protocol JSON number " + value + " is not a canonical integer");
  const parsed = BigInt(value);
  if (parsed < -9007199254740991n || parsed > 9007199254740991n) throw new TypeError("protocol JSON number " + value + " is outside the portable safe range");
}

function scanJSONString(input: string, start: number): number {
  for (let index = start + 1; index < input.length; index++) {
    const code = input.charCodeAt(index);
    if (code === 0x22) return index;
    if (code < 0x20) throw new TypeError("protocol JSON string contains an unescaped control character");
    if (code !== 0x5c) continue;
    index++;
    if (index >= input.length) throw new TypeError("protocol JSON string has a truncated escape");
    if (input[index] !== "u") continue;
    const unit = parseHexCodeUnit(input, index + 1); index += 4;
    if (unit >= 0xdc00 && unit <= 0xdfff) throw new TypeError("protocol JSON string has an unpaired low surrogate");
    if (unit < 0xd800 || unit > 0xdbff) continue;
    if (input[index + 1] !== "\\" || input[index + 2] !== "u") throw new TypeError("protocol JSON string has an unpaired high surrogate");
    const low = parseHexCodeUnit(input, index + 3);
    if (low < 0xdc00 || low > 0xdfff) throw new TypeError("protocol JSON string has an invalid surrogate pair");
    index += 6;
  }
  throw new TypeError("protocol JSON string is unterminated");
}

function parseHexCodeUnit(input: string, start: number): number {
  const text = input.slice(start, start + 4);
  if (!/^[0-9a-fA-F]{4}$/.test(text)) throw new TypeError("protocol JSON string has an invalid Unicode escape");
  return Number.parseInt(text, 16);
}

function canonicalJSONStringify(value: unknown): string {
  if (value === null || typeof value === "boolean") return JSON.stringify(value);
  if (typeof value === "string") { if (!hasScalarUnicode(value)) throw new TypeError("protocol strings must contain Unicode scalar values"); return JSON.stringify(value).replace(/[\u2028\u2029]/g, (character) => character === "\u2028" ? "\\u2028" : "\\u2029"); }
  if (typeof value === "number") { if (!Number.isSafeInteger(value) || Object.is(value, -0)) throw new TypeError("protocol numbers must be canonical safe integers"); return String(value); }
  if (isJSONArray(value)) return `[${value.map(canonicalJSONStringify).join(",")}]`;
  if (isObject(value)) return `{${Object.keys(value).sort().map((key) => `${canonicalJSONStringify(key)}:${canonicalJSONStringify(value[key])}`).join(",")}}`;
  throw new TypeError("unsupported protocol JSON value");
}

/** A preview/commit equality precondition, not a grant token. */
export interface AuthoringProof {
  access_fingerprint: Digest;
  base_revision: CommittedRevisionRef;
  decision_digest: Digest;
  evaluation_digest: Digest;
  expires_at?: Rfc3339Time;
  membership_version: CanonicalUint64;
  policy_refs: ReadonlyArray<PolicyRef>;
}

export function isAuthoringProof(value: unknown): value is AuthoringProof {
  return isProgrammaticWireValue(value, () => isObject(value) && hasOnlyKeys(value, new Set(["access_fingerprint", "base_revision", "decision_digest", "evaluation_digest", "expires_at", "membership_version", "policy_refs"])) && hasOwn(value, "access_fingerprint") && (isDigest(value["access_fingerprint"])) && hasOwn(value, "base_revision") && (isCommittedRevisionRef(value["base_revision"])) && hasOwn(value, "decision_digest") && (isDigest(value["decision_digest"])) && hasOwn(value, "evaluation_digest") && (isDigest(value["evaluation_digest"])) && (!hasOwn(value, "expires_at") || (isRfc3339Time(value["expires_at"]))) && hasOwn(value, "membership_version") && (isCanonicalUint64(value["membership_version"])) && hasOwn(value, "policy_refs") && (isJSONArray(value["policy_refs"]) && value["policy_refs"].every((item) => isPolicyRef(item)) && value["policy_refs"].length <= 64));
}

export function decodeAuthoringProof(input: string): AuthoringProof {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isAuthoringProof(value)) throw new TypeError("invalid AuthoringProof");
  return value;
}

export function encodeAuthoringProof(value: AuthoringProof): string {
  validateProgrammaticWireValue(value);
  if (!isAuthoringProof(value)) throw new TypeError("invalid AuthoringProof");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isAuthoringProof(emitted)) throw new TypeError("encoded value is invalid AuthoringProof");
  return encoded;
}

export interface CancelOperationInput {
  cancellation_token: CancellationToken;
  operation_id: OperationID;
  session: RuntimeSessionRef;
}

export function isCancelOperationInput(value: unknown): value is CancelOperationInput {
  return isProgrammaticWireValue(value, () => isObject(value) && hasOnlyKeys(value, new Set(["cancellation_token", "operation_id", "session"])) && hasOwn(value, "cancellation_token") && (isCancellationToken(value["cancellation_token"])) && hasOwn(value, "operation_id") && (isOperationID(value["operation_id"])) && hasOwn(value, "session") && (isRuntimeSessionRef(value["session"])));
}

export function decodeCancelOperationInput(input: string): CancelOperationInput {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isCancelOperationInput(value)) throw new TypeError("invalid CancelOperationInput");
  return value;
}

export function encodeCancelOperationInput(value: CancelOperationInput): string {
  validateProgrammaticWireValue(value);
  if (!isCancelOperationInput(value)) throw new TypeError("invalid CancelOperationInput");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isCancelOperationInput(emitted)) throw new TypeError("encoded value is invalid CancelOperationInput");
  return encoded;
}

export interface CancelOperationRequestEnvelope {
  deadline_at?: Rfc3339Time;
  operation: "runtime.cancel_operation";
  payload: CancelOperationInput;
  protocol: RuntimeProtocolRef;
  request_id: string;
}

export function isCancelOperationRequestEnvelope(value: unknown): value is CancelOperationRequestEnvelope {
  return isProgrammaticWireValue(value, () => isObject(value) && hasOnlyKeys(value, new Set(["deadline_at", "operation", "payload", "protocol", "request_id"])) && (!hasOwn(value, "deadline_at") || (isRfc3339Time(value["deadline_at"]))) && hasOwn(value, "operation") && (typeof value["operation"] === "string" && hasScalarUnicode(value["operation"]) && value["operation"] === "runtime.cancel_operation") && hasOwn(value, "payload") && (isCancelOperationInput(value["payload"])) && hasOwn(value, "protocol") && (isRuntimeProtocolRef(value["protocol"])) && hasOwn(value, "request_id") && (typeof value["request_id"] === "string" && hasScalarUnicode(value["request_id"]) && Array.from(value["request_id"]).length >= 1 && Array.from(value["request_id"]).length <= 128));
}

export function decodeCancelOperationRequestEnvelope(input: string): CancelOperationRequestEnvelope {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isCancelOperationRequestEnvelope(value)) throw new TypeError("invalid CancelOperationRequestEnvelope");
  return value;
}

export function encodeCancelOperationRequestEnvelope(value: CancelOperationRequestEnvelope): string {
  validateProgrammaticWireValue(value);
  if (!isCancelOperationRequestEnvelope(value)) throw new TypeError("invalid CancelOperationRequestEnvelope");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isCancelOperationRequestEnvelope(emitted)) throw new TypeError("encoded value is invalid CancelOperationRequestEnvelope");
  return encoded;
}

export interface CancelOperationResponseEnvelope {
  diagnostics: ReadonlyArray<ProtocolDiagnostic>;
  extensions?: Extensions;
  failure?: ProtocolFailure;
  host_release: ReleaseVersion;
  outcome: Outcome;
  payload?: CancelOperationResult;
  protocol: RuntimeProtocolRef;
  request_id: string;
}

export function isCancelOperationResponseEnvelope(value: unknown): value is CancelOperationResponseEnvelope {
  return isProgrammaticWireValue(value, () => isObject(value) && hasOnlyKeys(value, new Set(["diagnostics", "extensions", "failure", "host_release", "outcome", "payload", "protocol", "request_id"])) && hasOwn(value, "diagnostics") && (isJSONArray(value["diagnostics"]) && value["diagnostics"].every((item) => isProtocolDiagnostic(item)) && value["diagnostics"].length <= 256) && (!hasOwn(value, "extensions") || (isExtensions(value["extensions"]))) && (!hasOwn(value, "failure") || (isProtocolFailure(value["failure"]))) && hasOwn(value, "host_release") && (isReleaseVersion(value["host_release"])) && hasOwn(value, "outcome") && (isOutcome(value["outcome"])) && (!hasOwn(value, "payload") || (isCancelOperationResult(value["payload"]))) && hasOwn(value, "protocol") && (isRuntimeProtocolRef(value["protocol"])) && hasOwn(value, "request_id") && (typeof value["request_id"] === "string" && hasScalarUnicode(value["request_id"]) && Array.from(value["request_id"]).length >= 1 && Array.from(value["request_id"]).length <= 128) && hasValidOutcomeEnvelope(value));
}

export function decodeCancelOperationResponseEnvelope(input: string): CancelOperationResponseEnvelope {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isCancelOperationResponseEnvelope(value)) throw new TypeError("invalid CancelOperationResponseEnvelope");
  return value;
}

export function encodeCancelOperationResponseEnvelope(value: CancelOperationResponseEnvelope): string {
  validateProgrammaticWireValue(value);
  if (!isCancelOperationResponseEnvelope(value)) throw new TypeError("invalid CancelOperationResponseEnvelope");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isCancelOperationResponseEnvelope(emitted)) throw new TypeError("encoded value is invalid CancelOperationResponseEnvelope");
  return encoded;
}

export interface CancelOperationResult {
  operation_id: OperationID;
  phase: RecoveryPhase;
  status: "accepted" | "already_final";
}

export function isCancelOperationResult(value: unknown): value is CancelOperationResult {
  return isProgrammaticWireValue(value, () => isObject(value) && hasOnlyKeys(value, new Set(["operation_id", "phase", "status"])) && hasOwn(value, "operation_id") && (isOperationID(value["operation_id"])) && hasOwn(value, "phase") && (isRecoveryPhase(value["phase"])) && hasOwn(value, "status") && (typeof value["status"] === "string" && hasScalarUnicode(value["status"]) && ["accepted", "already_final"].includes(value["status"])));
}

export function decodeCancelOperationResult(input: string): CancelOperationResult {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isCancelOperationResult(value)) throw new TypeError("invalid CancelOperationResult");
  return value;
}

export function encodeCancelOperationResult(value: CancelOperationResult): string {
  validateProgrammaticWireValue(value);
  if (!isCancelOperationResult(value)) throw new TypeError("invalid CancelOperationResult");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isCancelOperationResult(emitted)) throw new TypeError("encoded value is invalid CancelOperationResult");
  return encoded;
}

export type CancellationToken = string;

export function isCancellationToken(value: unknown): value is CancellationToken {
  return isProgrammaticWireValue(value, () => typeof value === "string" && hasScalarUnicode(value) && new RegExp("^[A-Za-z0-9_-]+$").test(value) && Array.from(value).length >= 16 && Array.from(value).length <= 256);
}

export function decodeCancellationToken(input: string): CancellationToken {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isCancellationToken(value)) throw new TypeError("invalid CancellationToken");
  return value;
}

export function encodeCancellationToken(value: CancellationToken): string {
  validateProgrammaticWireValue(value);
  if (!isCancellationToken(value)) throw new TypeError("invalid CancellationToken");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isCancellationToken(emitted)) throw new TypeError("encoded value is invalid CancellationToken");
  return encoded;
}

export interface CommitOperationsRequestEnvelope {
  deadline_at?: Rfc3339Time;
  operation: "runtime.commit_operations";
  payload: RuntimeCommitInput;
  protocol: RuntimeProtocolRef;
  request_id: string;
}

export function isCommitOperationsRequestEnvelope(value: unknown): value is CommitOperationsRequestEnvelope {
  return isProgrammaticWireValue(value, () => isObject(value) && hasOnlyKeys(value, new Set(["deadline_at", "operation", "payload", "protocol", "request_id"])) && (!hasOwn(value, "deadline_at") || (isRfc3339Time(value["deadline_at"]))) && hasOwn(value, "operation") && (typeof value["operation"] === "string" && hasScalarUnicode(value["operation"]) && value["operation"] === "runtime.commit_operations") && hasOwn(value, "payload") && (isRuntimeCommitInput(value["payload"])) && hasOwn(value, "protocol") && (isRuntimeProtocolRef(value["protocol"])) && hasOwn(value, "request_id") && (typeof value["request_id"] === "string" && hasScalarUnicode(value["request_id"]) && Array.from(value["request_id"]).length >= 1 && Array.from(value["request_id"]).length <= 128));
}

export function decodeCommitOperationsRequestEnvelope(input: string): CommitOperationsRequestEnvelope {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isCommitOperationsRequestEnvelope(value)) throw new TypeError("invalid CommitOperationsRequestEnvelope");
  return value;
}

export function encodeCommitOperationsRequestEnvelope(value: CommitOperationsRequestEnvelope): string {
  validateProgrammaticWireValue(value);
  if (!isCommitOperationsRequestEnvelope(value)) throw new TypeError("invalid CommitOperationsRequestEnvelope");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isCommitOperationsRequestEnvelope(emitted)) throw new TypeError("encoded value is invalid CommitOperationsRequestEnvelope");
  return encoded;
}

export interface CommitOperationsResponseEnvelope {
  diagnostics: ReadonlyArray<ProtocolDiagnostic>;
  extensions?: Extensions;
  failure?: ProtocolFailure;
  host_release: ReleaseVersion;
  outcome: Outcome;
  payload?: RuntimeCommitResult;
  protocol: RuntimeProtocolRef;
  request_id: string;
}

export function isCommitOperationsResponseEnvelope(value: unknown): value is CommitOperationsResponseEnvelope {
  return isProgrammaticWireValue(value, () => isObject(value) && hasOnlyKeys(value, new Set(["diagnostics", "extensions", "failure", "host_release", "outcome", "payload", "protocol", "request_id"])) && hasOwn(value, "diagnostics") && (isJSONArray(value["diagnostics"]) && value["diagnostics"].every((item) => isProtocolDiagnostic(item)) && value["diagnostics"].length <= 256) && (!hasOwn(value, "extensions") || (isExtensions(value["extensions"]))) && (!hasOwn(value, "failure") || (isProtocolFailure(value["failure"]))) && hasOwn(value, "host_release") && (isReleaseVersion(value["host_release"])) && hasOwn(value, "outcome") && (isOutcome(value["outcome"])) && (!hasOwn(value, "payload") || (isRuntimeCommitResult(value["payload"]))) && hasOwn(value, "protocol") && (isRuntimeProtocolRef(value["protocol"])) && hasOwn(value, "request_id") && (typeof value["request_id"] === "string" && hasScalarUnicode(value["request_id"]) && Array.from(value["request_id"]).length >= 1 && Array.from(value["request_id"]).length <= 128) && hasValidOutcomeEnvelope(value));
}

export function decodeCommitOperationsResponseEnvelope(input: string): CommitOperationsResponseEnvelope {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isCommitOperationsResponseEnvelope(value)) throw new TypeError("invalid CommitOperationsResponseEnvelope");
  return value;
}

export function encodeCommitOperationsResponseEnvelope(value: CommitOperationsResponseEnvelope): string {
  validateProgrammaticWireValue(value);
  if (!isCommitOperationsResponseEnvelope(value)) throw new TypeError("invalid CommitOperationsResponseEnvelope");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isCommitOperationsResponseEnvelope(emitted)) throw new TypeError("encoded value is invalid CommitOperationsResponseEnvelope");
  return encoded;
}

export type CommitTrigger = "agent_apply" | "autosave" | "explicit_save" | "realtime_checkpoint" | "registry_install" | "restore";

export function isCommitTrigger(value: unknown): value is CommitTrigger {
  return isProgrammaticWireValue(value, () => typeof value === "string" && hasScalarUnicode(value) && ["agent_apply", "autosave", "explicit_save", "realtime_checkpoint", "registry_install", "restore"].includes(value));
}

export function decodeCommitTrigger(input: string): CommitTrigger {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isCommitTrigger(value)) throw new TypeError("invalid CommitTrigger");
  return value;
}

export function encodeCommitTrigger(value: CommitTrigger): string {
  validateProgrammaticWireValue(value);
  if (!isCommitTrigger(value)) throw new TypeError("invalid CommitTrigger");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isCommitTrigger(emitted)) throw new TypeError("encoded value is invalid CommitTrigger");
  return encoded;
}

export interface CommittedRevisionRef {
  definition_hash: Digest;
  document_id: DocumentID;
  provider_version?: ProviderVersionToken;
  revision_id: RevisionID;
}

export function isCommittedRevisionRef(value: unknown): value is CommittedRevisionRef {
  return isProgrammaticWireValue(value, () => isObject(value) && hasOnlyKeys(value, new Set(["definition_hash", "document_id", "provider_version", "revision_id"])) && hasOwn(value, "definition_hash") && (isDigest(value["definition_hash"])) && hasOwn(value, "document_id") && (isDocumentID(value["document_id"])) && (!hasOwn(value, "provider_version") || (isProviderVersionToken(value["provider_version"]))) && hasOwn(value, "revision_id") && (isRevisionID(value["revision_id"])));
}

export function decodeCommittedRevisionRef(input: string): CommittedRevisionRef {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isCommittedRevisionRef(value)) throw new TypeError("invalid CommittedRevisionRef");
  return value;
}

export function encodeCommittedRevisionRef(value: CommittedRevisionRef): string {
  validateProgrammaticWireValue(value);
  if (!isCommittedRevisionRef(value)) throw new TypeError("invalid CommittedRevisionRef");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isCommittedRevisionRef(emitted)) throw new TypeError("encoded value is invalid CommittedRevisionRef");
  return encoded;
}

export type DocumentID = string;

export function isDocumentID(value: unknown): value is DocumentID {
  return isProgrammaticWireValue(value, () => typeof value === "string" && hasScalarUnicode(value) && new RegExp("^[A-Za-z0-9][A-Za-z0-9._:-]*$").test(value) && Array.from(value).length >= 1 && Array.from(value).length <= 256);
}

export function decodeDocumentID(input: string): DocumentID {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isDocumentID(value)) throw new TypeError("invalid DocumentID");
  return value;
}

export function encodeDocumentID(value: DocumentID): string {
  validateProgrammaticWireValue(value);
  if (!isDocumentID(value)) throw new TypeError("invalid DocumentID");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isDocumentID(emitted)) throw new TypeError("encoded value is invalid DocumentID");
  return encoded;
}

export interface GetOperationResultInput {
  idempotency_key?: IdempotencyKey;
  lookup_by: "idempotency_key" | "operation_id";
  operation_id?: OperationID;
  session: RuntimeSessionRef;
}

export function isGetOperationResultInput(value: unknown): value is GetOperationResultInput {
  return isProgrammaticWireValue(value, () => isObject(value) && hasOnlyKeys(value, new Set(["idempotency_key", "lookup_by", "operation_id", "session"])) && (!hasOwn(value, "idempotency_key") || (isIdempotencyKey(value["idempotency_key"]))) && hasOwn(value, "lookup_by") && (typeof value["lookup_by"] === "string" && hasScalarUnicode(value["lookup_by"]) && ["idempotency_key", "operation_id"].includes(value["lookup_by"])) && (!hasOwn(value, "operation_id") || (isOperationID(value["operation_id"]))) && hasOwn(value, "session") && (isRuntimeSessionRef(value["session"])) && ((value["lookup_by"] === "idempotency_key" && hasOwn(value, "idempotency_key") && !hasOwn(value, "operation_id")) || (value["lookup_by"] === "operation_id" && hasOwn(value, "operation_id") && !hasOwn(value, "idempotency_key"))));
}

export function decodeGetOperationResultInput(input: string): GetOperationResultInput {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isGetOperationResultInput(value)) throw new TypeError("invalid GetOperationResultInput");
  return value;
}

export function encodeGetOperationResultInput(value: GetOperationResultInput): string {
  validateProgrammaticWireValue(value);
  if (!isGetOperationResultInput(value)) throw new TypeError("invalid GetOperationResultInput");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isGetOperationResultInput(emitted)) throw new TypeError("encoded value is invalid GetOperationResultInput");
  return encoded;
}

export interface GetOperationResultRequestEnvelope {
  deadline_at?: Rfc3339Time;
  operation: "runtime.get_operation_result";
  payload: GetOperationResultInput;
  protocol: RuntimeProtocolRef;
  request_id: string;
}

export function isGetOperationResultRequestEnvelope(value: unknown): value is GetOperationResultRequestEnvelope {
  return isProgrammaticWireValue(value, () => isObject(value) && hasOnlyKeys(value, new Set(["deadline_at", "operation", "payload", "protocol", "request_id"])) && (!hasOwn(value, "deadline_at") || (isRfc3339Time(value["deadline_at"]))) && hasOwn(value, "operation") && (typeof value["operation"] === "string" && hasScalarUnicode(value["operation"]) && value["operation"] === "runtime.get_operation_result") && hasOwn(value, "payload") && (isGetOperationResultInput(value["payload"])) && hasOwn(value, "protocol") && (isRuntimeProtocolRef(value["protocol"])) && hasOwn(value, "request_id") && (typeof value["request_id"] === "string" && hasScalarUnicode(value["request_id"]) && Array.from(value["request_id"]).length >= 1 && Array.from(value["request_id"]).length <= 128));
}

export function decodeGetOperationResultRequestEnvelope(input: string): GetOperationResultRequestEnvelope {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isGetOperationResultRequestEnvelope(value)) throw new TypeError("invalid GetOperationResultRequestEnvelope");
  return value;
}

export function encodeGetOperationResultRequestEnvelope(value: GetOperationResultRequestEnvelope): string {
  validateProgrammaticWireValue(value);
  if (!isGetOperationResultRequestEnvelope(value)) throw new TypeError("invalid GetOperationResultRequestEnvelope");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isGetOperationResultRequestEnvelope(emitted)) throw new TypeError("encoded value is invalid GetOperationResultRequestEnvelope");
  return encoded;
}

export interface GetOperationResultResponseEnvelope {
  diagnostics: ReadonlyArray<ProtocolDiagnostic>;
  extensions?: Extensions;
  failure?: ProtocolFailure;
  host_release: ReleaseVersion;
  outcome: Outcome;
  payload?: RuntimeOperationStatus;
  protocol: RuntimeProtocolRef;
  request_id: string;
}

export function isGetOperationResultResponseEnvelope(value: unknown): value is GetOperationResultResponseEnvelope {
  return isProgrammaticWireValue(value, () => isObject(value) && hasOnlyKeys(value, new Set(["diagnostics", "extensions", "failure", "host_release", "outcome", "payload", "protocol", "request_id"])) && hasOwn(value, "diagnostics") && (isJSONArray(value["diagnostics"]) && value["diagnostics"].every((item) => isProtocolDiagnostic(item)) && value["diagnostics"].length <= 256) && (!hasOwn(value, "extensions") || (isExtensions(value["extensions"]))) && (!hasOwn(value, "failure") || (isProtocolFailure(value["failure"]))) && hasOwn(value, "host_release") && (isReleaseVersion(value["host_release"])) && hasOwn(value, "outcome") && (isOutcome(value["outcome"])) && (!hasOwn(value, "payload") || (isRuntimeOperationStatus(value["payload"]))) && hasOwn(value, "protocol") && (isRuntimeProtocolRef(value["protocol"])) && hasOwn(value, "request_id") && (typeof value["request_id"] === "string" && hasScalarUnicode(value["request_id"]) && Array.from(value["request_id"]).length >= 1 && Array.from(value["request_id"]).length <= 128) && hasValidOutcomeEnvelope(value));
}

export function decodeGetOperationResultResponseEnvelope(input: string): GetOperationResultResponseEnvelope {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isGetOperationResultResponseEnvelope(value)) throw new TypeError("invalid GetOperationResultResponseEnvelope");
  return value;
}

export function encodeGetOperationResultResponseEnvelope(value: GetOperationResultResponseEnvelope): string {
  validateProgrammaticWireValue(value);
  if (!isGetOperationResultResponseEnvelope(value)) throw new TypeError("invalid GetOperationResultResponseEnvelope");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isGetOperationResultResponseEnvelope(emitted)) throw new TypeError("encoded value is invalid GetOperationResultResponseEnvelope");
  return encoded;
}

export interface HandshakeResponseEnvelopeBase {
  diagnostics: ReadonlyArray<ProtocolDiagnostic>;
  failure?: ProtocolFailure;
  host_release?: ReleaseVersion;
  outcome: Outcome;
  payload?: RuntimeHandshakeResult;
  protocol: RuntimeProtocolRef;
  request_id: string;
}

export function isHandshakeResponseEnvelopeBase(value: unknown): value is HandshakeResponseEnvelopeBase {
  return isProgrammaticWireValue(value, () => isObject(value) && hasOnlyKeys(value, new Set(["diagnostics", "failure", "host_release", "outcome", "payload", "protocol", "request_id"])) && hasOwn(value, "diagnostics") && (isJSONArray(value["diagnostics"]) && value["diagnostics"].every((item) => isProtocolDiagnostic(item)) && value["diagnostics"].length <= 256) && (!hasOwn(value, "failure") || (isProtocolFailure(value["failure"]))) && (!hasOwn(value, "host_release") || (isReleaseVersion(value["host_release"]))) && hasOwn(value, "outcome") && (isOutcome(value["outcome"])) && (!hasOwn(value, "payload") || (isRuntimeHandshakeResult(value["payload"]))) && hasOwn(value, "protocol") && (isRuntimeProtocolRef(value["protocol"])) && hasOwn(value, "request_id") && (typeof value["request_id"] === "string" && hasScalarUnicode(value["request_id"]) && Array.from(value["request_id"]).length >= 1 && Array.from(value["request_id"]).length <= 128) && hasValidOutcomeEnvelope(value));
}

export function decodeHandshakeResponseEnvelopeBase(input: string): HandshakeResponseEnvelopeBase {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isHandshakeResponseEnvelopeBase(value)) throw new TypeError("invalid HandshakeResponseEnvelopeBase");
  return value;
}

export function encodeHandshakeResponseEnvelopeBase(value: HandshakeResponseEnvelopeBase): string {
  validateProgrammaticWireValue(value);
  if (!isHandshakeResponseEnvelopeBase(value)) throw new TypeError("invalid HandshakeResponseEnvelopeBase");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isHandshakeResponseEnvelopeBase(emitted)) throw new TypeError("encoded value is invalid HandshakeResponseEnvelopeBase");
  return encoded;
}

export type IdempotencyKey = string;

export function isIdempotencyKey(value: unknown): value is IdempotencyKey {
  return isProgrammaticWireValue(value, () => typeof value === "string" && hasScalarUnicode(value) && new RegExp("^[A-Za-z0-9][A-Za-z0-9._:-]*$").test(value) && Array.from(value).length >= 16 && Array.from(value).length <= 256);
}

export function decodeIdempotencyKey(input: string): IdempotencyKey {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isIdempotencyKey(value)) throw new TypeError("invalid IdempotencyKey");
  return value;
}

export function encodeIdempotencyKey(value: IdempotencyKey): string {
  validateProgrammaticWireValue(value);
  if (!isIdempotencyKey(value)) throw new TypeError("invalid IdempotencyKey");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isIdempotencyKey(emitted)) throw new TypeError("encoded value is invalid IdempotencyKey");
  return encoded;
}

export type LeaseToken = string;

export function isLeaseToken(value: unknown): value is LeaseToken {
  return isProgrammaticWireValue(value, () => typeof value === "string" && hasScalarUnicode(value) && new RegExp("^[A-Za-z0-9_-]+$").test(value) && Array.from(value).length >= 16 && Array.from(value).length <= 512);
}

export function decodeLeaseToken(input: string): LeaseToken {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isLeaseToken(value)) throw new TypeError("invalid LeaseToken");
  return value;
}

export function encodeLeaseToken(value: LeaseToken): string {
  validateProgrammaticWireValue(value);
  if (!isLeaseToken(value)) throw new TypeError("invalid LeaseToken");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isLeaseToken(emitted)) throw new TypeError("encoded value is invalid LeaseToken");
  return encoded;
}

export interface ListRevisionsInput {
  cursor?: RuntimeCursor;
  max_items: CanonicalPositiveSafeInteger;
  max_output_bytes: CanonicalPositiveInt64;
  session: RuntimeSessionRef;
}

export function isListRevisionsInput(value: unknown): value is ListRevisionsInput {
  return isProgrammaticWireValue(value, () => isObject(value) && hasOnlyKeys(value, new Set(["cursor", "max_items", "max_output_bytes", "session"])) && (!hasOwn(value, "cursor") || (isRuntimeCursor(value["cursor"]))) && hasOwn(value, "max_items") && (isCanonicalPositiveSafeInteger(value["max_items"])) && hasOwn(value, "max_output_bytes") && (isCanonicalPositiveInt64(value["max_output_bytes"])) && hasOwn(value, "session") && (isRuntimeSessionRef(value["session"])));
}

export function decodeListRevisionsInput(input: string): ListRevisionsInput {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isListRevisionsInput(value)) throw new TypeError("invalid ListRevisionsInput");
  return value;
}

export function encodeListRevisionsInput(value: ListRevisionsInput): string {
  validateProgrammaticWireValue(value);
  if (!isListRevisionsInput(value)) throw new TypeError("invalid ListRevisionsInput");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isListRevisionsInput(emitted)) throw new TypeError("encoded value is invalid ListRevisionsInput");
  return encoded;
}

export interface ListRevisionsRequestEnvelope {
  deadline_at?: Rfc3339Time;
  operation: "runtime.list_revisions";
  payload: ListRevisionsInput;
  protocol: RuntimeProtocolRef;
  request_id: string;
}

export function isListRevisionsRequestEnvelope(value: unknown): value is ListRevisionsRequestEnvelope {
  return isProgrammaticWireValue(value, () => isObject(value) && hasOnlyKeys(value, new Set(["deadline_at", "operation", "payload", "protocol", "request_id"])) && (!hasOwn(value, "deadline_at") || (isRfc3339Time(value["deadline_at"]))) && hasOwn(value, "operation") && (typeof value["operation"] === "string" && hasScalarUnicode(value["operation"]) && value["operation"] === "runtime.list_revisions") && hasOwn(value, "payload") && (isListRevisionsInput(value["payload"])) && hasOwn(value, "protocol") && (isRuntimeProtocolRef(value["protocol"])) && hasOwn(value, "request_id") && (typeof value["request_id"] === "string" && hasScalarUnicode(value["request_id"]) && Array.from(value["request_id"]).length >= 1 && Array.from(value["request_id"]).length <= 128));
}

export function decodeListRevisionsRequestEnvelope(input: string): ListRevisionsRequestEnvelope {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isListRevisionsRequestEnvelope(value)) throw new TypeError("invalid ListRevisionsRequestEnvelope");
  return value;
}

export function encodeListRevisionsRequestEnvelope(value: ListRevisionsRequestEnvelope): string {
  validateProgrammaticWireValue(value);
  if (!isListRevisionsRequestEnvelope(value)) throw new TypeError("invalid ListRevisionsRequestEnvelope");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isListRevisionsRequestEnvelope(emitted)) throw new TypeError("encoded value is invalid ListRevisionsRequestEnvelope");
  return encoded;
}

export interface ListRevisionsResponseEnvelope {
  diagnostics: ReadonlyArray<ProtocolDiagnostic>;
  extensions?: Extensions;
  failure?: ProtocolFailure;
  host_release: ReleaseVersion;
  outcome: Outcome;
  payload?: RevisionPage;
  protocol: RuntimeProtocolRef;
  request_id: string;
}

export function isListRevisionsResponseEnvelope(value: unknown): value is ListRevisionsResponseEnvelope {
  return isProgrammaticWireValue(value, () => isObject(value) && hasOnlyKeys(value, new Set(["diagnostics", "extensions", "failure", "host_release", "outcome", "payload", "protocol", "request_id"])) && hasOwn(value, "diagnostics") && (isJSONArray(value["diagnostics"]) && value["diagnostics"].every((item) => isProtocolDiagnostic(item)) && value["diagnostics"].length <= 256) && (!hasOwn(value, "extensions") || (isExtensions(value["extensions"]))) && (!hasOwn(value, "failure") || (isProtocolFailure(value["failure"]))) && hasOwn(value, "host_release") && (isReleaseVersion(value["host_release"])) && hasOwn(value, "outcome") && (isOutcome(value["outcome"])) && (!hasOwn(value, "payload") || (isRevisionPage(value["payload"]))) && hasOwn(value, "protocol") && (isRuntimeProtocolRef(value["protocol"])) && hasOwn(value, "request_id") && (typeof value["request_id"] === "string" && hasScalarUnicode(value["request_id"]) && Array.from(value["request_id"]).length >= 1 && Array.from(value["request_id"]).length <= 128) && hasValidOutcomeEnvelope(value));
}

export function decodeListRevisionsResponseEnvelope(input: string): ListRevisionsResponseEnvelope {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isListRevisionsResponseEnvelope(value)) throw new TypeError("invalid ListRevisionsResponseEnvelope");
  return value;
}

export function encodeListRevisionsResponseEnvelope(value: ListRevisionsResponseEnvelope): string {
  validateProgrammaticWireValue(value);
  if (!isListRevisionsResponseEnvelope(value)) throw new TypeError("invalid ListRevisionsResponseEnvelope");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isListRevisionsResponseEnvelope(emitted)) throw new TypeError("encoded value is invalid ListRevisionsResponseEnvelope");
  return encoded;
}

export interface OpenDocumentRequestEnvelope {
  deadline_at?: Rfc3339Time;
  operation: "runtime.open_document";
  payload: OpenRuntimeDocumentInput;
  protocol: RuntimeProtocolRef;
  request_id: string;
}

export function isOpenDocumentRequestEnvelope(value: unknown): value is OpenDocumentRequestEnvelope {
  return isProgrammaticWireValue(value, () => isObject(value) && hasOnlyKeys(value, new Set(["deadline_at", "operation", "payload", "protocol", "request_id"])) && (!hasOwn(value, "deadline_at") || (isRfc3339Time(value["deadline_at"]))) && hasOwn(value, "operation") && (typeof value["operation"] === "string" && hasScalarUnicode(value["operation"]) && value["operation"] === "runtime.open_document") && hasOwn(value, "payload") && (isOpenRuntimeDocumentInput(value["payload"])) && hasOwn(value, "protocol") && (isRuntimeProtocolRef(value["protocol"])) && hasOwn(value, "request_id") && (typeof value["request_id"] === "string" && hasScalarUnicode(value["request_id"]) && Array.from(value["request_id"]).length >= 1 && Array.from(value["request_id"]).length <= 128));
}

export function decodeOpenDocumentRequestEnvelope(input: string): OpenDocumentRequestEnvelope {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isOpenDocumentRequestEnvelope(value)) throw new TypeError("invalid OpenDocumentRequestEnvelope");
  return value;
}

export function encodeOpenDocumentRequestEnvelope(value: OpenDocumentRequestEnvelope): string {
  validateProgrammaticWireValue(value);
  if (!isOpenDocumentRequestEnvelope(value)) throw new TypeError("invalid OpenDocumentRequestEnvelope");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isOpenDocumentRequestEnvelope(emitted)) throw new TypeError("encoded value is invalid OpenDocumentRequestEnvelope");
  return encoded;
}

export interface OpenDocumentResponseEnvelope {
  diagnostics: ReadonlyArray<ProtocolDiagnostic>;
  extensions?: Extensions;
  failure?: ProtocolFailure;
  host_release: ReleaseVersion;
  outcome: Outcome;
  payload?: OpenRuntimeDocumentResult;
  protocol: RuntimeProtocolRef;
  request_id: string;
}

export function isOpenDocumentResponseEnvelope(value: unknown): value is OpenDocumentResponseEnvelope {
  return isProgrammaticWireValue(value, () => isObject(value) && hasOnlyKeys(value, new Set(["diagnostics", "extensions", "failure", "host_release", "outcome", "payload", "protocol", "request_id"])) && hasOwn(value, "diagnostics") && (isJSONArray(value["diagnostics"]) && value["diagnostics"].every((item) => isProtocolDiagnostic(item)) && value["diagnostics"].length <= 256) && (!hasOwn(value, "extensions") || (isExtensions(value["extensions"]))) && (!hasOwn(value, "failure") || (isProtocolFailure(value["failure"]))) && hasOwn(value, "host_release") && (isReleaseVersion(value["host_release"])) && hasOwn(value, "outcome") && (isOutcome(value["outcome"])) && (!hasOwn(value, "payload") || (isOpenRuntimeDocumentResult(value["payload"]))) && hasOwn(value, "protocol") && (isRuntimeProtocolRef(value["protocol"])) && hasOwn(value, "request_id") && (typeof value["request_id"] === "string" && hasScalarUnicode(value["request_id"]) && Array.from(value["request_id"]).length >= 1 && Array.from(value["request_id"]).length <= 128) && hasValidOutcomeEnvelope(value));
}

export function decodeOpenDocumentResponseEnvelope(input: string): OpenDocumentResponseEnvelope {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isOpenDocumentResponseEnvelope(value)) throw new TypeError("invalid OpenDocumentResponseEnvelope");
  return value;
}

export function encodeOpenDocumentResponseEnvelope(value: OpenDocumentResponseEnvelope): string {
  validateProgrammaticWireValue(value);
  if (!isOpenDocumentResponseEnvelope(value)) throw new TypeError("invalid OpenDocumentResponseEnvelope");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isOpenDocumentResponseEnvelope(emitted)) throw new TypeError("encoded value is invalid OpenDocumentResponseEnvelope");
  return encoded;
}

export interface OpenRuntimeDocumentInput {
  document_id: DocumentID;
  requested_revision_id?: RevisionID;
}

export function isOpenRuntimeDocumentInput(value: unknown): value is OpenRuntimeDocumentInput {
  return isProgrammaticWireValue(value, () => isObject(value) && hasOnlyKeys(value, new Set(["document_id", "requested_revision_id"])) && hasOwn(value, "document_id") && (isDocumentID(value["document_id"])) && (!hasOwn(value, "requested_revision_id") || (isRevisionID(value["requested_revision_id"]))));
}

export function decodeOpenRuntimeDocumentInput(input: string): OpenRuntimeDocumentInput {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isOpenRuntimeDocumentInput(value)) throw new TypeError("invalid OpenRuntimeDocumentInput");
  return value;
}

export function encodeOpenRuntimeDocumentInput(value: OpenRuntimeDocumentInput): string {
  validateProgrammaticWireValue(value);
  if (!isOpenRuntimeDocumentInput(value)) throw new TypeError("invalid OpenRuntimeDocumentInput");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isOpenRuntimeDocumentInput(emitted)) throw new TypeError("encoded value is invalid OpenRuntimeDocumentInput");
  return encoded;
}

export interface OpenRuntimeDocumentResult {
  access_summary: AuthoringGrantSummary;
  capability_manifest: RuntimeCapabilityManifest;
  committed_revision: CommittedRevisionRef;
  session: RuntimeSessionRef;
  state_input: StateInput;
  working_document: WorkingDocumentRef;
}

export function isOpenRuntimeDocumentResult(value: unknown): value is OpenRuntimeDocumentResult {
  return isProgrammaticWireValue(value, () => isObject(value) && hasOnlyKeys(value, new Set(["access_summary", "capability_manifest", "committed_revision", "session", "state_input", "working_document"])) && hasOwn(value, "access_summary") && (isAuthoringGrantSummary(value["access_summary"])) && hasOwn(value, "capability_manifest") && (isRuntimeCapabilityManifest(value["capability_manifest"])) && hasOwn(value, "committed_revision") && (isCommittedRevisionRef(value["committed_revision"])) && hasOwn(value, "session") && (isRuntimeSessionRef(value["session"])) && hasOwn(value, "state_input") && (isStateInput(value["state_input"])) && hasOwn(value, "working_document") && (isWorkingDocumentRef(value["working_document"])));
}

export function decodeOpenRuntimeDocumentResult(input: string): OpenRuntimeDocumentResult {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isOpenRuntimeDocumentResult(value)) throw new TypeError("invalid OpenRuntimeDocumentResult");
  return value;
}

export function encodeOpenRuntimeDocumentResult(value: OpenRuntimeDocumentResult): string {
  validateProgrammaticWireValue(value);
  if (!isOpenRuntimeDocumentResult(value)) throw new TypeError("invalid OpenRuntimeDocumentResult");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isOpenRuntimeDocumentResult(emitted)) throw new TypeError("encoded value is invalid OpenRuntimeDocumentResult");
  return encoded;
}

export type OperationID = string;

export function isOperationID(value: unknown): value is OperationID {
  return isProgrammaticWireValue(value, () => typeof value === "string" && hasScalarUnicode(value) && new RegExp("^[A-Za-z0-9][A-Za-z0-9._:-]*$").test(value) && Array.from(value).length >= 1 && Array.from(value).length <= 256);
}

export function decodeOperationID(input: string): OperationID {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isOperationID(value)) throw new TypeError("invalid OperationID");
  return value;
}

export function encodeOperationID(value: OperationID): string {
  validateProgrammaticWireValue(value);
  if (!isOperationID(value)) throw new TypeError("invalid OperationID");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isOperationID(emitted)) throw new TypeError("encoded value is invalid OperationID");
  return encoded;
}

export interface OperationResult {
  committed_revision?: CommittedRevisionRef;
  diagnostics: ReadonlyArray<ProtocolDiagnostic>;
  failure_code?: RuntimeFailureCode;
  idempotency_key: IdempotencyKey;
  operation_id: OperationID;
  result_digest: Digest;
  state_version?: CanonicalNonNegativeInt64;
  status: OperationResultStatus;
}

export function isOperationResult(value: unknown): value is OperationResult {
  return isProgrammaticWireValue(value, () => isObject(value) && hasOnlyKeys(value, new Set(["committed_revision", "diagnostics", "failure_code", "idempotency_key", "operation_id", "result_digest", "state_version", "status"])) && (!hasOwn(value, "committed_revision") || (isCommittedRevisionRef(value["committed_revision"]))) && hasOwn(value, "diagnostics") && (isJSONArray(value["diagnostics"]) && value["diagnostics"].every((item) => isProtocolDiagnostic(item)) && value["diagnostics"].length <= 256) && (!hasOwn(value, "failure_code") || (isRuntimeFailureCode(value["failure_code"]))) && hasOwn(value, "idempotency_key") && (isIdempotencyKey(value["idempotency_key"])) && hasOwn(value, "operation_id") && (isOperationID(value["operation_id"])) && hasOwn(value, "result_digest") && (isDigest(value["result_digest"])) && (!hasOwn(value, "state_version") || (isCanonicalNonNegativeInt64(value["state_version"]))) && hasOwn(value, "status") && (isOperationResultStatus(value["status"])) && ((value["status"] === "committed" && hasOwn(value, "committed_revision") && !hasOwn(value, "failure_code")) || (value["status"] === "committed_state_stale" && hasOwn(value, "committed_revision") && hasOwn(value, "state_version") && !hasOwn(value, "failure_code")) || (value["status"] === "needs_review" && !hasOwn(value, "failure_code")) || (value["status"] === "rejected" && hasOwn(value, "failure_code") && !hasOwn(value, "committed_revision") && !hasOwn(value, "state_version"))));
}

export function decodeOperationResult(input: string): OperationResult {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isOperationResult(value)) throw new TypeError("invalid OperationResult");
  return value;
}

export function encodeOperationResult(value: OperationResult): string {
  validateProgrammaticWireValue(value);
  if (!isOperationResult(value)) throw new TypeError("invalid OperationResult");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isOperationResult(emitted)) throw new TypeError("encoded value is invalid OperationResult");
  return encoded;
}

export type OperationResultStatus = "committed" | "committed_state_stale" | "needs_review" | "rejected";

export function isOperationResultStatus(value: unknown): value is OperationResultStatus {
  return isProgrammaticWireValue(value, () => typeof value === "string" && hasScalarUnicode(value) && ["committed", "committed_state_stale", "needs_review", "rejected"].includes(value));
}

export function decodeOperationResultStatus(input: string): OperationResultStatus {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isOperationResultStatus(value)) throw new TypeError("invalid OperationResultStatus");
  return value;
}

export function encodeOperationResultStatus(value: OperationResultStatus): string {
  validateProgrammaticWireValue(value);
  if (!isOperationResultStatus(value)) throw new TypeError("invalid OperationResultStatus");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isOperationResultStatus(emitted)) throw new TypeError("encoded value is invalid OperationResultStatus");
  return encoded;
}

export type ProviderVersionToken = string;

export function isProviderVersionToken(value: unknown): value is ProviderVersionToken {
  return isProgrammaticWireValue(value, () => typeof value === "string" && hasScalarUnicode(value) && Array.from(value).length >= 1 && Array.from(value).length <= 1024);
}

export function decodeProviderVersionToken(input: string): ProviderVersionToken {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isProviderVersionToken(value)) throw new TypeError("invalid ProviderVersionToken");
  return value;
}

export function encodeProviderVersionToken(value: ProviderVersionToken): string {
  validateProgrammaticWireValue(value);
  if (!isProviderVersionToken(value)) throw new TypeError("invalid ProviderVersionToken");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isProviderVersionToken(emitted)) throw new TypeError("encoded value is invalid ProviderVersionToken");
  return encoded;
}

export type RecoveryPhase = "audit_pending" | "final" | "needs_review" | "outbox_ready" | "pending" | "publication_pending" | "published" | "recovering" | "staged" | "state_pending";

export function isRecoveryPhase(value: unknown): value is RecoveryPhase {
  return isProgrammaticWireValue(value, () => typeof value === "string" && hasScalarUnicode(value) && ["audit_pending", "final", "needs_review", "outbox_ready", "pending", "publication_pending", "published", "recovering", "staged", "state_pending"].includes(value));
}

export function decodeRecoveryPhase(input: string): RecoveryPhase {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isRecoveryPhase(value)) throw new TypeError("invalid RecoveryPhase");
  return value;
}

export function encodeRecoveryPhase(value: RecoveryPhase): string {
  validateProgrammaticWireValue(value);
  if (!isRecoveryPhase(value)) throw new TypeError("invalid RecoveryPhase");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isRecoveryPhase(emitted)) throw new TypeError("encoded value is invalid RecoveryPhase");
  return encoded;
}

export type RevisionID = string;

export function isRevisionID(value: unknown): value is RevisionID {
  return isProgrammaticWireValue(value, () => typeof value === "string" && hasScalarUnicode(value) && Array.from(value).length >= 1 && Array.from(value).length <= 256);
}

export function decodeRevisionID(input: string): RevisionID {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isRevisionID(value)) throw new TypeError("invalid RevisionID");
  return value;
}

export function encodeRevisionID(value: RevisionID): string {
  validateProgrammaticWireValue(value);
  if (!isRevisionID(value)) throw new TypeError("invalid RevisionID");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isRevisionID(emitted)) throw new TypeError("encoded value is invalid RevisionID");
  return encoded;
}

export interface RevisionMetadata {
  authoring_decision_digest: Digest;
  committed_at: Rfc3339Time;
  operation_id: OperationID;
  parent_revision_id?: RevisionID;
  revision: CommittedRevisionRef;
  trigger: CommitTrigger;
}

export function isRevisionMetadata(value: unknown): value is RevisionMetadata {
  return isProgrammaticWireValue(value, () => isObject(value) && hasOnlyKeys(value, new Set(["authoring_decision_digest", "committed_at", "operation_id", "parent_revision_id", "revision", "trigger"])) && hasOwn(value, "authoring_decision_digest") && (isDigest(value["authoring_decision_digest"])) && hasOwn(value, "committed_at") && (isRfc3339Time(value["committed_at"])) && hasOwn(value, "operation_id") && (isOperationID(value["operation_id"])) && (!hasOwn(value, "parent_revision_id") || (isRevisionID(value["parent_revision_id"]))) && hasOwn(value, "revision") && (isCommittedRevisionRef(value["revision"])) && hasOwn(value, "trigger") && (isCommitTrigger(value["trigger"])));
}

export function decodeRevisionMetadata(input: string): RevisionMetadata {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isRevisionMetadata(value)) throw new TypeError("invalid RevisionMetadata");
  return value;
}

export function encodeRevisionMetadata(value: RevisionMetadata): string {
  validateProgrammaticWireValue(value);
  if (!isRevisionMetadata(value)) throw new TypeError("invalid RevisionMetadata");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isRevisionMetadata(emitted)) throw new TypeError("encoded value is invalid RevisionMetadata");
  return encoded;
}

export interface RevisionPage {
  items: ReadonlyArray<RevisionMetadata>;
  page: PageInfo;
}

export function isRevisionPage(value: unknown): value is RevisionPage {
  return isProgrammaticWireValue(value, () => isObject(value) && hasOnlyKeys(value, new Set(["items", "page"])) && hasOwn(value, "items") && (isJSONArray(value["items"]) && value["items"].every((item) => isRevisionMetadata(item)) && value["items"].length <= 1024) && hasOwn(value, "page") && (isPageInfo(value["page"])));
}

export function decodeRevisionPage(input: string): RevisionPage {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isRevisionPage(value)) throw new TypeError("invalid RevisionPage");
  return value;
}

export function encodeRevisionPage(value: RevisionPage): string {
  validateProgrammaticWireValue(value);
  if (!isRevisionPage(value)) throw new TypeError("invalid RevisionPage");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isRevisionPage(emitted)) throw new TypeError("encoded value is invalid RevisionPage");
  return encoded;
}

export interface RuntimeBlobRef {
  blob: BlobRef;
  expires_at?: Rfc3339Time;
  scope: RuntimeScope;
  session_generation: RuntimeSessionGeneration;
}

export function isRuntimeBlobRef(value: unknown): value is RuntimeBlobRef {
  return isProgrammaticWireValue(value, () => isObject(value) && hasOnlyKeys(value, new Set(["blob", "expires_at", "scope", "session_generation"])) && hasOwn(value, "blob") && (isBlobRef(value["blob"])) && (!hasOwn(value, "expires_at") || (isRfc3339Time(value["expires_at"]))) && hasOwn(value, "scope") && (isRuntimeScope(value["scope"])) && hasOwn(value, "session_generation") && (isRuntimeSessionGeneration(value["session_generation"])));
}

export function decodeRuntimeBlobRef(input: string): RuntimeBlobRef {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isRuntimeBlobRef(value)) throw new TypeError("invalid RuntimeBlobRef");
  return value;
}

export function encodeRuntimeBlobRef(value: RuntimeBlobRef): string {
  validateProgrammaticWireValue(value);
  if (!isRuntimeBlobRef(value)) throw new TypeError("invalid RuntimeBlobRef");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isRuntimeBlobRef(emitted)) throw new TypeError("encoded value is invalid RuntimeBlobRef");
  return encoded;
}

export interface RuntimeByteLimitValue {
  hard_maximum: CanonicalPositiveInt64;
  unit: "bytes";
}

export function isRuntimeByteLimitValue(value: unknown): value is RuntimeByteLimitValue {
  return isProgrammaticWireValue(value, () => isObject(value) && hasOnlyKeys(value, new Set(["hard_maximum", "unit"])) && hasOwn(value, "hard_maximum") && (isCanonicalPositiveInt64(value["hard_maximum"])) && hasOwn(value, "unit") && (typeof value["unit"] === "string" && hasScalarUnicode(value["unit"]) && value["unit"] === "bytes"));
}

export function decodeRuntimeByteLimitValue(input: string): RuntimeByteLimitValue {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isRuntimeByteLimitValue(value)) throw new TypeError("invalid RuntimeByteLimitValue");
  return value;
}

export function encodeRuntimeByteLimitValue(value: RuntimeByteLimitValue): string {
  validateProgrammaticWireValue(value);
  if (!isRuntimeByteLimitValue(value)) throw new TypeError("invalid RuntimeByteLimitValue");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isRuntimeByteLimitValue(emitted)) throw new TypeError("encoded value is invalid RuntimeByteLimitValue");
  return encoded;
}

/** Only runtime operations backed by explicit typed host implementations may be enabled. */
export interface RuntimeCapabilityManifest {
  authoring_grant_summary?: AuthoringGrantSummary;
  limits: RuntimeLimits;
  manifest_etag: ManifestETag;
  operations: { readonly [key: string]: OperationCapability };
  storage_capabilities: ReadonlyArray<"assets" | "conditional_document_head" | "history" | "recovery_journal" | "state">;
}

export function isRuntimeCapabilityManifest(value: unknown): value is RuntimeCapabilityManifest {
  return isProgrammaticWireValue(value, () => isObject(value) && hasOnlyKeys(value, new Set(["authoring_grant_summary", "limits", "manifest_etag", "operations", "storage_capabilities"])) && (!hasOwn(value, "authoring_grant_summary") || (isAuthoringGrantSummary(value["authoring_grant_summary"]))) && hasOwn(value, "limits") && (isRuntimeLimits(value["limits"])) && hasOwn(value, "manifest_etag") && (isManifestETag(value["manifest_etag"])) && hasOwn(value, "operations") && (isObject(value["operations"]) && Object.values(value["operations"]).every((item) => isOperationCapability(item))) && hasOwn(value, "storage_capabilities") && (isJSONArray(value["storage_capabilities"]) && value["storage_capabilities"].every((item) => typeof item === "string" && hasScalarUnicode(item) && ["assets", "conditional_document_head", "history", "recovery_journal", "state"].includes(item)) && hasUniqueItems(value["storage_capabilities"])));
}

export function decodeRuntimeCapabilityManifest(input: string): RuntimeCapabilityManifest {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isRuntimeCapabilityManifest(value)) throw new TypeError("invalid RuntimeCapabilityManifest");
  return value;
}

export function encodeRuntimeCapabilityManifest(value: RuntimeCapabilityManifest): string {
  validateProgrammaticWireValue(value);
  if (!isRuntimeCapabilityManifest(value)) throw new TypeError("invalid RuntimeCapabilityManifest");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isRuntimeCapabilityManifest(emitted)) throw new TypeError("encoded value is invalid RuntimeCapabilityManifest");
  return encoded;
}

export interface RuntimeCommitInput {
  authoring_proof: AuthoringProof;
  cancellation_token?: CancellationToken;
  expected_definition_hash: Digest;
  expected_revision: CommittedRevisionRef;
  idempotency_key: IdempotencyKey;
  lease_token?: LeaseToken;
  operation_batch: SemanticOperationBatch;
  operation_id: OperationID;
  session: RuntimeSessionRef;
  state_mutation?: StateMutation;
  trigger: CommitTrigger;
}

export function isRuntimeCommitInput(value: unknown): value is RuntimeCommitInput {
  return isProgrammaticWireValue(value, () => isObject(value) && hasOnlyKeys(value, new Set(["authoring_proof", "cancellation_token", "expected_definition_hash", "expected_revision", "idempotency_key", "lease_token", "operation_batch", "operation_id", "session", "state_mutation", "trigger"])) && hasOwn(value, "authoring_proof") && (isAuthoringProof(value["authoring_proof"])) && (!hasOwn(value, "cancellation_token") || (isCancellationToken(value["cancellation_token"]))) && hasOwn(value, "expected_definition_hash") && (isDigest(value["expected_definition_hash"])) && hasOwn(value, "expected_revision") && (isCommittedRevisionRef(value["expected_revision"])) && hasOwn(value, "idempotency_key") && (isIdempotencyKey(value["idempotency_key"])) && (!hasOwn(value, "lease_token") || (isLeaseToken(value["lease_token"]))) && hasOwn(value, "operation_batch") && (isSemanticOperationBatch(value["operation_batch"])) && hasOwn(value, "operation_id") && (isOperationID(value["operation_id"])) && hasOwn(value, "session") && (isRuntimeSessionRef(value["session"])) && (!hasOwn(value, "state_mutation") || (isStateMutation(value["state_mutation"]))) && hasOwn(value, "trigger") && (isCommitTrigger(value["trigger"])));
}

export function decodeRuntimeCommitInput(input: string): RuntimeCommitInput {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isRuntimeCommitInput(value)) throw new TypeError("invalid RuntimeCommitInput");
  return value;
}

export function encodeRuntimeCommitInput(value: RuntimeCommitInput): string {
  validateProgrammaticWireValue(value);
  if (!isRuntimeCommitInput(value)) throw new TypeError("invalid RuntimeCommitInput");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isRuntimeCommitInput(emitted)) throw new TypeError("encoded value is invalid RuntimeCommitInput");
  return encoded;
}

export interface RuntimeCommitResult {
  authoring_decision: AuthoringDecision;
  authoring_impact: AuthoringImpact;
  operation_result: OperationResult;
}

export function isRuntimeCommitResult(value: unknown): value is RuntimeCommitResult {
  return isProgrammaticWireValue(value, () => isObject(value) && hasOnlyKeys(value, new Set(["authoring_decision", "authoring_impact", "operation_result"])) && hasOwn(value, "authoring_decision") && (isAuthoringDecision(value["authoring_decision"])) && hasOwn(value, "authoring_impact") && (isAuthoringImpact(value["authoring_impact"])) && hasOwn(value, "operation_result") && (isOperationResult(value["operation_result"])));
}

export function decodeRuntimeCommitResult(input: string): RuntimeCommitResult {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isRuntimeCommitResult(value)) throw new TypeError("invalid RuntimeCommitResult");
  return value;
}

export function encodeRuntimeCommitResult(value: RuntimeCommitResult): string {
  validateProgrammaticWireValue(value);
  if (!isRuntimeCommitResult(value)) throw new TypeError("invalid RuntimeCommitResult");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isRuntimeCommitResult(emitted)) throw new TypeError("encoded value is invalid RuntimeCommitResult");
  return encoded;
}

export type RuntimeCursor = string;

export function isRuntimeCursor(value: unknown): value is RuntimeCursor {
  return isProgrammaticWireValue(value, () => typeof value === "string" && hasScalarUnicode(value) && new RegExp("^[A-Za-z0-9._~-]+$").test(value) && Array.from(value).length >= 32 && Array.from(value).length <= 2048);
}

export function decodeRuntimeCursor(input: string): RuntimeCursor {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isRuntimeCursor(value)) throw new TypeError("invalid RuntimeCursor");
  return value;
}

export function encodeRuntimeCursor(value: RuntimeCursor): string {
  validateProgrammaticWireValue(value);
  if (!isRuntimeCursor(value)) throw new TypeError("invalid RuntimeCursor");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isRuntimeCursor(emitted)) throw new TypeError("encoded value is invalid RuntimeCursor");
  return encoded;
}

/** Wire shape for an opaque cursor. Schema validation does not authenticate a cursor; Runtime must require an exact match to a trusted issued binding before use. */
export interface RuntimeCursorBinding {
  cursor: RuntimeCursor;
  expires_at: Rfc3339Time;
  normalized_request_digest: Digest;
  operation: string;
  revision: CommittedRevisionRef;
  schema_version: number;
  scope: RuntimeScope;
}

export function isRuntimeCursorBinding(value: unknown): value is RuntimeCursorBinding {
  return isProgrammaticWireValue(value, () => isObject(value) && hasOnlyKeys(value, new Set(["cursor", "expires_at", "normalized_request_digest", "operation", "revision", "schema_version", "scope"])) && hasOwn(value, "cursor") && (isRuntimeCursor(value["cursor"])) && hasOwn(value, "expires_at") && (isRfc3339Time(value["expires_at"])) && hasOwn(value, "normalized_request_digest") && (isDigest(value["normalized_request_digest"])) && hasOwn(value, "operation") && (typeof value["operation"] === "string" && hasScalarUnicode(value["operation"]) && new RegExp("^runtime\\.[a-z][a-z0-9_]*$").test(value["operation"])) && hasOwn(value, "revision") && (isCommittedRevisionRef(value["revision"])) && hasOwn(value, "schema_version") && (typeof value["schema_version"] === "number" && Number.isSafeInteger(value["schema_version"]) && !Object.is(value["schema_version"], -0) && value["schema_version"] >= 1 && value["schema_version"] <= 2.147483647e+09) && hasOwn(value, "scope") && (isRuntimeScope(value["scope"])));
}

export function decodeRuntimeCursorBinding(input: string): RuntimeCursorBinding {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isRuntimeCursorBinding(value)) throw new TypeError("invalid RuntimeCursorBinding");
  return value;
}

export function encodeRuntimeCursorBinding(value: RuntimeCursorBinding): string {
  validateProgrammaticWireValue(value);
  if (!isRuntimeCursorBinding(value)) throw new TypeError("invalid RuntimeCursorBinding");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isRuntimeCursorBinding(emitted)) throw new TypeError("encoded value is invalid RuntimeCursorBinding");
  return encoded;
}

export type RuntimeFailureCode = "runtime.authorization_proof_invalid" | "runtime.authorization_stale" | "runtime.blob_expired" | "runtime.blob_scope_mismatch" | "runtime.cancelled" | "runtime.capability_unavailable" | "runtime.cross_document_handle" | "runtime.cursor_scope_mismatch" | "runtime.idempotency_mismatch" | "runtime.invalid_cursor" | "runtime.invalid_recovery_transition" | "runtime.malformed_handle" | "runtime.revision_scope_mismatch" | "runtime.session_expired" | "runtime.stale_revision" | "runtime.stale_session_generation";

export function isRuntimeFailureCode(value: unknown): value is RuntimeFailureCode {
  return isProgrammaticWireValue(value, () => typeof value === "string" && hasScalarUnicode(value) && ["runtime.authorization_proof_invalid", "runtime.authorization_stale", "runtime.blob_expired", "runtime.blob_scope_mismatch", "runtime.cancelled", "runtime.capability_unavailable", "runtime.cross_document_handle", "runtime.cursor_scope_mismatch", "runtime.idempotency_mismatch", "runtime.invalid_cursor", "runtime.invalid_recovery_transition", "runtime.malformed_handle", "runtime.revision_scope_mismatch", "runtime.session_expired", "runtime.stale_revision", "runtime.stale_session_generation"].includes(value));
}

export function decodeRuntimeFailureCode(input: string): RuntimeFailureCode {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isRuntimeFailureCode(value)) throw new TypeError("invalid RuntimeFailureCode");
  return value;
}

export function encodeRuntimeFailureCode(value: RuntimeFailureCode): string {
  validateProgrammaticWireValue(value);
  if (!isRuntimeFailureCode(value)) throw new TypeError("invalid RuntimeFailureCode");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isRuntimeFailureCode(emitted)) throw new TypeError("encoded value is invalid RuntimeFailureCode");
  return encoded;
}

export interface RuntimeHandshakeRequest {
  client_limits?: RuntimeLimits;
  client_release: ReleaseVersion;
  optional_capabilities: ReadonlyArray<CapabilityID>;
  protocols: ReadonlyArray<ProtocolOffer>;
  required_capabilities: ReadonlyArray<CapabilityID>;
}

export function isRuntimeHandshakeRequest(value: unknown): value is RuntimeHandshakeRequest {
  return isProgrammaticWireValue(value, () => isObject(value) && hasOnlyKeys(value, new Set(["client_limits", "client_release", "optional_capabilities", "protocols", "required_capabilities"])) && (!hasOwn(value, "client_limits") || (isRuntimeLimits(value["client_limits"]))) && hasOwn(value, "client_release") && (isReleaseVersion(value["client_release"])) && hasOwn(value, "optional_capabilities") && (isJSONArray(value["optional_capabilities"]) && value["optional_capabilities"].every((item) => isCapabilityID(item)) && value["optional_capabilities"].length <= 64 && hasUniqueItems(value["optional_capabilities"])) && hasOwn(value, "protocols") && (isJSONArray(value["protocols"]) && value["protocols"].every((item) => isProtocolOffer(item)) && value["protocols"].length >= 1 && value["protocols"].length <= 8) && hasOwn(value, "required_capabilities") && (isJSONArray(value["required_capabilities"]) && value["required_capabilities"].every((item) => isCapabilityID(item)) && value["required_capabilities"].length <= 64 && hasUniqueItems(value["required_capabilities"])) && hasUniqueArrayKey(value, "protocols", "name") && hasDisjointArrays(value, "required_capabilities", "optional_capabilities"));
}

export function decodeRuntimeHandshakeRequest(input: string): RuntimeHandshakeRequest {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isRuntimeHandshakeRequest(value)) throw new TypeError("invalid RuntimeHandshakeRequest");
  return value;
}

export function encodeRuntimeHandshakeRequest(value: RuntimeHandshakeRequest): string {
  validateProgrammaticWireValue(value);
  if (!isRuntimeHandshakeRequest(value)) throw new TypeError("invalid RuntimeHandshakeRequest");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isRuntimeHandshakeRequest(emitted)) throw new TypeError("encoded value is invalid RuntimeHandshakeRequest");
  return encoded;
}

export interface RuntimeHandshakeRequestEnvelope {
  deadline_at?: Rfc3339Time;
  extensions?: Extensions;
  operation: "runtime.handshake";
  payload: RuntimeHandshakeRequest;
  protocol: RuntimeProtocolRef;
  request_id: string;
}

export function isRuntimeHandshakeRequestEnvelope(value: unknown): value is RuntimeHandshakeRequestEnvelope {
  return isProgrammaticWireValue(value, () => isObject(value) && hasOnlyKeys(value, new Set(["deadline_at", "extensions", "operation", "payload", "protocol", "request_id"])) && (!hasOwn(value, "deadline_at") || (isRfc3339Time(value["deadline_at"]))) && (!hasOwn(value, "extensions") || (isExtensions(value["extensions"]))) && hasOwn(value, "operation") && (typeof value["operation"] === "string" && hasScalarUnicode(value["operation"]) && value["operation"] === "runtime.handshake") && hasOwn(value, "payload") && (isRuntimeHandshakeRequest(value["payload"])) && hasOwn(value, "protocol") && (isRuntimeProtocolRef(value["protocol"])) && hasOwn(value, "request_id") && (typeof value["request_id"] === "string" && hasScalarUnicode(value["request_id"]) && Array.from(value["request_id"]).length >= 1 && Array.from(value["request_id"]).length <= 128));
}

export function decodeRuntimeHandshakeRequestEnvelope(input: string): RuntimeHandshakeRequestEnvelope {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isRuntimeHandshakeRequestEnvelope(value)) throw new TypeError("invalid RuntimeHandshakeRequestEnvelope");
  return value;
}

export function encodeRuntimeHandshakeRequestEnvelope(value: RuntimeHandshakeRequestEnvelope): string {
  validateProgrammaticWireValue(value);
  if (!isRuntimeHandshakeRequestEnvelope(value)) throw new TypeError("invalid RuntimeHandshakeRequestEnvelope");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isRuntimeHandshakeRequestEnvelope(emitted)) throw new TypeError("encoded value is invalid RuntimeHandshakeRequestEnvelope");
  return encoded;
}

export type RuntimeHandshakeResponseEnvelope = HandshakeResponseEnvelopeBase;

export function isRuntimeHandshakeResponseEnvelope(value: unknown): value is RuntimeHandshakeResponseEnvelope {
  return isProgrammaticWireValue(value, () => isHandshakeResponseEnvelopeBase(value));
}

export function decodeRuntimeHandshakeResponseEnvelope(input: string): RuntimeHandshakeResponseEnvelope {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isRuntimeHandshakeResponseEnvelope(value)) throw new TypeError("invalid RuntimeHandshakeResponseEnvelope");
  return value;
}

export function encodeRuntimeHandshakeResponseEnvelope(value: RuntimeHandshakeResponseEnvelope): string {
  validateProgrammaticWireValue(value);
  if (!isRuntimeHandshakeResponseEnvelope(value)) throw new TypeError("invalid RuntimeHandshakeResponseEnvelope");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isRuntimeHandshakeResponseEnvelope(emitted)) throw new TypeError("encoded value is invalid RuntimeHandshakeResponseEnvelope");
  return encoded;
}

export interface RuntimeHandshakeResult {
  capability_manifest: RuntimeCapabilityManifest;
  capability_statuses: ReadonlyArray<RequestedCapabilityStatus>;
  endpoint_instance_id: EndpointInstanceID;
  host_release: ReleaseVersion;
  negotiated_protocols: ReadonlyArray<NegotiatedProtocol>;
  release_manifest_digest: Digest;
}

export function isRuntimeHandshakeResult(value: unknown): value is RuntimeHandshakeResult {
  return isProgrammaticWireValue(value, () => isObject(value) && hasOnlyKeys(value, new Set(["capability_manifest", "capability_statuses", "endpoint_instance_id", "host_release", "negotiated_protocols", "release_manifest_digest"])) && hasOwn(value, "capability_manifest") && (isRuntimeCapabilityManifest(value["capability_manifest"])) && hasOwn(value, "capability_statuses") && (isJSONArray(value["capability_statuses"]) && value["capability_statuses"].every((item) => isRequestedCapabilityStatus(item)) && value["capability_statuses"].length <= 128) && hasOwn(value, "endpoint_instance_id") && (isEndpointInstanceID(value["endpoint_instance_id"])) && hasOwn(value, "host_release") && (isReleaseVersion(value["host_release"])) && hasOwn(value, "negotiated_protocols") && (isJSONArray(value["negotiated_protocols"]) && value["negotiated_protocols"].every((item) => isNegotiatedProtocol(item)) && value["negotiated_protocols"].length >= 1 && value["negotiated_protocols"].length <= 8) && hasOwn(value, "release_manifest_digest") && (isDigest(value["release_manifest_digest"])) && hasUniqueArrayKey(value, "capability_statuses", "capability_id") && hasUniqueArrayKey(value, "negotiated_protocols", "name"));
}

export function decodeRuntimeHandshakeResult(input: string): RuntimeHandshakeResult {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isRuntimeHandshakeResult(value)) throw new TypeError("invalid RuntimeHandshakeResult");
  return value;
}

export function encodeRuntimeHandshakeResult(value: RuntimeHandshakeResult): string {
  validateProgrammaticWireValue(value);
  if (!isRuntimeHandshakeResult(value)) throw new TypeError("invalid RuntimeHandshakeResult");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isRuntimeHandshakeResult(emitted)) throw new TypeError("encoded value is invalid RuntimeHandshakeResult");
  return encoded;
}

export interface RuntimeItemLimitValue {
  hard_maximum: CanonicalPositiveInt64;
  unit: "items";
}

export function isRuntimeItemLimitValue(value: unknown): value is RuntimeItemLimitValue {
  return isProgrammaticWireValue(value, () => isObject(value) && hasOnlyKeys(value, new Set(["hard_maximum", "unit"])) && hasOwn(value, "hard_maximum") && (isCanonicalPositiveInt64(value["hard_maximum"])) && hasOwn(value, "unit") && (typeof value["unit"] === "string" && hasScalarUnicode(value["unit"]) && value["unit"] === "items"));
}

export function decodeRuntimeItemLimitValue(input: string): RuntimeItemLimitValue {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isRuntimeItemLimitValue(value)) throw new TypeError("invalid RuntimeItemLimitValue");
  return value;
}

export function encodeRuntimeItemLimitValue(value: RuntimeItemLimitValue): string {
  validateProgrammaticWireValue(value);
  if (!isRuntimeItemLimitValue(value)) throw new TypeError("invalid RuntimeItemLimitValue");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isRuntimeItemLimitValue(emitted)) throw new TypeError("encoded value is invalid RuntimeItemLimitValue");
  return encoded;
}

/** Hard host limits covering operations, result pages, and detached blobs. */
export interface RuntimeLimits {
  max_blob_bytes: RuntimeByteLimitValue;
  max_blob_total_bytes: RuntimeByteLimitValue;
  max_commit_operations: RuntimeItemLimitValue;
  max_history_items: RuntimeItemLimitValue;
  max_output_bytes: RuntimeByteLimitValue;
  max_state_mutations: RuntimeItemLimitValue;
}

export function isRuntimeLimits(value: unknown): value is RuntimeLimits {
  return isProgrammaticWireValue(value, () => isObject(value) && hasOnlyKeys(value, new Set(["max_blob_bytes", "max_blob_total_bytes", "max_commit_operations", "max_history_items", "max_output_bytes", "max_state_mutations"])) && hasOwn(value, "max_blob_bytes") && (isRuntimeByteLimitValue(value["max_blob_bytes"])) && hasOwn(value, "max_blob_total_bytes") && (isRuntimeByteLimitValue(value["max_blob_total_bytes"])) && hasOwn(value, "max_commit_operations") && (isRuntimeItemLimitValue(value["max_commit_operations"])) && hasOwn(value, "max_history_items") && (isRuntimeItemLimitValue(value["max_history_items"])) && hasOwn(value, "max_output_bytes") && (isRuntimeByteLimitValue(value["max_output_bytes"])) && hasOwn(value, "max_state_mutations") && (isRuntimeItemLimitValue(value["max_state_mutations"])));
}

export function decodeRuntimeLimits(input: string): RuntimeLimits {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isRuntimeLimits(value)) throw new TypeError("invalid RuntimeLimits");
  return value;
}

export function encodeRuntimeLimits(value: RuntimeLimits): string {
  validateProgrammaticWireValue(value);
  if (!isRuntimeLimits(value)) throw new TypeError("invalid RuntimeLimits");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isRuntimeLimits(emitted)) throw new TypeError("encoded value is invalid RuntimeLimits");
  return encoded;
}

export interface RuntimeOperationStatus {
  idempotency_key: IdempotencyKey;
  operation_id: OperationID;
  operation_result?: OperationResult;
  phase: RecoveryPhase;
  recovery_started_at?: Rfc3339Time;
  retry_after_ms?: CanonicalUint64;
}

export function isRuntimeOperationStatus(value: unknown): value is RuntimeOperationStatus {
  return isProgrammaticWireValue(value, () => isObject(value) && hasOnlyKeys(value, new Set(["idempotency_key", "operation_id", "operation_result", "phase", "recovery_started_at", "retry_after_ms"])) && hasOwn(value, "idempotency_key") && (isIdempotencyKey(value["idempotency_key"])) && hasOwn(value, "operation_id") && (isOperationID(value["operation_id"])) && (!hasOwn(value, "operation_result") || (isOperationResult(value["operation_result"]))) && hasOwn(value, "phase") && (isRecoveryPhase(value["phase"])) && (!hasOwn(value, "recovery_started_at") || (isRfc3339Time(value["recovery_started_at"]))) && (!hasOwn(value, "retry_after_ms") || (isCanonicalUint64(value["retry_after_ms"]))) && ((value["phase"] === "audit_pending" && !hasOwn(value, "operation_result") && !hasOwn(value, "recovery_started_at")) || (value["phase"] === "final" && hasOwn(value, "operation_result") && !hasOwn(value, "recovery_started_at") && !hasOwn(value, "retry_after_ms")) || (value["phase"] === "needs_review" && hasOwn(value, "operation_result") && !hasOwn(value, "recovery_started_at") && !hasOwn(value, "retry_after_ms")) || (value["phase"] === "outbox_ready" && !hasOwn(value, "operation_result") && !hasOwn(value, "recovery_started_at")) || (value["phase"] === "pending" && !hasOwn(value, "operation_result") && !hasOwn(value, "recovery_started_at")) || (value["phase"] === "publication_pending" && !hasOwn(value, "operation_result") && !hasOwn(value, "recovery_started_at")) || (value["phase"] === "published" && !hasOwn(value, "operation_result") && !hasOwn(value, "recovery_started_at")) || (value["phase"] === "recovering" && hasOwn(value, "recovery_started_at") && !hasOwn(value, "operation_result")) || (value["phase"] === "staged" && !hasOwn(value, "operation_result") && !hasOwn(value, "recovery_started_at")) || (value["phase"] === "state_pending" && !hasOwn(value, "operation_result") && !hasOwn(value, "recovery_started_at"))));
}

export function decodeRuntimeOperationStatus(input: string): RuntimeOperationStatus {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isRuntimeOperationStatus(value)) throw new TypeError("invalid RuntimeOperationStatus");
  return value;
}

export function encodeRuntimeOperationStatus(value: RuntimeOperationStatus): string {
  validateProgrammaticWireValue(value);
  if (!isRuntimeOperationStatus(value)) throw new TypeError("invalid RuntimeOperationStatus");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isRuntimeOperationStatus(emitted)) throw new TypeError("encoded value is invalid RuntimeOperationStatus");
  return encoded;
}

export interface RuntimeProtocolRef {
  name: "runtime";
  version: ProtocolVersion;
}

export function isRuntimeProtocolRef(value: unknown): value is RuntimeProtocolRef {
  return isProgrammaticWireValue(value, () => isObject(value) && hasOnlyKeys(value, new Set(["name", "version"])) && hasOwn(value, "name") && (typeof value["name"] === "string" && hasScalarUnicode(value["name"]) && value["name"] === "runtime") && hasOwn(value, "version") && (isProtocolVersion(value["version"])));
}

export function decodeRuntimeProtocolRef(input: string): RuntimeProtocolRef {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isRuntimeProtocolRef(value)) throw new TypeError("invalid RuntimeProtocolRef");
  return value;
}

export function encodeRuntimeProtocolRef(value: RuntimeProtocolRef): string {
  validateProgrammaticWireValue(value);
  if (!isRuntimeProtocolRef(value)) throw new TypeError("invalid RuntimeProtocolRef");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isRuntimeProtocolRef(emitted)) throw new TypeError("encoded value is invalid RuntimeProtocolRef");
  return encoded;
}

export interface RuntimeScope {
  access_fingerprint: Digest;
  document_id: DocumentID;
  local_scope_id: string;
  organization_scope_id?: string;
}

export function isRuntimeScope(value: unknown): value is RuntimeScope {
  return isProgrammaticWireValue(value, () => isObject(value) && hasOnlyKeys(value, new Set(["access_fingerprint", "document_id", "local_scope_id", "organization_scope_id"])) && hasOwn(value, "access_fingerprint") && (isDigest(value["access_fingerprint"])) && hasOwn(value, "document_id") && (isDocumentID(value["document_id"])) && hasOwn(value, "local_scope_id") && (typeof value["local_scope_id"] === "string" && hasScalarUnicode(value["local_scope_id"]) && Array.from(value["local_scope_id"]).length >= 1 && Array.from(value["local_scope_id"]).length <= 256) && (!hasOwn(value, "organization_scope_id") || (typeof value["organization_scope_id"] === "string" && hasScalarUnicode(value["organization_scope_id"]) && Array.from(value["organization_scope_id"]).length >= 1 && Array.from(value["organization_scope_id"]).length <= 256)));
}

export function decodeRuntimeScope(input: string): RuntimeScope {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isRuntimeScope(value)) throw new TypeError("invalid RuntimeScope");
  return value;
}

export function encodeRuntimeScope(value: RuntimeScope): string {
  validateProgrammaticWireValue(value);
  if (!isRuntimeScope(value)) throw new TypeError("invalid RuntimeScope");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isRuntimeScope(emitted)) throw new TypeError("encoded value is invalid RuntimeScope");
  return encoded;
}

export type RuntimeSessionGeneration = CanonicalPositiveInt64;

export function isRuntimeSessionGeneration(value: unknown): value is RuntimeSessionGeneration {
  return isProgrammaticWireValue(value, () => isCanonicalPositiveInt64(value));
}

export function decodeRuntimeSessionGeneration(input: string): RuntimeSessionGeneration {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isRuntimeSessionGeneration(value)) throw new TypeError("invalid RuntimeSessionGeneration");
  return value;
}

export function encodeRuntimeSessionGeneration(value: RuntimeSessionGeneration): string {
  validateProgrammaticWireValue(value);
  if (!isRuntimeSessionGeneration(value)) throw new TypeError("invalid RuntimeSessionGeneration");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isRuntimeSessionGeneration(emitted)) throw new TypeError("encoded value is invalid RuntimeSessionGeneration");
  return encoded;
}

export type RuntimeSessionID = string;

export function isRuntimeSessionID(value: unknown): value is RuntimeSessionID {
  return isProgrammaticWireValue(value, () => typeof value === "string" && hasScalarUnicode(value) && new RegExp("^[A-Za-z0-9_-]+$").test(value) && Array.from(value).length >= 16 && Array.from(value).length <= 256);
}

export function decodeRuntimeSessionID(input: string): RuntimeSessionID {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isRuntimeSessionID(value)) throw new TypeError("invalid RuntimeSessionID");
  return value;
}

export function encodeRuntimeSessionID(value: RuntimeSessionID): string {
  validateProgrammaticWireValue(value);
  if (!isRuntimeSessionID(value)) throw new TypeError("invalid RuntimeSessionID");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isRuntimeSessionID(emitted)) throw new TypeError("encoded value is invalid RuntimeSessionID");
  return encoded;
}

export interface RuntimeSessionRef {
  expires_at?: Rfc3339Time;
  runtime_session_id: RuntimeSessionID;
  scope: RuntimeScope;
  session_generation: RuntimeSessionGeneration;
}

export function isRuntimeSessionRef(value: unknown): value is RuntimeSessionRef {
  return isProgrammaticWireValue(value, () => isObject(value) && hasOnlyKeys(value, new Set(["expires_at", "runtime_session_id", "scope", "session_generation"])) && (!hasOwn(value, "expires_at") || (isRfc3339Time(value["expires_at"]))) && hasOwn(value, "runtime_session_id") && (isRuntimeSessionID(value["runtime_session_id"])) && hasOwn(value, "scope") && (isRuntimeScope(value["scope"])) && hasOwn(value, "session_generation") && (isRuntimeSessionGeneration(value["session_generation"])));
}

export function decodeRuntimeSessionRef(input: string): RuntimeSessionRef {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isRuntimeSessionRef(value)) throw new TypeError("invalid RuntimeSessionRef");
  return value;
}

export function encodeRuntimeSessionRef(value: RuntimeSessionRef): string {
  validateProgrammaticWireValue(value);
  if (!isRuntimeSessionRef(value)) throw new TypeError("invalid RuntimeSessionRef");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isRuntimeSessionRef(emitted)) throw new TypeError("encoded value is invalid RuntimeSessionRef");
  return encoded;
}

export interface StateInput {
  expected_state_version?: CanonicalNonNegativeInt64;
  kind: "none" | "snapshot";
  snapshot?: StateQuerySnapshot;
  snapshot_hash?: Digest;
}

export function isStateInput(value: unknown): value is StateInput {
  return isProgrammaticWireValue(value, () => isObject(value) && hasOnlyKeys(value, new Set(["expected_state_version", "kind", "snapshot", "snapshot_hash"])) && (!hasOwn(value, "expected_state_version") || (isCanonicalNonNegativeInt64(value["expected_state_version"]))) && hasOwn(value, "kind") && (typeof value["kind"] === "string" && hasScalarUnicode(value["kind"]) && ["none", "snapshot"].includes(value["kind"])) && (!hasOwn(value, "snapshot") || (isStateQuerySnapshot(value["snapshot"]))) && (!hasOwn(value, "snapshot_hash") || (isDigest(value["snapshot_hash"]))) && ((value["kind"] === "none" && !hasOwn(value, "expected_state_version") && !hasOwn(value, "snapshot") && !hasOwn(value, "snapshot_hash")) || (value["kind"] === "snapshot" && hasOwn(value, "expected_state_version") && hasOwn(value, "snapshot") && hasOwn(value, "snapshot_hash"))));
}

export function decodeStateInput(input: string): StateInput {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isStateInput(value)) throw new TypeError("invalid StateInput");
  return value;
}

export function encodeStateInput(value: StateInput): string {
  validateProgrammaticWireValue(value);
  if (!isStateInput(value)) throw new TypeError("invalid StateInput");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isStateInput(emitted)) throw new TypeError("encoded value is invalid StateInput");
  return encoded;
}

export interface StateMutation {
  affected_subjects: ReadonlyArray<StableAddress>;
  expected_state_version: CanonicalNonNegativeInt64;
  mutation_blob: RuntimeBlobRef;
  mutation_digest: Digest;
}

export function isStateMutation(value: unknown): value is StateMutation {
  return isProgrammaticWireValue(value, () => isObject(value) && hasOnlyKeys(value, new Set(["affected_subjects", "expected_state_version", "mutation_blob", "mutation_digest"])) && hasOwn(value, "affected_subjects") && (isJSONArray(value["affected_subjects"]) && value["affected_subjects"].every((item) => isStableAddress(item)) && value["affected_subjects"].length <= 4096 && hasUniqueItems(value["affected_subjects"]) && hasStableAddressOrder(value["affected_subjects"], "$item")) && hasOwn(value, "expected_state_version") && (isCanonicalNonNegativeInt64(value["expected_state_version"])) && hasOwn(value, "mutation_blob") && (isRuntimeBlobRef(value["mutation_blob"])) && hasOwn(value, "mutation_digest") && (isDigest(value["mutation_digest"])));
}

export function decodeStateMutation(input: string): StateMutation {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isStateMutation(value)) throw new TypeError("invalid StateMutation");
  return value;
}

export function encodeStateMutation(value: StateMutation): string {
  validateProgrammaticWireValue(value);
  if (!isStateMutation(value)) throw new TypeError("invalid StateMutation");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isStateMutation(emitted)) throw new TypeError("encoded value is invalid StateMutation");
  return encoded;
}

export interface WorkingDocumentRef {
  base_revision: CommittedRevisionRef;
  session: RuntimeSessionRef;
  working_generation: WorkingGeneration;
}

export function isWorkingDocumentRef(value: unknown): value is WorkingDocumentRef {
  return isProgrammaticWireValue(value, () => isObject(value) && hasOnlyKeys(value, new Set(["base_revision", "session", "working_generation"])) && hasOwn(value, "base_revision") && (isCommittedRevisionRef(value["base_revision"])) && hasOwn(value, "session") && (isRuntimeSessionRef(value["session"])) && hasOwn(value, "working_generation") && (isWorkingGeneration(value["working_generation"])));
}

export function decodeWorkingDocumentRef(input: string): WorkingDocumentRef {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isWorkingDocumentRef(value)) throw new TypeError("invalid WorkingDocumentRef");
  return value;
}

export function encodeWorkingDocumentRef(value: WorkingDocumentRef): string {
  validateProgrammaticWireValue(value);
  if (!isWorkingDocumentRef(value)) throw new TypeError("invalid WorkingDocumentRef");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isWorkingDocumentRef(emitted)) throw new TypeError("encoded value is invalid WorkingDocumentRef");
  return encoded;
}

export type WorkingGeneration = CanonicalNonNegativeInt64;

export function isWorkingGeneration(value: unknown): value is WorkingGeneration {
  return isProgrammaticWireValue(value, () => isCanonicalNonNegativeInt64(value));
}

export function decodeWorkingGeneration(input: string): WorkingGeneration {
  validateWireJSONText(input);
  const value: unknown = JSON.parse(input);
  if (!isWorkingGeneration(value)) throw new TypeError("invalid WorkingGeneration");
  return value;
}

export function encodeWorkingGeneration(value: WorkingGeneration): string {
  validateProgrammaticWireValue(value);
  if (!isWorkingGeneration(value)) throw new TypeError("invalid WorkingGeneration");
  const encoded = canonicalJSONStringify(value);
  validateWireJSONText(encoded);
  const emitted: unknown = JSON.parse(encoded);
  if (!isWorkingGeneration(emitted)) throw new TypeError("encoded value is invalid WorkingGeneration");
  return encoded;
}
